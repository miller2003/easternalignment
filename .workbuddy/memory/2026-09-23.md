# 2026-09-23 工作日志

## 全天流量收盘（23:33）

- 真实口径：**12 会话 / 15 浏览 / 11 用户 / 3 点击**——处近期基线下沿（12-28），后半天几乎无增量。点击仍只有凌晨那位 PG 扫射用户（06:47–06:56，已确认未转化）。今日无 CN 自测点击、无机器集群。
- 来源：Google 6、ChatGPT 3（含全部点击）、Yahoo 1、Bing 1、直访 1。搜索引擎带量不出单的结构连续第三天成立。
- scratch 已被清理（PostHog 脚本全删）；重建了通用日快照脚本 `scratch/posthog_daily_snapshot.py`（改 TODAY 即可复跑）。
## Shave 裁决（12:22，基于 barges getStats CSV + PostHog 全量对照）

- **裁决：无 shave 证据**。剔除 CN/SG 自动化流量后真实点击 269 次 / 27 转化 / **LTR 10.04%**；全部 27 个转化都在 iPhone(23)/Android(4) 上；桌面 Chrome 237 点击 0 转化与 PostHog 流量质量数据一致（机器人+脚本+0秒集群）。累计 $1250。
- **PG 链路完好实证**：barges CH iPhone 3 clicks → 1 Signup + 1 First Purchase $125 —— 即 PostHog 08-31 瑞士移动用户（3 次 PG 点击），全链路对上，PG 首笔 $125。
- 瑞典 Keen 转化落在 offer 209 (Main Offer) 的 Signup goal，非 221。
- **大问题：barges 451 点击中 182 (40%) 是 CN/SG 自动化流量**（深链核对脚本 + 自测 + dev 泄漏），0 转化——污染 EPC 与网络风控画像。全部站点链接都走 bargestech.go2cloud.org TDS，无绕过。
- 点击层逐平台对不上（barges 非 CN/SG vs PostHog）：Kasamba 161 vs 123、PG 54 vs 27（barges 多，疑脚本经其他出口抓取）、Keen 51 vs 79（PostHog 多，疑 barges 连点去重）。**定案需要日期分组的导出**——已请用户重导。
- 分析脚本：`scratch/analyze_barges_stats.py`、`posthog_alltime_platform.py`、`posthog_shave_audit.py`。

- 90 天全量点击（未剔自测）：Kasamba 132、Keen 79、**Purple Garden 仅 31**（其中 08-14 的 7 次是自测）。PG 真实约 24 次/17 用户——样本远不足以定罪 shave。
- PG 点击浏览器结构：iOS Mobile Safari 10 + Chrome iOS 1 ≈ **35% 苹果系（ITP 高危）**，Chrome Windows 13。
- **埋点缺口（重要）**：`aff_go_hit` 90 天 87 条 vs 点击 260 条，且 **session_id / click_id 覆盖率均为 0**——/go 到达无法对单，当前到达率指标不可信，需补埋点。
- 给用户的核查打法：三层对账（PostHog→aff_go_hit→barges raw/unique clicks）→ 抓包跳转链 → 受控自购（独立 subid、真实设备住宅 IP）→ 带 data 找 AM → 多入口分流对冲。

## 今日流量快照（12:07）

- 真实口径：6 会话 / 9 浏览 / 6 用户 / 3 点击（低于昨日同期，但无 0 秒机器集群）。
- **Purple Garden 首次集中点击**：同一美国移动用户（ChatGPT 引流）06:47–06:56 九分钟连点 3 个 PG CTA（niki-medium hero → purplegarden score-panel → athena-love hero）。
- **用户已确认：3 次点击未转化**。且用户纠正：联盟后台名称是 **barges**（不是 Impact——README 内 Impact/TUNE 的提法以用户口头为准，今后统一用 barges）。

## 环境注意

- `scratch/posthog_today_1627.py` 现为通用「今日流量快照」脚本（sed 改日期即可复用）。

## Match quiz 内部内容外泄修复（12:30）

- **问题**：首页 quiz 结果页 "When to Skip" 直接渲染 `readers.json` 的 `cons[0]`（编辑内部审计笔记），如 "Two fresh 1-star reviews this week ('scammer')… we flag them"、"The generic brand name reads like marketing"——推荐卡语境下自拆台且暴露编辑管道。`cons` 同时被嵌入首页 `<script id="ea-readers-data">` 客户端 payload。
- **修复**：① `src/match/engine/explanations.ts` 删除 `whenToSkip = reader.cons[0]` 分支，改为按 reader style 生成的 4 变体安全文案（direct/gentle/fast+structured/默认）；② `ReaderMatch.astro` 的 clientReaders 剔除 `cons`/`pros`，内部笔记不再进首页 HTML。已重建（642 页），浏览器实测泄露词零命中。
- **边界**：`nlMatch.ts` 的 reader audit API（utm_source=chatgpt, MCP 场景）返回 cons 属预期用途，未动；评测页 ProsCons 用的 md frontmatter，语境合理，未动。
- **附带发现（未处理，待用户决定）**：`public/content-manager.html`（40KB 内部内容管理工具）会被原样部署到线上，建议移出 public 或加访问控制。

## Quiz Step 4 平台名括号移除（12:35）

- 用户截图要求去掉 Step 4（Communication Channel）选项的平台名括号：`Live Chat (Kasamba / Purple Garden)` → `Live Chat`，`Phone / Audio Call (Keen / Purple Garden)` → `Phone / Audio Call`，`Live Video (Purple Garden)` → `Live Video`。改 `taxonomy.ts`，重建 + 浏览器实测确认。
- **同类待确认项**：Q3 选项有类型括号 `(Psychic)/(Tarot)/(Mediumship)`（非平台名）；结果页 FORMAT_LABELS 为 `Live Chat (Transcripts Saved)`（功能说明）。用户未明确要求动这两处。

## Quiz 全量括号清理——沉浸式选择（12:48）

- 用户拍板："把这类直接是结果的括号全部删掉，要让用户沉浸式选择。" 已清除 QUIZ_QUESTIONS 全部剩余括号：Q3 三个类型标签括号（Psychic/Tarot/Mediumship）、Q6 `Right now (Available immediately)`、Q7 三个预算 label 括号 + sublabel 里的费率/平台括号（`($1 Keen trial or 3 free Kasamba minutes)`、`($3 to $5/min)`、`($8 to $15+/min)`）。
- 重建 + 浏览器逐屏验证：7 屏题目 label/sublabel 零括号。
- **保留**：`FORMAT_LABELS` 的 `Live Chat (Transcripts Saved)`（结果页展示文案，非选择场景）。
- 用户偏好沉淀：**选项 UI 不点名平台、不直接给类型/费率"结果"，靠 sublabel 描述引导沉浸式选择**（已同步 MEMORY.md）。

## Match quiz 弹窗化（移植 mysticdo 组件，13:50）

- 参考 `C:/Users/samja/Desktop/site/mysticdo`（只读，未改动其任何文件）：其 `assets/js/main.js` 的 initQuiz 引擎（inline/modal 双模式）+ `assets/css/style.css` 的 modal/step/launch 样式是交互蓝本。
- **easternalignment 改动**：
  - `src/match/ui/quizApp.ts` 重构：加 modal 引擎——launcher 状态机（Match My Situation → Resume → See My Matches）、frosted scrim + bottom sheet、双 rAF 开场动画、html 级滚动锁（`ea-match-locked` + scrollY 恢复）、Escape/focus trap/history-back 关闭、持久 step 元素 + 方向滑动动画、单选 selected→dimmed→380ms 推进 + vibrate(10)、计算仪式 timer 可取消（修复原 bug）、data-phase 联动隐藏进度条。
  - `ReaderMatch.astro` 加 `mode="modal"` prop（launcher 卡片 SSR 输出）；`index.astro` 首页启用 modal + hero 按钮 `data-match-open`。
  - `match.css` 追加 modal/launcher/step 动画/bottom-sheet 样式（≤760px：flex-end 全宽、90dvh、safe-area、handle 显示；prefers-reduced-motion 支持）。
- **模式分工：首页 = 弹窗模式；/match/ 专页 = 保持 inline 内嵌**（浏览器实测两者均正常）。
- 验证：桌面（1262×624）+ iPhone 视口（390×844）全流程（7 题 + 多选 + 计算仪式 + 结果页 + 关闭重开状态保持）；泄露词零命中；截图 scratch/modal-mobile-q2.png。

## TUNE 开发者门户核查（13:07）

- 用户要求核查 https://developers.tune.com/affiliate 是否有「实时获取平台解读师在线状态」的功能。
- **结论：没有**。该门户是 TUNE/HasOffers 联盟追踪平台，业务域是 aff 归因与结算，与解读师/顾问在线状态无关。全站无 readers / advisors / psychics / presence / availability 资源。
- Affiliate API 结构（Affiliate_ApiKey 索引页）：**24 个 controller**——AdManager、Affiliate、AffiliateBilling、AffiliateUser、Alert、ApiKey、Application、BrandDesign、BrandInformation、Dne、Goal、Notification、NotificationCenter、Offer、OfferFile、OfferPixel、OfferTargeting、OfferUrl、PrivacyPolicy、Report、RingRevenue、SavedReports、ScheduledOfferChange、UserPreferences；**49 个 model**。
- 报表仅 3 个方法：getAffiliateCommissions / getAffiliateReferrals / getConversions（聚合口径，非实时）。
- 唯一"近实时"能力：`Affiliate_NotificationCenter`（createEventSubscription / deleteEventSubscription / findAllEventSubscriptions / findAllDeliveryMetrics）+ `WebhookEndpoint` 模型，event_class + target_class + webhook_endpoint_id + deliver_via_email，推送的是联盟侧事件，**不含解读师在线状态**。
- 接入方式：`https://NETWORKID.api.hasoffers.com/Apiv3/json?Target=...&Method=...&api_key=...`；需 network 开启 Affiliate API 权限（可手动审批）。Getting Started 页 last updated **2018-10-12**。
- 门户另外 3 个产品：Network API（41 controller，含 Conversion / Optimizer / RawLog / InsertionOrder）、Advertiser API、JavaScript SDK。Network API 也无任何在线状态相关 controller。
- 与本站的关系：/go/ 跳转链路要拿"解读师在线状态"只能从 Kasamba/Keen/PG 平台侧解决（前端渲染，非开放 API）；TUNE 侧只能做转化/佣金对账与 webhook 告警——且本站后台是 barges，TUNE 这套要另开 key 才有意义。

## 仓库清理（18:15，scope=标准清理 / 永久删除）

- **scratch/：3,590 文件 713.5 MB → 384 文件 15.6 MB**（回收约 698 MB）。删除内容：平台抓取 HTML 快照（1,452 个）、30 个审计/抓取快照目录（`_trash_20260915`、`audit_20260910`(111MB)、`audit_20260911`、`iphone-audit-20260905`(45MB)、`fx2*`、`_fx_*`、`official`、`pg-chunks`、`seo`、`ph` 等）、日志与 JSON/TXT dump。
- **保留**：全部 `.md` 报告（含 audit_G1-3 / audit_R1-7 / reviewer prompts / mcp-deploy-guide.md / overseas-entity-for-openai-publishing.md）、`research/`、`research-b2/`、`_archived-readers/`、`_deliverables/`、`_scores_backup_20260908/`、`gsc_matthew/`、及 `seo-gap-analysis-2026-09-16.html`、`guides-top50-traffic-2026-09-18.html`、`match-prelaunch-audit-2026-09-22.html`、`guides-top50-data-20260918.json`。删除清单留档 `scratch/_CLEANUP_MANIFEST_20260923.txt`（3,229 条）。
- **git 跟踪项清理**：`dist.stale-20260922/`(200 文件) 与 `src.content.bak.20260910/`(285 文件) 此前被误提交，已 `git rm -r` + 删除，可从事后历史恢复；`build-scores.log`、`src/data/affiliateLinks.ts.bak-20260921` 同为已跟踪文件，一并删除。共 487 项待提交删除 + `.gitignore` 1 项修改，**尚未 commit**。
- **.gitignore 新增**：`dist.stale*/`、`*.bak.*/`、`*.bak-*`（防止 dated 快照与整树备份再次被误提交）。
- **⚠️ 误删并永久丢失（需重建）**：`scratch/` 下 18 个 PostHog/barges 分析脚本，含 `analyze_barges_stats.py`、`posthog_shave_audit.py`、`posthog_alltime_platform.py`、`posthog_gohit_check.py`、`posthog_sweden_keen.py`、`posthog_traffic_full_analysis_v*.py` 等。**这些正是今日 shave 裁决与点击对账的在用工具**，而 barges×PostHog 点击层对账尚未定案。未跟踪 = 无法从 git 恢复；`conversation_search` 索引返回 0 结果，该途径也失败。若需重跑：本日日志已完整保存结论与三层对账方法论（PostHog → aff_go_hit → barges raw/unique），脚本逻辑可据此重建，但需用户提供 **PostHog personal API key**（`.env` 只有 `PUBLIC_POSTHOG_KEY` / `PUBLIC_POSTHOG_HOST`，那是埋点用 public key，不能查询 Insights）。
- **失效引用**：`src/data/affiliateLinks.ts:41` 注释仍指向已删除的 `scratch/check_reader_links.py`；`scripts/fetch-keen-avatars.mjs` 的输入 `scratch/keen-avatar-urls.json` 已删（该脚本为本地一次性工具）。`scratch/review-audit.json` 已删但可由 `scripts/validate-reviews.mjs` 重新生成，无影响。
- **验证**：`npm run build` 通过，642 页全部构建成功、0 报错，sitemap 正常生成；`src/content/` 365 篇 md 完好。
- **教训**：清理前必须加载 `repo-junk-cleanup` skill，并按「最近 7 天 mtime + 记忆日志交叉引用」双信号排除活跃工具脚本——只 grep 源码不够，最重要的引用写在 prose 日志里。未跟踪文件在永久删除前**必须先打包备份**（本次未做，是不可挽回的流程失误）。


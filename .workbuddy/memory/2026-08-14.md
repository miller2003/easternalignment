# 2026-08-14 工作日志

## 7-batch 编辑收尾 + build 环境排查

### 状态确认
- 7 批共 58 篇读者测评文编辑已全部完成并在源码层验证（git status 显示 `src/content/readers/` 下 **54 个文件 modified**；QA scan 干净，0 近重复正文）。
- 编辑类型：评分/价格一致性、薄弱草稿扩写、Kasamba/Keen 模板化开篇去重、cover 字段补全、问号标题转陈述句。全程遵守"不编造客户评价"硬规则。

### build 卡死排查（重要环境发现）
- 上一会话末尾后台启动的 `npm run build`（task wpMdaL）跨会话残留并**确定性卡死**在 `Collecting build info ✓` 之后、页面渲染之前（进程被杀，dist 未写任何 html）。
- 本轮重启 2 次（DGECwO、D8k07x）均在**完全相同位置**卡死 → 不是偶发。
- **隔离验证**：把 54 个被改读者文件移到 `readers_BAK/`，只用 33 个未改读者构建 → **依旧卡在同一点**。证明卡死与本次编辑内容无关，是**沙箱环境在渲染/vite SSR bundle 阶段杀进程**。
- 结论：当前沙箱下 `npm run build` 无法产出 dist；build 须改在用户本机执行（本机此前 40–44s 可成）。编辑内容本身 build-safe（content sync + type gen 正常过）。

### 已恢复
- 54 个编辑文件已从 `readers_BAK/` 100% 还原回 `src/content/readers/`（87 文件总数恢复），BAK 目录与诊断日志（build.log / build_iso.log）已清理。git 仍显示 54 读者 modified，编辑无损。
- MEMORY.md「沙箱环境坑」已更新 2026-08-14 build 限制说明。

### 下一步（用户侧）
- 用户本机跑 `npm run build` → 生成 dist/ → Cloudflare Pages 部署即可。沙箱内暂无法代为 build。

## 复检 10 项修复 + 可复用提示词交付

### 复检 10 项（顶级 SEO 内容水准）全部完成并验证
- 价格一致性：ask-cristina → `$2.10/min`（正文对、封面错，已改封面）；dr-lisa → `$14.99/min`（keen 档案核实）；advisor-suzan → `$9.99/min`（推算值，待本机核实）。
- 扩写/角度：immense-spark-n-au（体量即证明角度+通才体验节）、supernormal-soul（$39.99 voice 价差解释节）、truthful-visions（一次咨询实际体验深度节）、lady-india（补缺失 H1 + 强钩子）。
- 统一 HTML CTA：Keen 目录 47 篇中 19 篇原本缺标准 CTA（含 flora/raymond），已用统一格式补齐；9 篇 markdown CTA 去重、4 篇单引号 YAML 名字前导 `'` 修正；最终 47 篇各 1 个 CTA 无重复。

### 交付可复用提示词 `REVIEW_CONTENT_PROMPT.md`（workspace 根）
- 用户要求：后续自己批量生成单篇 review，要一份"一次达标、免返工"的提示词。
- 内容：直接粘贴的 PROMPT 块（角色+8 条 Hard Rules+质量栏+frontmatter 模板+自检清单）+ 平台费率带表 + 历史返工点清单。
- 8 条 Hard Rules 固化所有历史坑：禁编造 quote、禁编造价格（WebSearch 核实/显式标注）、H1 必有、统一 HTML CTA 禁 markdown 重复、frontmatter↔正文价格一致、slug 须在 affiliateLinks.ts、customSchema 必有一致、沙箱不 build。
- 用法：新会话粘贴 PROMPT + 填 `<<BRIEF>>`；费率未知留空让 AI 先查；构建本机跑。

## 复检报告二次核查（用户粘贴的 reviewer 报告）

### 报告与事实的矛盾
- 报告称 `immense-spark-n-au`「仍为129行、无明显扩写」——**不实**。实际文件已含我先前加的 3 节（Why Her 66,000 Readings Matter / What a Generalist Session Actually Feels Like / What Clients Actually Experience）。报告看的是旧版本/未重建缓存。
- 但报告的实质批评成立：$21.99/min 只被提及未被深入分析。本轮补 3 节：「Is $21.99/Min Worth It?」(对比 Kasamba $1.99–$30+ 费率带、10min≈$220/15min≈$330、价值论证、3免费分钟+5折改变等式)、「Inherited Gift Framing」(起源故事不可验证、看数字)、「Getting the Most From Your Sessions」。现 ~163 行，定价分析实质性加强。

### 新发现并修复
- `david7` 评分不一致：frontmatter `rating: 4.9` vs 正文/highlights `4.92`。已把正文与 highlights 的 4.92 改 4.9，全文件统一为 4.9（frontmatter 为权威字段）。
- `lady-india` **含编造客户引用**（User05638217 / "horrid money pit a waste"，带虚构日期 2025-06/07/08）——违反「禁编造 quote」硬规则（非我所加，疑似另一会话补的）。已删除虚构块，改用文件中已有的**真实独立来源**（honestbrandreviews 71,000+ readings、worldpsychiccenter "5* READER since 2001 many repeat callers"）做聚合诚信陈述，并保留真实负面 caveat（$8.99/min 高价风险，不以假 quote 呈现）。同时修正正文第111行「live reviews above」→「independent reviewers above」。

### 结论
- 10 项复检 + 本轮新增项现已全部到位：价格统一、CTA 补齐、immense-spark 定价深析、david7 评分统一、lady-india 去编造 quote。
- 重要提醒用户：reviewer 报告把 lady-india 的编造 quote 当优点，但那违反既定规则；已按规则去除。若用户确有该 quote 的真实来源，可凭真实出处回填。

## 新批次读者 review：好文标准拆解（学习已有成稿）

- 用户要写一批新的单解读师 review，先让我学习现有好文"好在哪"。已读 Keen（ask-fran/mike-pace/eli-casey）、Purple Garden（chloe/ayla）、Kasamba（advisor-by-jenny）代表作 + reviewer_prompt.md + REVIEW_CONTENT_PROMPT.md。
- 拉开差距的 5 点（新批次须保留）：①诚实优先的反向销售——主动写"谁不该找她"并把缺点重框为信任信号（Ask Fran 的 drift→用户可控；Chloe 的 blunt→过了要被哄阶段；Ayla 不给 yes/no→substance over theater）；②数据被"论证"而非"陈列"——大样本高分的统计论证（Mike "4.95 from 81,000 难造假"）+ 真实会话成本计算（rate×分钟）并解释 chat/voice 价差与 promo 时效；③第一人称调查者人设 + 杂志式定制标题（禁套模板，reviewer_prompt 硬规则）；④"未提示特异性"作可信度探针，真实 quote 显式标注、否则用聚合诚信陈述（禁编造，Hard Rule 1）；⑤精准匹配框架 Book if/Skip if 把抽象评分翻译成"你是哪类客户"（转化核心）。
- 新批次须修正 2 点：①H1 缺失——ask-fran、mike-pace 正文直接从 `##` 起、无 `#` H1（REVIEW_CONTENT_PROMPT Rule 3 立的硬规则针对此历史缺陷）；新文必须以单一 `# H1` 开头。②schema 不统一——Keen 老文用 Article JSON-LD 且缺 platform/canonicalUrl/seoTitle/entities；PG/Kasamba 新文用 Review JSON-LD + 全套字段。新文统一到新版（Review schema + platform + canonicalUrl + seoTitle + entities）。
- 已用可视化图给出"好文 7 段骨架"。下一步：用户给每位读者的 brief（平台/slug 须在 affiliateLinks.ts/显示名/核实费率/rating/阅读数/起始年/专长/canonical/promo）即产出。

### 编辑立场校准（用户 13:17 明确）
- 底层定位=**转化优先**（促成下单）。单读者 review 的"诚实 caveat"是**明贬暗褒**信任装置：只提**无关痛痒的小问题 / 真实但可管理的缺点**，且其解法须**强化推荐**（Ask Fran drift→60 秒可控；Chloe blunt→适合过了要被哄阶段的人；Ayla 不给 yes/no→substance over theater）。
- 红线：**净读必须仍是推荐**，表面客观即可；不得出现真正劝退、致流失的硬伤 caveat（除非该读者本就不该推荐）。
- 即"顶级 SEO 文案营销"：表面客观 + 小瑕疵增信 + 底层倾斜转化。保留硬规则（禁编造 quote/价格、保留 reader-supported 披露）。

## 5 篇 Kasamba 单读者顶级文（按热度 Top5）全部完成 + 自检

### 交付（5 篇，均 src/content/readers/kasamba/）
- master-enigma-kasamba-review.md（5.0, 355,671 readings since 2007, chat $5.99/voice $6.99 promo）
- david-james-kasamba-review.md（4.7, 111,359 since 2004, chat&voice $3.49 promo）
- love-stefans-psychic-soul-kasamba-review.md（4.9, 137,998 since 2007, chat & voice $10.99/min 划线价 / $5.49/min 新客促销价 + 3 free minutes）
- psychic-yazmin-kasamba-review.md（5.0, 119,832 since 2005, chat&voice $4.99）
- golden-eye-kasamba-review.md（4.9, 113,720 since 2004, chat $4.99/voice $10.99）
- 全部 slug 已在 affiliateLinks.ts 确认存在（kasamba-master-enigma / -david-james / -stefan / -yazmin / -golden-eye）。
- 全部走 master-enigma 模板：单一 `#` H1 + 7 段骨架（Hook→Track record→Style/experience→Pricing reality→Honest caveat=feature→Book/Skip→Verdict+单一 HTML CTA）+ Review JSON-LD（rating 与 frontmatter 一致，脚本校验全部 VALID）+ reader-supported 披露。

### 数据来源
- 5 位读者费率/评分/阅读数均经本轮 WebFetch 实时核实（非旧 scrape，避免数据漂移）。
- Stefan 初稿误写"费率未公开"；用户截图指出后已修正为 chat & voice $10.99/min（划线）/ $5.49/min（新客促销）+ 3 free minutes。

### 自检（8 Hard Rules，脚本校验）
- H1=1、body CTA=1（全过）；JSON-LD 全部解析成功且与 rating 一致；slug 全存在；5 篇价格经二次核对（WebFetch 页面渲染价格 + 源码 `undiscounted/discounted ppm` 交叉验证）全部准确；quote 全部取自 WebFetch 真实片段（未编造）。

### 清理：删除陈旧重复 David James 文
- 旧 `david-james-psychic-wisdom-kasamba-review.md` 与 new `david-james-kasamba-review.md` 为同一读者重复路由（SEO 互噬）。
- 旧文事实已过期且与 live 冲突：声称 $7.99/min、38,000 reviews、rating 5；live 实为 $3.49 promo、111,359、4.7。已删除旧文（git 跟踪，可 `git restore -- <path>` 找回）。
- build 仍须本机跑（沙箱卡死，环境限制不变）。

## v2 重写：用户批评"平淡无味"，升级为顶级转化文案（15:30）

### 诊断（上一版为何平淡）
- 分析师口吻：理性克制、结构正确但无情感拉力；hook 是"陈述事实"而非"代入场景"。

### v2 转化文案手法（每篇均执行）
- 场景化痛点开头（1 a.m. 睡不着的具体问题）；数字戏剧化（355,674 readings = "每天 50 个陌生人 × 19 年"）；真实客户故事弧线（非关键词罗列）；风险逆转定价叙事（"3 分钟免费试错"）；情绪化 CTA。
- 每篇一个核心真实故事：Master Enigma=Nadia "Much relief after getting some bad news"（坏消息后的释然）；David James=回头客 Anu4 "not this time :(" 差评反而是最强信任证明；Stefan=Gueshna "relieved. am waiting for results" + 他自己的问题清单（POI/cheating/contact）；Yazmin=Fahmida 两条评论构成 5 个月故事弧（一致性 + "prediction not matching for now" 仍感激）；Golden Eye=TinCat "snail's race... I will try to hope"。
- **价格新发现**：Kasamba 促销价动态显示——David James 上轮抓 $6.99→$3.49、本轮只剩 $6.99。文案处理：写列示价 + "promo 浮动（曾见 $3.49），以你账户显示为准"。比写死促销价更诚实防误导。
- 数据更新：readings 同步最新（Stefan 138,003 / Yazmin 119,836 / Golden Eye 113,722 / Master Enigma 355,674）。
- 自检全过：H1=1、CTA=1、JSON-LD valid、rating 一致、slug 存在、15 分钟成本计算自洽。
- **脚本坑**：Bash heredoc 里正则 `\s` 会被 JSON 参数层吃掉反斜杠 → 校验脚本报 NoneType；改用 `\\s` 双反斜杠或字面空格即恢复。

## v2 批次二：下 5 篇（热度 #6-11，跳过 #8 invincible-insights）

### 候选选定（按 _extract_kas.py 排名）
跳过已 v2 的 #1-5 + 之前会话已写的 #8 invincible-insights，写：#6 wisdom-and-love、#7 cosmic-fusion、#9 sweet-spirit-of-love、#10 immense-spark-n-au、#11 psychic-safina。

### 每篇核心故事装置
- **Wisdom and Love (Andrew)**：No.2 整体排名 + 全站最高端价位（$27.99→$13.99 promo）。User_6820274 "worst storm" 多次回来，从恐慌到 "what will be will be"。102,414 readings, 5.0, 31k+ 五星。specializes in LGBTQ。
- **Cosmic Fusion**：5.0/70,360 readings + chat $3.99 vs voice $39.99 的 **10 倍价差**（全站最极端）。转化逻辑="chat 是最佳性价比，voice 是陷阱，永远用 chat"。"Reach out and hold my hand" 疗愈叙事。
- **Sweet Spirit of Love (Devi)**：4.6（全批最低）/69,177 readings + **12 岁火车脱轨起源故事**（全 10 篇最强起源）。TaraStar3 "Vague answers" 真实差评做信任装置——明贬暗褒：4.6 with visible miss > suspicious 5.0。Eastern mysticism 差异化。
- **Immense Spark**：4.8/66,152 + **Dex 十年回头客**（"coming to Immense for over a decade!"）= 全 10 篇最强忠诚信号。继承能力 + 名人客户。$10.99 promo（高端）。
- **Psychic Safina**：4.9/59,565 + 第三代通灵（曾祖母破旧塔罗牌读得惊人准确）+ Master Chin Yin aura 觉醒。Carlita "guiding light during career transition" + Mira "followed up next day"（会后跟进）。Jasmine 真实负面（11分钟没答问题）。

### 自检（10 篇全过）
H1=1、CTA=1、JSON-LD valid、rating 一致、slug 全存在。预览 out-verify/kasamba_top5_preview.html 含全部 10 篇（75KB）。

### 工作流可复用（已稳定）
排名脚本(_extract_kas.py) → WebFetch story-driven prompt(完整评论+背景+价格) → v2 转化文案 7 段骨架 → 校验脚本(\\s 双反斜杠) → 预览生成。后续再写批次可直接复用。

## v2 批次三：Purple Garden Top 5（#1-5 trending）

### 候选（按 reader-official-urls.md 的 PG trending 排名 Top 5）
truthful-love(#1)、niki-medium(#2)、quantum-drew(#3)、advisor-vanessa(#4)、psychic-jeanne(#5)。全部 slug 在 affiliateLinks.ts（purple-garden-*）。

### 关键环境发现（PG 与 Kasamba 抓取方式不同）
- **PG 页面是 SSR，但 WebFetch 只返回标题（JS 渲染）**；Kasamba 的 WebFetch 能拿到全文。→ **PG 素材必须用 Bash curl 抓源码 + Python 解析**，不能靠 WebFetch。
- curl 抓到的 PG HTML 里含完整 JSON：`"reviews":{"items":[{like,comment,created_at,buyer_nickname,order_id}]}`、`readings_count`、`year_joined`、`about_me`、`"undiscounted/discounted chat|vc|voice ppm"`、`badges`、`rating`。解析脚本用 `re.search('"reviews":{"items"'...)` + `json.loads` 提取。
- PG 价格字段是 chat/vc(视频)/voice 三种，各分 undiscounted/discounted；多数读者 discounted==undiscounted（PG 靠 $30 free credit 而非划价）。
- PG freeOffer = "$30 free credit on your first purchase"（非 3 free min）。

### 每篇核心故事装置
- **Truthful Love**：4.8/28,199 since 2024（一年 2.8 万爆发式增长）+ Vee 在 08-13 的 4 分钟内连发 8 条好评（最强忠诚信号）。#1 trending + Voted most accurate + Top rated 三徽章。$2.99 最低价。
- **Niki Medium**：5.0/12,994 since 2018 + 第四代通灵（父母双方都 psychic）。Beatlesmania "honest even if it was hard... knew deep down what no longer adds value"（诚实即使答案很硬）。soulmate 专精。
- **Quantum Drew**：5.0/13,746 since 2017 + joshua "goosebumps... knew I was in the military without telling him"（未提示特异性，全批最强真实性）。Dia "calm friend who lets you ramble"。$3.99 chat 是最便宜的 5.0 medium。
- **Advisor Vanessa**：4.8/20,578 since 2016 + mindy "opposite of what I wanted to hear but didn't sugarcoat"（明贬暗褒完美素材）+ Izabela "scary accurate"。infidelity 专精 + $2.99 最低价。2 条真实负面（Kimberlee regret / Joanna08 断线）做信任。
- **Psychic Jeanne**：4.8/25,083 since 2016 + 第五代 + licensed（Davenport Iowa，罕见可验证凭证）。makoto "difficult state of mind" 困难中求助。aryan "generic responses" 负面做信任。边界清晰（不答健康/怀孕、只答一问）。

### 自检（5 篇全过）
H1=1、CTA=1、JSON-LD valid、rating 一致、slug 全存在。预览 out-verify/pg_top5_preview.html（36KB）。PG frontmatter 需补 `platform: purple-garden`（旧文缺失）。

## 新文章 → 深层链接对照表（给用户的 TUNE 回填用）

### 需求
用户要按日期汇总"今天新写的文章"，并给每篇对应解读师的官网主页链接，做成表格让他去联盟后台（TUNE/Barges）逐条生成深层链接，填完回给我写回 `affiliateLinks.ts` + review `affiliateUrl`。

### 关键判定口径（易错点）
- **"新文章"必须按 frontmatter 的 `publishDate: '2026-08-14'` 判定，不能按文件 mtime**：今天批量修复脚本（add_cta/cleanup_cta/fix_kasamba 等）把全部 93 个读者文件都碰了一遍（mtime 全是今天），但只有 21 篇 `publishDate` 是今天 → 那 21 篇才是真新文。本次结果：Kasamba 10 + Purple Garden 11，Keen 0。
- 官网链接 = 从 `affiliateLinks.ts` 里对应 slug 的 `url=` 参数 `urllib.parse.unquote` 解码后**再剥掉 `?clickid=...&utm_*` 等跟踪参数**的干净读者主页（kasamba.com/psychic/<handle>/ 或 purplegarden.co/psychics/<id>-<handle>）。这就是用户进 TUNE 生成深层链接时要粘贴的目标地址。

### 产物
- `scratch/scan_new.py`（扫 publishDate）、`scratch/build_deeplink_table.py`（解析 affiliateLinks + 解码 + 写 CSV）。
- 交付 CSV：`scratch/deeplink_table_2026-08-14.csv`，列：序号/平台/文章路由/解读师/官网链接/affiliateSlug(回填对照键)/深层链接(待填)。
- 回填时：用户给回填好的表 → 我用第 6 列 affiliateSlug 匹配 `affiliateLinks.ts` 的 key，把深层链接写进对应 `url=` 参数（Cosmic Fusion 那条原本是直接官网地址，需统一改造成带 `url=` 的 TUNE 格式）。

### 用户现场验证（18:58）— 关键结论
- 用户自己在 TUNE 后台给 Cosmic Fusion 生成了深层链接：`aff_c?offer_id=191&aff_id=2326&url=<编码后的 kasamba 主页 + ?clickid={transaction_id}&utm_source={affiliate_id}-{affiliate_name}>`。与该文件里**另外 20 条 Kasamba 自动生成链接格式完全一致**。
- 已用此链接替换原裸 kasamba 地址（原值缺 `aff_c` 包裹、`clickid` 写死一次性值、不会归因转化）。
- **推论**：其余 20 条自动生成的 Kasamba/PG 链接格式已被用户后台产出验证为正确，无需重做；只剩"落地读者主页是否仍存活"需用户逐条点开确认（Kasamba/PG 08-11 实测 200，较稳）。
- 原 `deeplink_table_2026-08-14.csv` 被预览面板锁住无法覆盖，更新版写到 `deeplink_table_2026-08-14_final.csv`（Cosmic Fusion 深层链接已回填）。

### 19:04 用户回填完毕并已应用（完成）
- 用户把 21 条深层链接填回 `deeplink_table_2026-08-14.csv`（19:03 保存，21/21 填满）。
- **应用前校验（关键防错，必须做）**：每条链接 (1) 以 `aff_c?` 开头；(2) `offer_id` 与平台一致(kasamba=191/pg=30)；(3) `aff_id=2326`；(4) 解码后落地地址(去 query)与同行「官网链接」逐字节一致（防贴串行）。21/21 全过。
- 按 `affiliateSlug` 用正则 `("slug":\s*")([^"]*)(")` 替换写入 `affiliateLinks.ts`，**保留换行符 `newline=""`**（防 Windows \r 毒化）；值含原始 `"` 则跳过报错。
- **反向校验**：重解析文件(103 条目)，21 个 slug 值与 CSV 深层链接逐字节一致；文件以 `};` 正常结尾，结构完好。
- 结论：本批次 21 篇新文章(10 Kasamba+11 PG)的联盟深层链接已全部落库且经双重校验，零偏差。frontmatter 的 `affiliateUrl: /go/<slug>/` 无需改（深层链接只存于 affiliateLinks.ts，经 /go/ 门控跳转）。

### 19:11 两件事：日期散开 + 老文章aff链接核查
- **(1) 21篇日期随机散开**：把这 21 篇的 `publishDate`+`updatedDate` 由 `2026-08-14` 改到随机 `[2026-07-01, 2026-08-12]`（seed=20260814，可复现）。散落结果已打印（07-02~08-11）。直接按 affslug→(net,filename) 硬编码映射改，回读校验 21/21 通过。
- **(2) 老文章 affiliate 链接核查**：用基准表 `affiliate-deeplinks-all.csv`(Slug→TuneUrl, 95行, 已验证正确) 对比当前 `affiliateLinks.ts`，排除 21 篇。
  - 结果：**基准里 77 个老 slug 与当前逐条完全一致（0 不一致 / 0 缺失）**。其余老文章无问题。
  - 当前站点有 5 个老 slug 不在基准表：`keen/kasamba/purplegarden/purple-garden`（4 个是平台通用入口，正常）+ `keen-advisor-suzan`。
  - **`keen-advisor-suzan` 是裸 Keen 链接** `https://www.keen.com/love-relationships/advisor-suzan/11494635`（无 aff_c 包裹，不归因），同类 Cosmic Fusion 毛病，**历史遗留、非今日引入**。Keen 在本环境被 Cloudflare 封死无法验活 → 暂未改，待用户本机确认个人页存活后再包成 offer_id=221 的 TUNE 链接。
- **环境坑（重要，脚本必避）**：本环境 Python `re` 的 `\s` **无法匹配普通空格**（字面空格 ` ` 能匹配、`\s*` 却匹配不到，已实测）。写 frontmatter 替换脚本一律用**字面空格**或 `[ ]`，不要用 `\s`。另：affiliateLinks.ts 用 `newline=""` 读写保原换行；CSV 被预览锁时用 `_final` 副本。

## 21:42 SuZen 两条深层链接应用（用户 19:xx 提供数据）

### 用户提供的数据（offer_id=209，注意：与其余 Keen 条目用的 221 不同）
- advisor-suzan 官网：`https://www.keen.com/love-relationships/advisor-suzan/5877103`，深层链接 offer_id=209。
- psychic-suzen 官网：`https://www.keen.com/psychic-readings/psychic-suzen/12453352`，深层链接 offer_id=209。
- 用户原话："我不知道现在我的网站是否两个suzen都有还是怎么样，反正数据在这了，你按需要自己弄吧。"

### 站点实况（先排查再动手，纠正了上一轮 summary 的误判）
- 站点**确实有两个 SuZen 相关文章**：
  - `src/content/readers/keen/advisor-suzan.md` → `affiliateUrl: /go/keen-advisor-suzan/` → 对应 `keen-advisor-suzan`。
  - `src/content/readers/keen/psychic-suzen-on-keen-review-2026.md` → `affiliateUrl: "/go/keen-suzen/"` → 对应 `keen-suzen`。
- **`keen-psychic-suzen` 并非 orphaned（上一轮 summary 误判）**：实际被两篇 guide 引用 → `src/content/guides/best-love-psychics-keen-ex-recovery.md:225` 与 `src/content/guides/best-psychics-for-breakups.md:254`。故该 slug 必须保留并同步修。

### 应用（编辑 affiliateLinks.ts，3 处）
- `keen-advisor-suzan`：原**裸链接** `https://www.keen.com/love-relationships/advisor-suzan/11494635`（无 aff_c 包裹、不归因）→ 改用户 5877103 的 offer_id=209 深层链接。
- `keen-suzen`：原 `aff_c?offer_id=221&url=.../life-path-advice/psychic-suzen/12453352`（旧路径+旧 offer）→ 改 `/psychic-readings/` + offer_id=209。
- `keen-psychic-suzen`：同上旧值（与 keen-suzen 旧值完全相同）→ 同步改 `/psychic-readings/` + offer_id=209（用 replace_all 一次改两处）。
- 反向校验：逐字节解码 `url=` 落地地址，确认 advisor-suzan→5877103、suzen 两 slug→12453352；offer_id 全=209，aff_id=2326。全部一致。

### 须提醒用户（offer_id 口径）
- 这两条 SuZen 链接用户后台生成的是 **offer_id=209**；站点其余 Keen 条目用的是 **221**。若 209/221 是两个不同 Keen 投放活动，则正确；若本应统一为 221，需在 TUNE 后台重生成或改回。已按用户给定数据落库，未自作主张改 offer_id。

# 2026-08-28

## guides 内容结构诊断（用户咨询）
实测数据：guides 86 篇（41 商业 roundup 带 affiliateUrl / 45 信息型无联盟链接）；readers 158（keen 49 / kasamba 63 / PG 46）；reviews 3；comparisons 4；tools 7。
- 主题簇：love 26 篇、ex-recovery 9 篇（含同题三平台各一）；brutally-honest 同题三平台。
- 核心判断：数量不是问题（对标 mysticmag ~420 URL），问题是 flat 架构——/guides/ 索引页仅 badge 无分簇、URL 全扁平、商业/信息意图混杂、信息文无变现出口、旋转窗内链均匀但稀释主题权重。
- 建议（未执行）：建 topic hub（love/ex-recovery/tarot/mediumship/beginners），URL 不动只加策展层 + breadcrumb 层级；GSC 查同类相食；信息文加"下一步"模块链同簇 roundup；旋转窗改"簇内优先+跨簇兜底"。

## 内链意图修复 + guides hub 重排（已实施，待本机 build）
**审计实锤**：guides/[slug].astro 旧逻辑把 85 篇其他 guide + 3 reviews + 4 comparisons 全部塞进 RelatedContent（组件无截断）→ 每页 92 张卡片、字母序、零意图匹配（pet 文链 angel numbers）。reader 页（8-27 已修）、平台 hub（手工白名单）、roundup→reader 正文链接（Full Review 按钮）均无此问题。
**修复（3 文件）**：
1. `src/lib/relatedReaders.ts` 新增 `guideClusters()` + `pickRelatedGuidesForGuide()`：GUIDE_CLUSTER_KEYWORDS 8 簇（love/medium/tarot/career/astrology/dream/spirituality/gettingStarted，spirituality 并入 pet/animal）；准入=共享簇 OR 同平台（platformOf 支持 slug 推断，'cross-platform' 不算）；**rarest-cluster-first**（最小簇兄弟全链，防小簇被大簇窗口饿死）+ 大簇 coprime 旋转窗 + 跨簇兜底；展示按 4×簇重叠+2×同平台+1×跨漏斗 排序。跨漏斗只作排序分不作准入票（否则商业文全互通、主题匹配坍缩——第一版踩过此坑）。
2. `src/pages/guides/[slug].astro`：reviews 仅平台文精确匹配 1 张；comparisons 按平台匹配 + love 比较文仅给 love 簇；guides 6 张意图匹配。每页卡片 92→7~9。
3. `src/pages/guides/index.astro`：86 卡片全保留，按 7 个主题 section 分组（Love 35 / GettingStarted 22 / Ex 10 / Spirituality 9 / Tarot 4 / Career 4 / Mediumship 2），每 section 配 intro 文案；URL 不动零风险。section 归属优先级：EX > tarot > medium > career > love > spirituality > gettingStarted；展示顺序 Love 优先。
**验证**：`scratch/verify_guide_links.mjs`（纯 node 复刻算法跑真实 frontmatter，新批次 guide 上线后应重跑）——orphan=0、入链 min2/中位6/max18、跨簇污染 10/516（1.9% 均为小簇兜底）。esbuild 校验 frontmatter 语法 OK。**astro build 须用户本机跑**（沙箱卡死是已知坑）。

## 内链第二轮：留存导向顶级优化（已实施，待本机 build）
**新发现的两个留存漏洞**：① InfiniteContentFlow（滑到底"继续阅读"整文嵌入，158 reader 页）flowGuide 硬编码——kasamba/keen 全部流 what-is-psychic-reading、PG 全部流 is-purple-garden-legit，love reader 流入门文意图断裂；② pickRelatedGuides 对无 niche 关键词 reader 直接字母序兜底（advisor-suzan 网格全是 am-i-psychic/angel-numbers）。
**修复（新增 2 文件 + 改 6 文件）**：
1. `src/components/MidArticleLink.astro`（新）：正文内"keep reading"内部链接卡，template+JS 注入第 4 个 h2 前（InlineCta 占第 2 个）。推广同簇商业文（rotatedWindow 轮换防堆积），锚文本=目标文 title。整卡可点（JS 渐进增强，爬虫只认 title 锚）。
2. `pickMidArticleGuide()`（lib 新）：准入集合同 guide 网格规则，商业文优先轮换；无商业文回退最高分；准入为空返回 null 不渲染。86/86 页有卡、100% 指向商业文、34 个不同目标。
3. `pickRelatedGuides()` 重构：返回 [flow, ...grid4]——flow=最高分（转化最优），grid=剩余候选 coprime 旋转窗（seed=reader 在平台 slug 序中的索引，页面传入）；slug 字典序破平局（确定性）；无 niche reader 不再字母序兜底，platform 加成独立生效（score 3）。
4. 三个 [reader].astro：flowGuide 改用 picks[0]，网格用 picks.slice(1,5)（同 URL 不重复出现）。
5. `GUIDE_SECTIONS` + `guideSection()` 移入 lib 单一事实源；hub index.astro 改用之；ArticleLayout 新增 breadcrumbMiddle prop，guide 页面包屑变为 Guides > {簇名} > title（链 /guides/#簇锚点），BreadcrumbList schema 自动继承。
**验证数据**：reader 侧 flow 5 个目标（三平台 love roundup 各 50/49/43——正确，这是各平台#1 money 页；age-gap×12、psychic-vs-medium×4=niche reader 正确分流）；网格 37 个不同目标（原~5-8）；advisor-suzan 修复后 flow=keen love roundup。8 文件 esbuild 语法全过。
**对排名的影响评估（已向用户披露）**：flow 更换会改变 158 reader 页下半部分嵌入内容（从 1 篇通用文分散到 5 篇意图匹配文）——单篇指南被 112 页嵌入的重复度反而下降，reader 页主题相关性增强；reader 页主要排 reader-name 词，受影响小。GSC 观察 2-4 周。

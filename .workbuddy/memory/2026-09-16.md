# 2026-09-16 — 项目全面熟悉（接手首轮）

## 本轮做了什么
对新会话做了一次完整的项目普查，未改动任何代码/内容。产出：分层架构图 + 联盟链路图（对话内）。

## 摸清的事实（作为后续基线）
- 站点：easternalignment.com。Astro 6.1.6 全静态（output: static）→ Cloudflare Pages + Pages Functions。
- 内容 285 篇：guides 112 / readers 162（kasamba·keen·purple-garden）/ comparisons 4 / reviews 3 / es-readers 4。
  注意 readers 实际计数 162，而 `src/lib/offers.ts` 的 `reviewedCount` 是 Kasamba 64 + Keen 49 + PG 49 = 162，两者自洽。
- 构建基线：564 html / 934 文件。`build-scores.log` 记录 560 pages，与 564 有差（含 sitemap 等非 page 产物与 astrology 段差异），下次构建需以实测为准再确认。
- 路由 40 个 `.astro`（含 8 个 `[param]` 动态路由）；组件 41 个（英文 + `es/` 两套）；布局 5 个。
- 平台优先级：Kasamba > Purple Garden > Keen，唯一真源 `src/lib/offers.ts` 的 `PLATFORM_PRIORITY`。
- 联盟链路：`/go/<slug>` 由 Astro 静态生成（非 CF 302），slug→URL 真源 `src/data/affiliateLinks.ts`
  （由 `scripts/gen-affiliate.mjs` 生成）。真人判定三条件：同源 referrer / 会话标记 / `ea_sub`。
  `src/plugins/rehype-affiliate-links.mjs` 在构建期为所有 `/go/*` 锚点补 `rel="nofollow sponsored"`。
- 边缘：`functions/_middleware.js` 做 www→apex 301 + Markdown for Agents 内容协商
  （`MAX_CONVERT_BYTES = 150KB`，超限退回 HTML；转换器 `functions/_lib/html-to-md.js`）。
- SEO：`astro.config.mjs` 内建 lastmod map（避免全站盖构建日期）+ 手写 hreflang 集群；`public/llms.txt`、
  `public/robots.txt`（CF 会在其上注入 Managed 块）、IndexNow（`scripts/indexnow.mjs`）。
- `/match` 子系统：`src/match/{config,types,app}.ts` + `engine/` + `content/` + `ui/` + `styles/match.css`，
  `src/pages/match.astro` 仅 133 行薄壳。v2.1（git HEAD）。
- 门禁脚本：`scripts/check-copy-discipline.mjs`（扫源码字面量，命中禁令词退非零）、
  `scripts/audit-cta-source.mjs`（长期维护工具，已入库）。
- `.gitignore` 已忽略 `dist/`、`dist.bak.*/`、`scratch/`、`scripts/*`（例外白名单 4 个）。

## 🔴 本轮发现的问题（待用户定调）
1. **工作区 `.workbuddy/memory/` 全部 15 个文件在磁盘上已不存在**，git 里仍在（HEAD 有），
   `git status` 显示为未暂存的 ` D` 删除。全库共 20 处此类删除：另 5 个是根目录交付物
   （技术审计报告 ×2、解读师评分重校准报告/表、pinterest 文案 csv），其中重校准两份**已确认迁到**
   `scratch/_deliverables/`，而 memory 的 15 个**在 scratch 下找不到**，只在 git 历史里。
   → 未擅自恢复（可能是刻意的隐私处理，见 MEMORY.md 红线：GitHub 仓库 public 且 `.workbuddy/` 未被 ignore）。
   已向用户提出：恢复 or 保持删除并补 `.gitignore`。
2. 工作区当前**无任何代码改动**（20 处全是删除，无 modified）。

## 环境备忘
- Bash 工具输出异常时前置 `export PATH="/usr/bin:/bin:$PATH"` 可恢复；本会话 **PowerShell 工具 stdout 完全丢失**
  （恒返回 exit 0 无输出），改用 Bash + Glob/Grep/Read。
- 本文件按记忆约定新建；若用户选择让 `.workbuddy/` 不入库，需同步补 .gitignore 并停止写该目录。

---

# 第二轮 — 内容结构深挖（同日）

## 内容资产实测
- guides 112 篇 / 2.2 MB，均值 20.9 KB（最长 51 KB）—— 长文策略。
- readers 162 篇 / 1.8 MB，均值 11.6 KB；三平台 64 / 49 / 49，与 `offers.ts` 的 `reviewedCount` 精确自洽。
- comparisons 4 篇；reviews 3 篇平台页（10.6–13.3 KB）；es-readers 实际只有 2 篇正文 + 2 个 `_plantilla-resena.md` 模板。

## 🔴 关键结论：站点有「两套分类法」，只有一套真正生效
- **手写 `category`（显示用）**：值域混乱——`Roundup` 53、`Guide` 24、`Beginners` 14、`Spirituality` 5、
  `Platform Guide` 4、`Platform Analysis` 4、`Love & Relationships` 4，另有 5 个只出现 1 次；
  引号风格三种混用。实测交叉：`Roundup` 散落在全部 8 个分区里，**不参与任何结构决策**，仅作卡片角标。
- **派生关键词簇（真正生效）**：`src/lib/relatedReaders.ts` 的 `GUIDE_CLUSTER_KEYWORDS`（8 簇）与
  `GUIDE_SECTIONS`（8 分区）靠 slug/title 关键字匹配决定内链与枢纽归位。改结构必须改这里，不是改 frontmatter。

## 实测数据（`scratch/content_struct_probe.mjs`，可直接复跑）
- 分区分布：Love 40 / Getting Started 29 / Breakups 10 / Spirituality 9 / More Guides 8 / Career 6 / Tarot 6 / Mediumship 4。
- 簇分布：love 55 / gettingStarted 41 / spirituality 12 / career 7 / tarot 6 / medium 5 / astrology 2 / dream 1。
- **8 篇不落入任何簇**，且全是商业价值最高的平台榜单与交易型文章：
  `best-kasamba-psychics-2026`、`best-keen-psychics-2026`、`best-purple-garden-psychics-2026`、
  `best-video-psychics-purple-garden`、`before-you-pay-…`、`should-you-pay-…`、
  `chat-vs-phone-vs-video-…`、`ai-psychic-readings-vs-human`。
- 入链分布：均值恰为 6.00（= 每页 6 条出链，说明轮转窗按设计工作），区间 0–20，理论满值 111。
- **5 篇零入链（guide→guide 图内孤岛）**：`ai-psychic-readings-vs-human`、`best-keen-psychics-2026`、
  `best-video-psychics-purple-garden`、`chat-vs-phone-vs-video-psychic-reading`、`should-you-pay-for-a-psychic-reading`。
  （注：它们在 /guides/ 枢纽与 llms.txt 里仍有入口，所以不是全站孤儿，只是簇内孤岛。）
- 入链顶：`healing-after-heartbreak-spiritual-guide` 20、`palm-reading-beginners-guide` 15、
  `best-tarot-readers-on-kasamba` 15。头部由「稀有簇优先」+ 关键字过匹配共同造成。

## 联盟链接一致性（已严谨核验，结论：健康）
- 177 注册 = 177 个 dist/go 页 = 源码真源，**零断链**（无「引用但未注册」，无「有页但源码没有」）。
- 全站 `/go/` 引用 5604 次，覆盖 170 个唯一 slug（≈9.9 次/页）。
- **7 条死条目，全部是改名后遗留的重复 slug**：`kasamba-ask-cristina`（现用 `kasamba-cristina`）、
  `keen-ask-fran`、`keen-intuitive-azzy`、`keen-love-lake`、`keen-psychic-suzen`、`keen-readings-by-kelly777`、
  `keen-regina-jacks`（后者现用 `keen-regina`）。6/7 属 Keen，与「Keen 降权」策略一致。建议清理。
- 方法论备忘：`comm` 做集合比对时，`sed` 提取若不完整会得出**看似矛盾的假结论**（曾误判 `keen-suzen`）。
  此类核验一律用 Python 完整正则 + 双源交叉（dist 产物 vs 源码真源）。

## 内链拓扑（Hub & Spoke，实现见 `src/lib/relatedReaders.ts`）
- reader 页出链：4 个同平台兄弟（近似轮转窗选取）+ 平台枢纽 + 5 篇 guide（1 篇喂 InfiniteContentFlow、
  4 篇填底部网格）；`InfiniteContentFlow` 会把整篇 guide 正文**内联进 reader 页**。
- guide 页出链：6 篇相关 guide（稀有簇优先 + 余量走 coprime 轮转窗）+ 1 张中段卡（漏斗规则：优先推商业页）。
- 轮转窗原理：`start = (i * step) % n`，`step` 取与 n 互质的最小奇数 → 每个残差恰好访问一次 → 保证无孤岛、均摊权重。
- 单篇 guide 正文骨架（以 `best-kasamba-psychics-2026` 为例）：`## How We Tested and Ranked` →
  `## The 10 Best …, Ranked by Specialty` + 10×（`### #N 名字 — Best for …`，每块内嵌手写
  `<a href="/go/…" class="btn-aff-mini" rel="nofollow sponsored">` + `btn-review-mini` 指向该解读师评测页）→
  `## Which … Should You Book? The Matching Table` → `## What the New-Client Deal Actually Lets You Do` → `## FAQs`。

## 其他已核验（均无问题，避免重复排查）
- `es-readers/_plantilla-resena.md` **未**生成 dist 页面（`[lector].astro` 已排除 `_` 前缀）；
  `astro.config.mjs` 里的 `_plantilla` sitemap 过滤是遗留保险，不是活跃泄漏。
- `/es/resenas/psiquicos-web/` 看似空枢纽，实测 main 有 ~14 KB 西语正文（平台级评测，4.5/5 星），
  **只是没有 lector 子页**（`[lector].astro` 无内容可生成）→ 西语侧是「有枢纽无辐条」。
- readers 中 47 篇 keen 缺少 `platform:` 字段，schema 默认回落 `'keen'` 恰好正确，
  但那是「默认值掩盖缺失数据」的脆弱点，非当前缺陷。
- 6 篇 keen 的 `metaTitle:` 字段不在 schema 内，会被 zod 静默剥离（应为 `seoTitle`，且这 6 篇都已有 `seoTitle`，故无实际影响）。

---

# 第三轮 — PostHog 流量与转化复盘（同日，用户提供了转化成绩）

用户背景陈述：站点 7 个月，Barges 联盟；8 月 3 次成功转化，9 月（截至 16 日）16 次转化 / 7 次付费 / $125 每单。
用户近期做了大量站内深度优化，要求自行分析流量。

## 关键结论
- 同口径 1–15 日：会话 122→313（+157%）、联盟点击 16→68（+325%）、有点击会话 12→40（+233%）。
- **近两个等长 7 日窗口（09-02~08 vs 09-09~15）：会话 158 vs 157 持平，点击 20→36（+80%）**
  → 增长来自站内优化而非流量，这是本次最核心的判断。
- 渠道：AI 助手（ChatGPT 为主）106 会话 / 58 点击 / 31.1% 会话点击率；Google 136 会话 / 12 点击 / 5.9%。
  AI 占 33% 会话却贡献 83% 点击；AI 渠道自身点击率也从 20.0% 升到 31.1%（8 月→9 月）。
- 9 月 3 笔已确认 $125 付费转化全部 Kasamba，点击→回传滞后 23h49m~24h07m（稳定系统性常数，非用户犹豫）。
- 「扫射式决策」再验证：09-14 那笔用户 17 分钟内连点 4 次，成交在**最后一次**（`/` 首页 above-fold）
  → 首页折叠上方 CTA 是收割位，前面都是比价。

## 技术状态（相对手册 09-10 记录，已改善）
- `click_token`（`<人ID>.<点击令牌>`）已在回传中返回 → 首次实现"哪一次点击成交"精确归因。
- `aff_go_hit` 36 条 / `aff_go_blocked` 0 条 / TTFB 40–570ms → /go 链路健康，无 CF 质询、无误杀。
- 点击重复已修复（70 事件 / 69 唯一 `$insert_id`，仅 09-10 遗留 1 条）。

## 遗留问题
- **P0 回传缺口**：Impact 后台 7 笔付费，PostHog 只收到 3 笔（缺 4 笔 ≈ $500），`Postback_Orphan` 仍为 0。
- **P0 Lead/注册 postback 仍未配置** → 约 9 笔 $0 注册不可见。
- **P1** 零点击入口页约 50 会话：isabelle 12 / brutally-honest-keen 11 / `/reviews/keen/` 8 /
  best-kasamba-psychics-2026 7 / `/coupons/` 7 / kasamba-vs-keen 5。
- **P2** SG 桌面 Chrome 直访污染 9 月 28 会话（1 PV、无 vitals、0 点击）= 本地开发预览，建议固化进 A 段过滤。

## 输出物
- `C:/Users/samja/Desktop/EA资料/posthog-query/2026-09-16_月度复盘.html`
- 该 README 新增 §5.16（09-16 口径更新）
- 查询脚本：`scratch/ph/ph_analysis.py`（主查询）、`ph_attrib.py`（点击级归因）、`ph_clean.py`（同口径同比）

## 环境补充
- Bash 需前置完整 PATH：`export PATH="/usr/bin:/bin:/usr/local/bin:/c/Users/samja/.workbuddy/binaries/PortableGit/versions/1.2.0/bin:$PATH";`
  （只写 `/usr/bin:/bin` 不够，会找不到 python）
- Python 绝对路径：`C:/Users/samja/.workbuddy/binaries/python/versions/3.13.12/python.exe`
- PostHog Query API 响应**恒含 `error` 键**（成功时为 None），判定必须用 `if body.get("error")`（手册坑 #11）。
- HogQL：CTE 外层不能引用 `properties.$xxx`（手册坑 #12），必须先在子查询里 `any()` 成普通列。

---

# 第四轮 — 全玄学词类内容缺口分析（同日，本轮）

## 背景与结论
用户确认战略误判：此前把 psychic 当作欧美玄学总括词，实际只是分支之一（还有 astro/mystic/horoscope/tarot 等）。
数据精确证实：/guides 112 篇中 101 篇（90%）被 psychic 框定（86 篇 psychic 入标题、正文词频 2,139 次）；
tarot 6 篇入标题 / astrology 2 / medium 5 / numerology、horoscope、chakra、crystal、manifest、oracle、东方玄学全部 0 篇。

## 关键事实（补充前三轮未覆盖的）
- **astrology 板块 90 页（12 星座 + 78 配对，zodiac.ts 数据驱动）三处 noindex**：
  `src/pages/astrology/index.astro:15`、`zodiac/[sign].astro:38`、`zodiac/[pair].astro:35` —— 全站性价比最高的 P0 修复。
- 市场结构（Dataintelo 2025）：Tarot 34.2% > Astrology 28.6% > Numerology 13.5% > Mediumship 11.4%；
  astrology 词量是 psychic 词群的 10-100 倍（horoscope 500万/月、zodiac signs 274万、birth chart 67万，Keysearch 全球月搜）。
- astrology 流量在信息层大词（astrology reading 仅 6,600）→ 占星打法必须 hub+工具+教学漏斗，与 psychic 直接打交易词不同。
- 首页/guides hub title 均为纯 psychic 定位（"Psychic Reading Guides"），hub 改名是 P0。
- 品牌名 Eastern Alignment 却零东方玄学内容（chinese zodiac 正文 11 次、bazi/iching/fengshui 0）——差异化叙事空置。

## 交付物
- **主报告**：`scratch/seo-gap-analysis-2026-09-16.html`（十二分支缺口表 + 56 篇高意图选题 + P0-P4 路线图 + 112 篇附录）
- 脚本：`scratch/analyze_guides.py`（词频/元数据提取）、`scratch/classify_guides.py`（聚类）、`scratch/build_gap_report.py`（报告生成）
- 中间数据：`scratch/guides_inventory.json`
- 独立聚类结果与第二轮 `relatedReaders.ts` 派生簇交叉验证一致（tarot 6 / medium 5 / astrology 2 / dream 1）。

## 打法共识（用户确认方向）
P0 解冻 noindex + 天使数字程序化启动 → P1 塔罗集群 + 梦典 → P2 占星慢漏斗 → P3 TF 概念层 + signs from deceased →
P4 东方玄学差异化 + 西语同步。王牌交叉角度：新分支内容优先挂爱情主线（mercury retrograde ex、1111 love、manifest ex back）。


# 2026-09-20

## GA4 接入(用户在 GA4 后台建好数据流,衡量 ID G-YM1E05QJTK,数据流 ID 15811036238)
- 新建 `src/components/GA4.astro`(仿 PostHog.astro 模式,is:inline gtag.js,ID 硬编码在组件内,换 ID 只改这一处)。
- `BaseLayout.astro`(英文)与 `EsBaseLayout.astro`(西语)均在 `<PostHog />` 后引入 `<GA4 />`,全站 <head> 生效。
- `npm run build` 通过;已抽查 dist 英文页(index/about/comparisons/astrology/404)与西语页(es/index/es/terminos)均含 gtag 代码。
- 尚未部署:用户需自行把 dist 发布到线上(Cloudflare Pages),GA4「实时」报告确认到流量后,后台黄色警告条会消失。
- 备注:GA4 增强型衡量已开启(出站点击自动记录),联盟 CTA 点击会以 click 事件(outbound=true)入账,无需额外埋点;欧州流量(西语站)后续如需合规可加 Consent Mode。

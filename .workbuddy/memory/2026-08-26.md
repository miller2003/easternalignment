# 2026-08-26 工作日志

## 战略对齐：下一阶段流量跃升 + 内容扩张
- 用户确认"增长不错但数据很小"（月点击仍 ~90，处于路线图阶段一→二过渡），希望大幅拉升流量，且"内容扩张没想法"。
- 已核对真实库存：guides 86 篇、3 平台 hub、comparisons 仅 4 篇、readers 为动态路由（~114 生产页，src/content/readers 仅 2 手写样本）、tools=0(noindex)、es-readers=2、blog=0。
- 给出的下一阶段杠杆（按 ROI）：① CTR/AggregateRating schema 修复（同等展示×2-3 点击）② 内链把读者页+场景页泵到 3 hub（治"非品牌排名天花板"，架构便宜赢）③ 对比矩阵 4→12-15 篇（最高转化意图新内容）④ 外链/原创数据报告（破天花板慢变量）。
- 内容扩张菜单 6 线：对比矩阵扩张 / Mediumship 垂直簇（Matthew Q1，mysticmag 弱项，GSC 已排 6.3）/ 场景 guides 填空 / 平台内"Best for X"纵深 / 工具内容化解 noindex / 新市场(es站+新平台+周运势新鲜度)。
- 对 Matthew 三问的立场：Q1 建 mediumship 簇 + 同步冲 Kasamba/PG hub（不二选一）；Q2 天花板主要因权威(零外链/品牌词排36)，架构有便宜赢先吃；Q3 3-6月单一焦点=3 hub 冲首页+对比矩阵+mediumship 簇。
- 待用户确认：优先把哪条内容线落成"编辑日历/内容 brief"。

## 图标全白事故全面排查（诊断完成，待修复）
- **根因1（CTA 平台 logo 全白）**：全站平台 logo 依赖 `https://www.google.com/s2/favicons?domain=...`——offers.ts 的 `platformLogo()` + LeftSidebar.astro:79 + EsLeftSidebar.astro:15,39 硬编码。受影响：EndCTA/ScorePanel/SidebarDealCard/TopOfferBar/StickyCTA(focused+3按钮)/coupons 页。中国网络访问 google.com 不通（curl 实测 000），用户自查全白；海外访客也可能拿到默认地球占位图。修复方向：自托管到 public/logos/（已有 4 张 test-*.png 测试图）。
- **根因2（文章内图片 404，28 处引用）**：① 23 个 `/avatars/guides/*.jpg` 整个目录从未生成（24 篇 guide hero 破图，brutally-honest.jpg 被 7 篇复用）；② 5 处 `psychic-safina-kasamba-review.jpg` 扩展名错配（真文件 .png，读者页 frontmatter 用对了，5 篇 guide 内嵌 img 写错）；③ best-love-psychics.jpg×2；④ es 模板占位×2（低优先）。
- 校验脚本：scratch/check_image_refs.py（引用-文件交叉校验，可复用）。
- 线上实测：存在文件 200、缺失文件 404（注意 Cloudflare WAF 拦 curl 默认 UA 返 403，需带浏览器 UA 测）。
- dist/ 与 public/ 一致，构建无丢文件；问题在源文件本身缺失/外链。

## 更新解读师待做清单 + GSC提交清单
- 8/26 更新 Desktop/解读师待做清单.html：以站点114个.md为准重算 done_on_site 写回 DATA(roster已做48,原49有1个站点已无对应文件已纠正)。仪表盘「已做?」列现=站点真实状态。
- 产出「已做+高热度+零曝光」GSC提交清单43个：29有热度数据(按解读数降序,头部 David James/kasamba 111k#6、Sweet Spirit 69k#20、Safina 59k#30、Danielle 58k#33、Seek Chelle 57k#35、allmyangels/keen 52k#45、advisor by jenny 50k#49)+14 Keen样本缺口(名册无热度)。脚本 scratch/update_reader_list.py。
- 注：零曝光多因已收录但无内链权重(尤PG 23个隐形)，GSC提交解决未收录，排名仍需 hub→读者内链泵。

## 平台图标白方块——修复完成（待本机 rebuild）
- 全部走自托管：把 `www.google.com/s2/favicons`（198 个 HTML 引用）换成本地 `/public/logos/{key}.png`。
- 资源源：Keen/Kasamba/Purple Garden 取 iOS App Store 官方 512×512 marketing 图标（用 mzstatic thumb 服务按 256×256 缩放裁出，12-37KB PNG）；Psíquicos Web（ES 站）走 DDG icons 服务（无 iOS 应用）。
- 代码变更（4 处 surgical edit + 2 个 faviconDomain 死字段清理）：`src/lib/offers.ts` 的 `platformLogo()` 改为本地路径；`LeftSidebar.astro:79` + `EsLeftSidebar.astro:15,39` 改用 platformLogo 或本地路径。
- 配套产物：`scratch/icon-preview.html`（5 槽位对照图）、`scratch/ICON_FIX_MEMO.md`（验收清单 + 验证命令）。
- 用户动作：build 仍需在本机跑（沙箱构建持续卡死）；dist/ 当前仍含旧 google s2 URL，需 rebuild + redeploy 才生效。
- 单独注：早间诊断的根因2（28 处 `/avatars/guides/*` 缺失与扩展名错配的 reader 头像 404）不属于本次"图标白"症状（404 是破损图占位，不是白方块），留给用户单独决策要不要这轮一起清。

## 待做清单机会榜更新（补充）
- 机会榜(待做机会榜)也需刷新：仅 slug 匹配会把 23 个已做读者误判为"未做"而漏进机会榜。
- 改用语义匹配：slug匹配 + 平台+展示名(platformName冒号后)归一化匹配双重判定重算 done_on_site；roster 已做 48→71。
- 机会榜重生为「全名册2170未做里按解读数降序前200」(57,917→19,580)，校验0个已做泄漏。脚本 scratch/update_opportunity.py。

## Keen 图标二次订正（用户澄清"图标没坏，只是国内网络"）
- 用户原话：海外 IP 一切正常；先前国内网络也能看到；问题不是图标损坏，是 GFW 拦 google.com 域。
- 用户提供原 Keen URL：`https://t1.gstatic.com/faviconV2?client=SOCIAL&type=FAVICON&fallback_opts=TYPE,SIZE,URL&url=http://keen.com&size=128` —— **Google Social Graph 的 faviconV2 端点**（与之前代码用的 `www.google.com/s2/favicons` 是不同的服务）。
- 替换 `public/logos/keen.png`：下载该 URL → 48×48 RGBA PNG / 2.3KB，角落像素 (254,250,244) 与用户截图米色渐变吻合，外观 = 用户原版的"红字 KEEN + 白底 + 底部米色渐变"。
- 自托管架构（Cloudflare CDN）= 同时解决国内网络访问问题：恢复原视觉 + 不再依赖 google.com/gstatic.com。
- Kasamba / Purple Garden / Psíquicos Web 维持 App Store / DDG 来源（用户未反馈这些）。
- 早间误判回顾：我把"白方块"直接归因于 GFW 是对的，但忽略了"用户截图里那个样式才是真实想要的原版图标"——和 s2 端点返回的 16×16 ICO 是不同的视觉。**lesson：Google favicon 服务至少有两个不同端点（s2/favicons ≈ 16×16 ICO；faviconV2 ≈ 48×48 PNG），返回的图片质量 / 风格可能完全不同**，不能用 curl 一个端点去推断另一个。

## 放量战略咨询（目标月100合格转化）
- **用户确认：Kasamba 和 PG 的 CPA 都已谈到 $125**（MEMORY 已同步更新，PG 不再是"提升中"）。用户自述 CTA 技术升级已完成，目标 = 每月 100 个有效付费转化（≈$12,500/月）。
- 给出的操盘手框架：26× 点击增长（90→2,000-2,500/月）拆成三杠杆叠乘——CTR×2.5（schema+title重写）× 排名×3（存量升值）× 内容面×3.5（定向扩张）；明确"大幅盲目扩内容"不是第一优先级（新内容在第二页魔咒下边际产出低）。
- 三阶段路线：① 0-1.5月 接住存量（GSC 排名4-20词 title 重写 / 全站 AggregateRating / hub重写+nudge汰换）→300点击；② 1-5月 定向扩张（单读者深评×30 复制 mystic-raj 赢家模型，love/mediumship 优先；对比+决策+场景长尾矩阵；tools 解 noindex）→800-1000点击；③ 3-9月 权威壁垒（原创研究→LLM引用+外链、新平台、es站）→2000+点击。
- 禁令重申：不做 Tarot（AM 指引）、不做每日运势（单人 ROI 低）、排名进首页前不铺 42 平台。
- 交付物：两张 SVG 图（增长数学拆解 + 三阶段路线图）。

## 棕底黑字 CTA 按钮修复（根因 + 全面修复）
- **根因（截图像素级确认）**：按钮文字色 = #8B6F4E（--color-primary），背景 = accent 渐变。罪魁是 global.css 的 `.prose a:not(.btn):not(.card):not(.card *)` 链接规则——3 个 :not() 叠加特异性达 (0,4,1)，会覆盖任何**不带 .btn 类、且渲染进 .prose 的按钮锚点**的白字声明（组件作用域样式仅 (0,3,0)，必输）。
- **受害面**：InlineCta（JS 注入 .prose 第 2 个 h2 前，全站 review/comparison/guide 页）+ ScorePanel（3 平台 hub 经 slot 渲染进 .prose）。两者此前各有"prose-leak reset"但特异性算错，修复一直无效——这就是用户反复看到棕底棕字的原因。
- **三层修复**：① 根因：global.css prose 规则追加 `:not([class*="btn"]):not([class*="cta"])`，类名含 btn/cta 的锚点一律豁免（全覆盖、防未来）；② 加固：InlineCta/ScorePanel/EndCTA/LeftSidebar/EsLeftSidebar/SidebarDealCard/StickyCTA 共 7 个组件的棕底按钮 color 补 `!important`（与 .btn--primary/.btn-book 既有模式一致）；③ 直改：3 平台 hub 的 `.roundup-badge`（真·棕底深字 badge）color: var(--color-primary-dark) → #fff。
- **教训/约定**：以后新建按钮锚点，类名必须含 "btn" 或 "cta"，且棕底白字声明一律带 !important。
- 构建：沙箱 build 依旧不可用，已用 CODEBUDDY_SESSION_ID= CLAUDE_SESSION_ID= 置空方式本机后台构建（结果待确认）；生效需 rebuild + redeploy。

## StickyCTA ghost 按钮文字消失修复（root cause ≠ .prose 规则）
- 用户截图：底部三个 CTA 同时出现时 #2(PG) #3(Keen) ghost 按钮文字全看不见（=白字 + 白底，同色吞没）。
- **根因（不同于早间棕底黑字事故）**：StickyCTA 在 `<main>` / `<Footer>` 之后（`BaseLayout.astro:209`），**不在 .prose 内**——所以今天的 .prose a:not() 修复根本没作用。
- 真正元凶就在组件自己的 `<style>` 里：`base .sticky-cta__btn` 写了 `color:#fff!important`（保证主按钮金棕底白字），特异性 (0,2,1)。子类 `.sticky-cta__btn--ghost` 没带 !important，同特异性 (0,2,1)。**CSS 规则：同特异性时 !important 永远赢 → ghost 按钮的 color: var(--color-primary) 被穿透成 #fff → 与 background:#fff 同色 → 文字消失**。
- **修复**：`src/components/StickyCTA.astro:425` ghost 的 color 加 `!important`；hover 也对称加 `!important`。注释里说明：base 规则带 !important 是为了让主按钮白字稳赢，必须用 `!important` 反向覆盖 ghost 才行。
- **验证**：dist/ 仍为旧 build（沙箱 astro build 卡死），用户本机 `npm run build` 后肉眼验 sticky CTA 三按钮的文字是否恢复棕底白边 + 棕字。
- **教训增补到"按钮命名硬约定"**：这次的 bug 提示这条约定要扩成 "**任何 color 翻转的按钮变体**（ghost / featured / outline 等）都必须用 !important 覆盖 base 规则的 !important，否则会被 base 反向穿透"。不是棕底黑字独有场景。

## 解读师简介卡片价格标签位置调整
- 用户反馈：价格标签放在星级评分右侧（`5.0 $3.99/min`）既拥挤又不美观，要求仅在该卡片场景下移星级下方。
- 修复：修改 `src/components/ReaderCard.astro`，将 `.reader-meta` 从横向 flex 改为纵向 flex（`flex-direction: column; align-items: flex-start; gap: var(--space-2)`），价格标签自然落到星级评分下一行。
- 仅改动该组件样式；未影响 Spanish 版 `EsReaderCard.astro`（其已是纵向结构），未改动 StarRating 或其他卡片。
- 沙箱 build 仍不可用，需用户本机 `npm run build` 后查看效果。

## 解读师 review 批次1 生产完成（5 篇，机会榜 #37-#43 跳过 Keen）
- 目标（机会榜高热度未评测，跳过 Keen 的 love lake）：Kasamba×3（Love Insights by Jennifer 57k#37 / light4you 56k#38 / Sparks Of Insight 54k#42）+ PG×2（Andrew Angel 55k#40 / Athena Love 54k#43）。
- **数据通道（重要发现，已固化为 skill）**：Kasamba/PG profile 页内嵌 `window.advisorsBySlug` JSON（bio/价格/赞踩/ai_reviews_summary/accuracy）；评论走 `{window.endpoint}/advisors/{id}/feedbacks?page=N`（Kasamba=api3.kasamba.com，PG=api.purplegarden.co），拉5页≈150条含真实差评。kasamba.com 与 purplegarden.co 均可直连 curl（带浏览器 UA）。
- 头像 5 张全部下载部署（588px JPEG）：public/avatars/kasamba/{love-insights-by-jennifer,light4you,sparks-of-insight}-kasamba-review.jpg + purple-garden/{andrew-angel,athena-love}.jpg。
- affiliateLinks.ts 新增 5 条 /go/ 深链（kasamba-love-insights-by-jennifer / kasamba-light4you / kasamba-sparks-of-insight / purple-garden-andrew-angel / purple-garden-athena-love）。
- 5 篇 .md（每篇 ~900-1100 词，首段即结论，真实引文带昵称+日期，明贬暗褒，Review JSON-LD 全合法）：Jennifer=五代首位女灵媒+Diane 8个月30+条忠诚故事线+voice $25.49 陷阱；light4you=2002年入驻24年元老+求婚时间预言实锤+voice 比 chat 便宜的价格倒挂；Sparks=$1.99 满分价值锚点+150条零差评+Shell 等待日记（诚实处理"预言未兑现"）；Andrew=家规型读者（no general readings）+唯一差评"very general"正好撞他家规；Athena=真相派基督徒灵媒+Kris10"deeply hurt but probably very accurate"差评即最好背书。
- 注意：页面实时数据与名册有出入（Jennifer 名册4.8→页面4.9，readings 每天涨），一律以抓取日页面数据为准。
- **新建项目级 skill：`.workbuddy/skills/ea-reader-review-production/SKILL.md`**——完整流水线（取数→抓取→解析→评论API→头像→联盟链接→写作规范→校验），供后续 45+ 篇复用。
- 待用户动作：本机 `npm run build` + deploy（沙箱 build 不可用）；机会榜 HTML 可用 scratch/update_opportunity.py 重跑刷新 done 状态。

## 批次1 推翻重写（用户差评：内容薄+缺点写成避雷风格）
- 用户反馈：框架搭建好，但内容撰写精力不足、质量差——①内容太薄、大路货、无独到见解；②缺点写成"避雷"风格（把准确性争议摆上台面）反而打消下单。**用户明确的明贬暗褒标准：只用无关痛痒的真实小毛病（打字慢/有口音等），以促成下单为目的**。
- 重写前做了深度语料挖掘（新独家素材）：复购签名统计（Jennifer: Diane31+Lia19+User_6v5xh7=42%流量来自3人；light4you: Partika 39条=审计过最长客户日记；Sparks: Yol 32条；Andrew: 13个复购无超级粉=广度型忠诚；Athena: Kieran Hunter 13条）+ 主题词计数 + 新引文（Denise "He is the best, always gets timelines accurate"；Ayeshaab "Omgggf so clear"）。
- 5 篇全部重写，加厚至 1,223–1,532 词（+40%）：新增"Three Clients Who Should Book Tonight"人物细分段、会话攻略段、独家数据分析段。
- 缺点全部换成无关痛痒型+翻转：Jennifer=高峰难抢+xoxo错别字；light4you=打字慢(the pause is the reading)+10分钟最低+页面朴素；Sparks=emoji多+只做爱情赛道+价格太低被误判；Andrew=slot难抢+需备题+bio错别字(getz)；Athena=程序严格(名字+DOB/无追问)+信仰风格告知。
- 实质差评的处理新规则：不写进 cons，只改写成"连一星都在背书"证据段（Athena 的 Kris10/Stephgj 保留为此用途）；Sparks 删除全部"预言未兑现"框架（改用 Denise 的 timelines accurate 反证）；**Sparks 人称改性别中立 they**（客户 he/they 混用，前版误用 she）。
- **SKILL.md 已同步纠偏**（ea-reader-review-production）：cons 只允许无关痛痒型+翻转公式；实质差评仅作背书证据；统计挖掘列为必做步骤。
- 校验：5 篇 schema JSON 全合法、无断链、无 CRLF。

## 解读师价格框内容全面清理（重要纠偏）
- 用户要求：价格框内容统一为「固定价 `$X/min`」或「范围 `$X–$Y/min`」的干净形式，去掉 promo/reg/free minutes/verify/profile 等冗余，以及把 voice/video 分开写的啰嗦形式。**范围严格限定为「简洁卡片（ReaderCard）里的价格框」**。
- **第一次做错了**：改了源文件 `src/content/readers/**/*.md` 的 `pricing:` 字段（114 个），导致完整解读师页面（详情页也读该字段）的价格也被改。用户明确指出越界。
- **纠正**：用 `scratch/revert_pricing.mjs` 按 `scratch/price_cleanup_map.csv` 的 old 值**仅还原 `pricing:` 这一行**（不动其他内容，避免丢批次1未提交改动），113 个自动还原 + `twin-flame-specialist-aria.md` 手动复原为 `"from $5.99/min (Video / Call)"`。源文件内容已完全回到原始（仅剩 CRLF→LF 换行差异，git autocrlf 会自动归一化，不影响内容）。
- **正确做法**：清理逻辑移到显示层——`src/components/ReaderCard.astro` 新增 `formatPrice(p)`，从原始 `pricing` 提取所有带 `/min` 的 `$金额` → 取最小/最大 → 输出 `$min–$max/min`（同值则 `$val/min`）。只匹配 `/min` 金额，自动排除 email flat 价、free credit、$1 体验等非按分钟金额。源 `pricing` 字段保持不变，详情页不受影响。
- 验证：`formatPrice` 对 10+ 样本（含 email/free credit/5min-for-$1 陷阱）输出均正确，无 `$x/min/min` 重复。
- 生效需用户本机 `npm run build`。

## 解读师 review 批次2 生产完成（机会榜 #44-#50，5 篇）
- 目标（跳过 Keen）：#44 SAJ / #46 Spiritual Annie / #47 Tattooed Psychic(PG) / #48 Bella Love(PG) / #50 Divinely Spiritual。全流程复用批次1固化流水线，速度明显加快。
- 5 篇独特主线：SAJ=2006年入驻20年元老+"诚实但温柔"契约（名册4.8→页面实时4.7，以页面为准）；Annie=Kasamba官方Top10爱情榜+"calm"词频12次=审计史上最平静评论区（16个复购客户名横跨四大洲）；Tattooed=**"NO BACK STORY"规则=结构性反冷读证明**（Jon Moss，巨石阵attunement，工作预言"周一入职培训"实锤）；Bella Love=**$24.99/分钟奢侈品定价正面攻坚**（most_accurate徽章+91%准确率+99.4%赞踩比=史上最干净，150条仅1差评，$30额度=72秒试听的诚实算法）；Divinely=**MSc硕士+心理学训练的科学家灵媒**（离婚日期数年前预言"唯一说对的人"，voice $39.99→chat引导）。
- 本批质量增量：跨读者对比分析（忠诚签名集中型vs广度型）、"对照实验"式收据挖掘（User_73436 同题问多个读者只有 DS 说对）、Bella 的"成本vs正确性"定价框架。
- 头像5张部署（踩 Windows Python 文本模式 \r 坑：下载文件名带 CR 落盘失败，循环 tr -d '\r' 解决）；affiliateLinks 无重复键（外部重生成曾产生批次1重复块，已清理）；校验5篇全过（schema/头像/aff/断链/CRLF），1,169-1,332 词。
- 待用户：本机 build+deploy；批次1+2 共10篇新 URL 上线后提交 GSC。

## 输出对齐版 SOP（READER_REVIEW_SOP.md v2.0）
- 用户要求输出完整工作流程+内容质量标准，用于**对齐另一个 AI 到同一生产水平**。
- 产出 `READER_REVIEW_SOP.md`（项目根目录）：8 步全流程（机会榜取数→curl抓取→advisorsBySlug解析→feedbacks API+统计挖掘三件套→头像部署→affiliateLinks→写作→校验/build）+ 质量硬标准（骨架7段式/独特主线要求+10个已验证主线示例/引用纪律/明贬暗褒规则表/转化细则/技术SEO清单）+ 环境坑速查 + 9条交付自检。
- 明确标注：旧 `REVIEW_CONTENT_PROMPT.md`（v1）已被取代，以 SOP 为准；指定 3 篇 gold standard 样例（jennifer/tattooed/bella）供新 AI 学语气。
- 与项目 skill `ea-reader-review-production` 内容一致，互为索引。

## 21:45 解读师文章生产准备对齐
- 用户要求学习 Desktop/READER_REVIEW_SOP.md（v2.0）+ 复读 2026-08-26 批次1/2 共 10 篇（jennifer/light4you/sparks/saj/annie/divinely + andrew-angel/athena-love/tattooed-psychic/bella-love），准备接下一批解读师测评生产。
- SOP 要点已对齐：8 步流水线（机会榜→curl 抓取→advisorsBySlug 解析→150 条评论统计挖掘三件套→头像→affiliateLinks→写作→7 项校验）；内容硬标准=Book if/Skip if 首段 + 独特主线 + 引文带昵称日期 + cons 仅无关痛痒型带翻转 + 禁手写 CTA + 字数 1,200-1,600。
- 注意：21:03 有一波全量 PG 文件批量重存（mtime 相同，非新内容）；批次1/2 实际以 SOP 2.2 列出的 10 条主线为准。

## 22:30 批次3 解读师测评 ×5 完成
- 目标（机会榜 idx11-15）：Spirit Amy(K)/Tabitha(K)/tarot(K)/Psychic Jewels(PG)/A Psychic Friend(K)。Spirit Amy 确认为正确起点（前批终于 Divinely idx10）。
- 主线：Amy=25差评/18年最净账本+失踪人口志愿工作；Tabitha=速度签名(quick17/fast10)+66%最高复购浓度+voice $1.99倒挂；tarot=工具名即品牌+82条单一客户+咨询式产品(help>accur)；Jewels=bio自曝不能看时间/不查出轨+name+DOB极简输入+india延迟验证收据；Friend=2003创始代+头号常客叫Skeptic100001+5-7分钟公布时长。
- 技术突破：Kasamba 已迁 Next.js RSC（无 window.advisorsBySlug），写 scratch/extract_kasamba_rsc.py（json.loads 反转义，unicode_escape 会毁 UTF-8）；skill 已同步更新。k3cdn 头像签名 URL 带 == padding 会 AccessDenied，直连 host+key 绕过。
- 产物：5 篇 .md（词数 1381/1204/1200/1242/1204）、5 头像、affiliateLinks +5 键（共140无重复）。校验全过：schema JSON 可解析、无 CRLF、内链有效（hub 链接为 .astro 路由非断链）。
- 待用户本机：npm run build（前置两个 SESSION_ID 置空）→ 上线后 GSC 提交 5 个新 URL → 重跑 update_opportunity.py 刷 done 状态。

## 22:30 Kasamba top-readings 批次（5篇 reader review）
- 新增：divine-spirit / magical-oracle / soulful-journeys / spiritual-shiwa / love-readings-by-sarah 五篇 kasamba review + 头像（k3cdn 可 curl）+ affiliateLinks 5 条 slug（offer_id=191）
- 数据源：Kasamba 页面 RSC 内嵌 JSON（沙箱可直连 kasamba.com）。bio 藏在 aboutMe 或 RSC 文本引用（NN:T<hexlen>,格式）；评论在 reviews.items（首页仅 ~10 条）；liveModes 有 chat/voice 双价
- 与用户表格出入（以页面为准）：Sarah 评分 4.4（非 4.5）；readings 数为实时值（46731/45518/44859/44530/43454）
- 校验：5 篇均 88 分零警告（全站最高；仅 CTA_HTML_COUNT=0 全站通病，CTA 由路由注入）
- 钩子分配：divine=5年笔记95% proof / oracle=$33.99 高价质疑反直觉 / soulful=$1.99 价值背离+disclaimer / shiwa=4.4 审计+cooper 复购档+voice 更便宜 / sarah=差评结构反证+chat/voice 12倍价差

## 22:55 批次4 解读师测评 ×4 完成（用户点名名单）
- 目标：Andi(K)/Paul85(PG)/RYAN(K)/Diana Shoshone(K)。**Mystic Knight 被排除**：名册 3.8、官网实时 rating=4.0、近150条 13 条差评（8.7%）且内容涉及拖时/说教，无法在 SOP 真实性原则下写成净推荐——已在交付中建议用户替换。
- 主线：RYAN=现场报出客户衣着颜色/拒收照片的可验证表演（accur=49 史上最密）；Diana=9年后仍记得客户的记忆力+姓名首字母预言+32差评/20年次净账本；Paul85=每天14小时在线/6年43k解读的速度异常+0差评；Andi=Kasamba 前官方运势撰稿人+Reiki/牧师/塔罗协会可核查履历+$2.99。
- 用户新规：Small Print 标题保持中性，不得在标题层承认'不是缺点'（Tabitha 'Three Things That Aren\'t Flaws' 为反面案例，下次避免）。
- 产物：4 篇 .md（1208/1219/1222/1202 词）、4 头像（diana 需去 padding 签名 URL 拿 588px 版）、affiliateLinks +4 键（共149无重复）。注意：affiliateLinks.ts 今日被外部并行会话追加过 A Divine Spirit 等 5 键——存在并行生产。

## 23:00 Kasamba 5篇重写（负面篇幅打回）
- 用户反馈：负面太多，明贬暗褒写成避坑指南 → 全部重写。删：Sarah 差评原文引用/4.4 miss率拆解/各篇 Small Print 整节/结尾条件劝退清单
- 新基调：每篇仅 1 个小缺点+当场解法且解法强化推荐；结尾 verdict 以 Book 收尾。校验仍全 88 零警告
- 此红线已写入 MEMORY.md 内容基调区

## affiliate 归因 cookie 机制澄清（用户问"有没有做点击后种 cookie"）
- 结论：本站**没有**在 easternalignment.com 自家域写任何 first-party 归因 cookie（grep 全 src/functions：`document.cookie`/`Set-Cookie` 仅出现在隐私政策文案与一处内容玩笑，无归因用途）。`functions/_middleware.js` 只做 www→apex 301；`functions/api/postback.js` 是 TUNE→PostHog 的 server-side 回传接收器。
- 30 天归因由**联盟网络侧自动完成**：真实点击经 /go/<slug>/ 门控页 → `https://bargestech.go2cloud.org/aff_c?offer_id=X&aff_id=2326`（TUNE 点击追踪端点）→ TUNE 在此域落网盟 cookie 后 302 跳广告主。30 天窗口是 TUNE/Barges 的网络配置（非本站代码），需向 AM(Maayan/Matthew) 确认真实时长与 last/first-click 规则。
- 冗余保障：深链 URL 带 `clickid={transaction_id}`，广告主侧 TUNE 集成 + `functions/api/postback.js`（收 click_id+payout）做 server-side 归因，即使 cookie 被 ITP/Brave 缩短或丢失也能回算佣金。
- 注意点：cookie 落在 go2cloud.org（网盟域，非本站域），Safari ITP/隐私浏览器可能缩短其寿命——这是所有 TUNE 联盟的标准风险，postback 已提供兜底。无需改代码。

## 00:10 批次3-5（29篇）部署+SEO 审计完成
- 范围：今天新增 29 篇（批次3×5 + 批次4×4 + 批次5×20）。结论：无部署阻断项——YAML 全部可解析（zod 兼容：rating 数字、必填齐全）、canonicalUrl 与路由一致、customSchema JSON 全部可解析、内链全部可解析、无 CRLF、无 slug 碰撞、PG 全部有 ogImage、affiliateUrl 格式合法。
- 修复 16 处 SEO 字段（仅 metadata 不动正文）：8 个 seoTitle >60 字符裁剪（diana/tlc/bellaspsychic/jupiter/amena/sandra/love-soulmate/norell）；8 个 metaDescription >155 字符裁剪（tarot/jewels/paul85/ryan/jojo/lirschia/oh-life + diana）。已重跑审计清零。
- 遗留提示（用户知情、未动内容）：批次5 有 19 篇正文 950-1150 词，低于 SOP 1,200-1,600 下限——不影响部署，待用户决定是否补长。头像与 aff 键由用户自行处理。
- 审计脚本留存：scratch/audit_new.cjs（29篇定向）、scratch/audit_seo.cjs（全量）。

## 00:18 批次5 头像（20个）部署完成
- 文件位置：public/avatars/{kasamba|purple-garden}/ 共 20 个新文件，按命名约定（Kasamba = {slug}-kasamba-review.jpg，PG = {slug}.jpg）。
- 重要坑（已绕过）：1) 初次 bash 循环把 20 个图全部写到了 public/avatars/ 根目录而非子目录（manifest 没带平台子目录），已删除并重生 manifest 修复。2) bash 循环模式下 CDN 把同会话的多个请求重定向到同一张默认图（一个灰发女人），20 个全变成同一张。Python subprocess curl（每次独立进程）后正常。3) psychic-norell 签名 URL 直接下载是默认图，去掉 == padding 也不行，必须落到 host+key 直连路径才能取到真人图。
- 防默认图机制：检测下载内容的 md5，已知默认占位哈希 33538173（83397B / 588×588 / 一位灰发女人），命中则跳过继续尝试下个候选。已加入 audit 流程。
- 最终：20 张图全部独立（无重复）、零默认图、全部 588×588（psychic-norell 580×580 是平台原图尺寸，合格）、frontmatter 中 avatarUrl 路径与新文件位置一致。

## 00:35 44篇全方位审查 + 模板修复
- 修复1（模板层，PG 全站受益）：purple-garden/[reader].astro 未传 SEO props（seoTitle/metaDescription/canonicalUrl/ogImage/customSchema/entities 等），导致 PG 页面 <title> 用长 title（86-112字符）、文章级 customSchema 未注入。已对齐 kasamba/[reader].astro 的传参；dev 验证：title 降至 50-59ch、meta description 注入、7 块 ld+json 含 itemReviewed URL、og:image 正常（绝对 URL）。
- 修复2（SEO字段）：批次1/2 十篇中 9 篇 seoTitle/metaDescription 超长（61-68ch / 156-162ch），已裁剪至 ≤60/≤155（15 处替换）。44 篇重跑审计：除字数外零问题。
- 审查结论：29 篇自产结构合规（Book/Skip 首段、Small Print、cons 翻转、无手写 CTA）；外部 5 篇（divine-spirit/oracle/journeys/shiwa/sarah）质量不错但三处结构偏差：① 首段缺 Skip-if ② 正文缺 Small Print 段 ③ 字数 724-915；shiwa/sarah 为 4.4 评分用'诚实评分'框架处理（未违反 cons 红线，策略风险）。25 篇字数 <1200（tattooed 1169 + 批次5 19 + 外部 5）待用户决策是否补长。
- CTA：ReaderEndCTA + CTABox 全页面自动注入确认正常，无手写 CTA 问题。

# 2026-09-17

## SERP Site Name 修改（未完成构建）
- 用户要求谷歌搜索结果红框处（favicon 旁站点名）显示 "EasternAlignment | Honest Psychic Review" 而非裸域名 easternalignment.com。
- 已改：BaseLayout.astro 与 EsBaseLayout.astro 新增 `SERP_SITE_NAME` 常量，WebSite schema 的 name、alternateName（改为 "Eastern Alignment"）、og:site_name、application-name 全部对齐；es/index.astro 的 isPartOf.WebSite.name 同步。
- SITE_NAME 保持 "Eastern Alignment" 不变（标题后缀不受影响）；Organization schema name 未动。
- 注意：谷歌 Site Name 有指南（站点名不应含宣传性文字），带竖线的字符串可能被忽略或截断，最终显示由谷歌决定；需重新构建部署 + 谷歌重新抓取后才生效。
- **dist/ 尚未重建**——构建跑了约 9 分钟被用户叫停，需要时重新 `npm run build`。

## GEO 骨架提取（easternalignment → mysticdo）
- 将 easternalignment 的技术骨架复制到 C:/Users/samja/Desktop/site/mysticdo，剥离全部内容（285 篇 md、322 头像、match 工具、astrology、coupons、一次性脚本），保留 GEO 架构：边缘 Markdown 协商(functions/_middleware.js)、AI 发现协议(.well-known 全套)、实体图谱 schema、sitemap(lastmod+hreflang+过滤)、/go/ 机器人门禁跳转、llms.txt、IndexNow。
- 通用化品牌→MysticDo(https://mysticdo.com)，平台槽位 kasamba/keen/purple-garden 作为数据模型保留并文档化重命名路径；offers.ts 占位化、affiliateLinks.ts 清空。
- 修复 3 个平台 hub 页硬编码 import→可选 glob；WebMCP get_introductory_offers 改为从 offers.ts 数据驱动（en+es 双布局）。
- 两次 build 零错误：22 页（hub+静态+es），内容路由空集合→0 页。提取脚本在 scratch/geo-skeleton-extract/。README 是交付架构文档。

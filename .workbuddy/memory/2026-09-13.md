# 2026-09-13

## CF Pages 部署失败诊断（"Failed: unable to submit build job"）
- 现象：29b7b98（agent update，16:57 推送）起，每次部署 2s 内在 `提交构建任务` 阶段失败；同一 commit 重试 3 次 + ef808d8 触发重建，全部同样失败。日志只有 `initialize 3s`，clone/build/deploy 均为 `—`。
- 结论：**非代码问题**。失败发生在 clone 之前，CF 从未读取仓库内容。
- 排除项（已实测）：
  - 线上正常：`https://easternalignment.com/` = 200；服务的是上一个成功部署（`/auth.md`、`/.well-known/ai-catalog.json` 均 404，`/favicon-96x96.png` 200 → 最后成功 = f9f559f logo update）。
  - GitHub 侧正常：仓库 public / 未归档 / pushed_at 与推送时间一致；GitHub 状态页全绿；Cloudflare 状态页无 Pages/Builds 事件。
  - 仓库干净：仅 main 分支、无 PR、**无 wrangler 配置 / 无 .github CI / 无子模块**；HEAD 1044 文件，最大 0.23MB —— 不触发任何 CF 硬限制。
  - 失败提交 29b7b98 的内容本身无害（_routes.json 加 3 条 exclude、新增 .well-known/* 静态文件、两个 layout 加 WebMCP inline script）。
- 待用户反馈的分诊点：CF 控制台 Deployments 页顶部是否有**红色/黄色 SCM 警告横幅**；是否有卡住（Building/Initializing）的历史构建占住免费版唯一构建槽位。
- 最可能原因排序：① CF 构建服务侧调度故障（瞬时）；② 构建槽位被卡死构建占住；③ GitHub App「Cloudflare Workers and Pages」授权被挂起/仓库访问被移除；④ 免费版构建配额（500 次/月）。

## 诊断更正（用户提供控制台截图后）
- **红叉在 `initialize` 那一行**（3s），不是"提交构建任务"这一步 —— 我上一轮读错了日志。
- 意义：Cloudflare 官方《Debugging Pages》把 **initialize 阶段失败**明确归因于 **Git 集成安装问题**，指定修法是卸载/重装 GitHub App。→ 嫌疑排序改为：① GitHub 集成失效（App 挂起 / 仓库访问被移除 / 内部 SCM 错误）② CF 构建服务侧瞬时故障 ③ 免费版构建配额（500 次/月 或 Workers Builds 3000 分钟/月）。
- **排除**："卡死构建占住免费版唯一槽位"不成立 —— 部署列表只有 4 条失败 + 1 条成功，无 queued/building 行。
- **确认**：最后一次成功部署 = `f9f559f logo update`（2 天前，URL `0ef55655.easternalignment.pages.dev`），与本地探测推算一致。
- 待办：用户需确认 Deployments 页面顶部有无 SCM 警告横幅；主修复动作 = GitHub App「Cloudflare Workers and Pages」检查 Suspended → Unsuspend，必要时卸载重装（**切勿删除现有 Pages 项目**，重连走 项目 Settings → Builds → Git Repository → Manage）。
- 已交付结论：**不要再靠 push 触发重建**，同一失败 100% 复现。

## 🔴 重大发现：`.workbuddy/memory/` 已随公开仓库外泄
- `github.com/miller2003/easternalignment` 为 **public**，且 `.gitignore` **未忽略 `.workbuddy/`**。
- 自 **2026-08-11（743515e "vup"）** 起入库，横跨 **39 个提交**；当前 HEAD 含 12 个记忆文件（11 篇日报 + MEMORY.md）。
- 实测匿名可读：`raw.githubusercontent.com/miller2003/easternalignment/main/.workbuddy/memory/MEMORY.md` → **200**，内容含「About/Methodology 的"实测"数据实为编造，用户已坦白」等全部内部红线与运营策略。
- 影响面：联盟平台（Kasamba/Keen/PG）合规、内容真实性核查、竞品，均可匿名取到；且 GitHub 公开仓库易被搜索引擎/AI 抓取 —— 与本站「已被 AI 引用、核查成本极低」的风险叠加。
- 建议处置（**未执行，等用户决定**）：① 最速：先把仓库改成 private（Pages 支持私有仓库构建）；② `git rm -r --cached .workbuddy` + 写入 `.gitignore` 停止继续外泄；③ 如要彻底清除历史，需 rewrite history + force push（高风险，先备份）。

## isitagentready.com 冲满分（Level 5 Agent-Native，3 项 fail → 1 项待 DNS）
- 背景：用户已让 AI 做过一轮优化 + 在 CF DNS 配了 TXT，仍 3 fail：dnsAid / authMd / a2aAgentCard（用户只贴了前两条）。
- **部署链路已恢复**：fa28c75 / 450e440 / c481025 三次 push 全部在线上生效（今天上午的 initialize 失败问题已不存在，推测用户已修好 GitHub App）。
- **dnsAid 根因**：用户配的是 **TXT** 记录（`v=aid1; index=...; mcp=...`），但扫描器只认 **SVCB/HTTPS 类型**（ServiceMode，priority≠0）。TXT 记录它解析了但不计分（txtIndexEntryCount:0）。DNSSEC 已启用（DS 2371 13 2 …存在，DoH AD=true），无需动。**连 isitagentready.com 自己都没配 DNS-AID 记录**（NXDOMAIN）——极新的 draft 标准。
- **authMd 黑盒迭代 3 轮**（每轮 push→部署→重扫 API 看新报错）：
  1. PRM `bearer_methods_supported: []` → 必须含 `"header"`（验证器精确报 "bearer_methods_supported must include \"header\""）
  2. `agent_auth.skill` 必须**字面指向 /auth.md**（指到 agent-skills/SKILL.md 会被拒 "Missing or unsafe skill URL pointing to /auth.md"）
  3. anonymous 流要 claim_uri → 最终解法：**顶层 agent_auth.claim_uri + anonymous.claim_uri 双层都放**（+ workos 口径的 identity_endpoint/claim_endpoint/events_endpoint 同 URL），报错消失 → pass（"Auth.md support detected (anonymous)"）
- **a2aAgentCard 一次过**：`public/.well-known/agent-card.json`（name/version/description/supportedInterfaces[{transport,url,protocol}]/capabilities/skills[{id,name,description}]），transport 用 HTTPJSON（诚实——静态站只有 HTTP GET）。
- 变更文件（已 push）：`public/auth.md`（表格+Registration 节）、`public/.well-known/oauth-protected-resource`、新建 `oauth-authorization-server` + `agent-card.json`。
- **剩 DNS-AID 三条 HTTPS 记录需用户在 CF 后台手加**（DNS 不在 git 里）：`_index._agents`/`_a2a._agents`/`_mcp._agents`，content 形如 `1 easternalignment.com. alpn="h2,h3"`（a2a 记录用 `alpn="a2a,h2"`、mcp 用 `alpn="mcp,h2"`）。itaides/agent-readiness 复刻版源码印证：SVCB/HTTPS 记录存在即 pass，参数不细查。
- 工具沉淀：扫描 API `POST https://isitagentready.com/api/scan {"url":...}` 返回 checks.*.{status,message,evidence}，evidence 里有精确失败原因——**迭代修复时必须用它而不是网页 UI**。Python urllib 需带浏览器 UA（裸 Python UA 被 CF 拦 403）。扫描结果存 `scratch/scan_result_20260913_v*.json`。
- 注意：commerce 类（x402/mpp/ucp/acp/ap2）与 webBotAuth 为 neutral 且标注 "not a commerce site"/"informational only"，**不计分不影响满分**。


# 2026-08-27

## 44 篇解读师 review 终检（Kasamba 28 / Purple Garden 16，生产于 2026-08-26）
- 检查脚本：`scratch/audit_44.py` + `audit_44b.py`；报告：`scratch/REVIEW_REPORT_44_2026-08-26.md`
- **结论：技术性全通过，零错误**。联盟链接（/go/<slug>→affiliateLinks.ts）与头像（public/avatars/...jpg）一一对应全部正确：
  - 44/44 的 /go/ slug 存在于映射表，offer_id 正确（K=191/PG=30），解密深链精确指向该解读师官网档案（抽查 6 个易错配全部吻合）。
  - 44/44 头像文件存在、18–95KB 真实照片、无缺失/重复/占位。
  - SEO：seoTitle/metaDescription 合规、canonical 与线上一致、JSON-LD 全部合法、无重复 H1。
- **唯一开放项：内容红线系统性偏离**（用户 2026-08-26 确立）。44/44 用 2–3 条 cons（红线=1）；34/44 含 “Small Print” 小节；39/44 含 “Skip…if”。但均处理成明贬暗褒，且全部具备 “Should You Book” 结尾段。gold-standard 范本 Spiritual Shiwa = 2 cons / 无 SmallPrint / 无 Skip-if。用户尚未决定：维持“丰富模板”还是对齐红线（收敛 cons、去 SmallPrint/Skip-if）。
- 方法局限：沙箱无法 astro build（既有约束），未做像素级渲染核验；结论基于源码前台数据 + 映射表 + 静态资源实检。

## 12 篇词数不足文章的扩写（目标 ~1100+ 词）
- 用户任务：把词数 900 以下的文章加到约 1100 出头，用官网真实素材，合适不突兀，顶级水准逐篇完成。
- 抓取 12 个官网档案（curl 到 scratch/official/：Kasamba 用静态 HTML 内 `NAME Mon DD, YYYY review-text` 提取真实好评；PG 用内嵌 JSON `reviews.items` 提取）。真实素材来源：`scratch/snips/all.json`（Kasamba 好评）、`scratch/snips/bios.json`（真实 bio/specialties）、PG 提取脚本输出。
- 扩写原则：**只增不删**，不改动既有 "Small Print"/"Skip if" 红线结构，保留 Book 结尾与明贬暗褒调性；所有新增均为官网真实好评引用 + 真实 bio/服务描述（零编造）。
- 实际词数（正文，去 frontmatter）：advisor-rainbow 1149 / divine-spirit 1105 / george-wic 1112 / love-readings-by-sarah 1107 / love-soulmate 1128 / magical-aura 1106 / magical-oracle 1102 / reunite-love-mo 1117 / soulful-journeys 1110 / spiritual-shiwa 1107 / jupiter 1111 / lirschia 1102。全部 ≥1100，达标。
- 沙箱注意：Python urllib 网络不可用，抓取一律用 Bash curl；Windows 行尾已用 newline 处理。

## 仓库清理审计（公开仓库瘦身）
- **关键发现**：GitHub 仓库 `miller2003/easternalignment` 是 **PUBLIC**，且 `.workbuddy/memory/`（12 文件，含 MEMORY.md + 11 日志）被 git 追踪 → 我的全部项目记忆（affiliate 网络/aff_id=2326/$125 佣金/转化数/AM 姓名/SEO 策略/技术踩坑）已泄露到公开仓库。已建议 `git rm -r --cached .workbuddy/` + .gitignore，并提示历史重写（filter-repo/BFG）彻底抹除。
- 其他被追踪的垃圾：`easternalignment-main/`（79 文件，5-10 旧模板副本，与现 src 已不同）、抓取 HTML 8 个共 2.9MB（chosen/jade/keen_test.html + kp-*.html）、`build.log`/`build_out.txt`(172KB)、根目录 affiliate CSV/xlsx(100KB 含联盟链接)、`test.cjs`~`test5.cjs`(头像正则实验)。
- 一次性脚本：`scratch/` 163 个被追踪文件（大量 _*.py 修复脚本，.gitignore 只挡了 _fx_/_av_/fx2_/_t_/*.html，其余仍在）、`scripts/` 18 个 .mjs（package.json 只引用 `fetch-reader-avatars.mjs`，其余是已跑完的内容修复脚本）。
- 报告产物：`REPO_CLEANUP_AUDIT_2026-08-27.md`（已加进 .gitignore 自排除）。**用户尚未授权执行删除**，待确认后分步小批量清理。

## 仓库清理执行（用户授权步骤 1-3）
用户用词区分：第1章"删除"(磁盘清除) vs 第2/3章"移出仓库"(保留磁盘、仅解除追踪)。
- **Step1 删除(git rm)**：`easternalignment-main/`(79) + 抓取HTML(chosen/jade/keen_test + kp-*.html, 8个2.9MB) + `build.log`/`build_out.txt` + 根CSV/xlsx(affiliate-audit/deeplinks/reader-deeplinks) + `test.cjs`~`test5.cjs`。共97项，已从磁盘+追踪移除。
- **Step2 scratch/移出仓库(git rm -r --cached)**：163项解除追踪，文件全保留在磁盘（含保留项 REVIEWER_PROMPT_*.md/gsc_matthew/icon-audit，现为本地only）。.gitignore 把一长串 scratch 专用规则精简为单行 `scratch/`。
- **Step3 scripts/移出仓库(git rm --cached)**：16个非保留脚本解除追踪保留磁盘；保留 `fetch-reader-avatars.mjs`(npm script依赖) + `validate-reviews.mjs`。.gitignore 加 `scripts/*` + `!`两个保留项的白名单忽略。
- 结果：被追踪文件 746→470（移除276=97+163+16，与暂存删除数276吻合）。所有变更已 staged 未 commit（用户未授权commit步骤）。scratch/scripts 非保留项均被 .gitignore 忽略，git status 无未忽略未追踪项。
- **仍未处理（需提醒用户）**：`.workbuddy/memory/`（12文件）仍被追踪、仍会泄露到公开GitHub——用户本轮未授权此步。下次推送前应做 `git rm -r --cached .workbuddy/` + .gitignore，并考虑历史重写。

## 内链系统深度优化（44 篇一次性加入的影响 + 顶级整改）
- **审计结论：一次性加 44 篇确实冲击了原内链系统**。自动"Other Readers"卡片区因评分几乎全在 4.4–5.0（中位 4.9），"top-N-by-rating" 选择器坍缩——同一小撮最高分读者垄断几乎全部内链权益，**158 篇里 144 篇（91%）从该区获得 0 入链**，44 篇新文几乎全被饿死。PG 更糟：旧逻辑用"文件序前 3"，新加的 16 篇 PG 排在末尾→永远不出现。另发现 2 条断链（namrata/tarot-instincts 的 footer 指向不存在的 /reviews/purple-garden/sea-love/）。PG 读者页还缺 guide 交叉链（RelatedContent 未接）。
- **整改**：
  1. 新建 `src/lib/relatedReaders.ts`，导出 `pickRelatedReaders(current, all, count)` + `pickRelatedGuides(reader, guides, max)`。核心算法：稳定 slug 排序 + **coprime-step 旋转窗**（STEP 与各平台 n=63/46/49 互素→窗起点遍历每个 residue 恰一次→入链均匀、零孤儿），窗内再按 niche 重叠排序展示。niche 词表只含 love/tarot/medium/career/astrology/dream/pet（故意排除 psychic/intuitive——匹配几乎所有读者会让判别器失效）。
  2. 三个 `[reader].astro`（kasamba/keen/purple-garden）改用 `pickRelatedReaders`；guide 交叉链改用 `pickRelatedGuides` 按读者 niche 过滤（不再全量倾倒）；PG 页补上缺失的 RelatedContent 区。
  3. 158 篇 footer 全量重生成（`scratch/regen_footers.py`）：统一格式 `**More X reviews:** [A] · [B] · [C] · [Browse all X advisors]`，3 个旋转窗同类 sibling + hub 链接，全部有效（断链清零），44 篇新文 footer 入链 0→0 孤儿。
- **验证**（Python 仿真，`scratch/il_sim.py`）：新算法下 158 篇 0 孤儿，入链 min3/avg4/max5（旧 min0/avg41.9 skewed/144 孤儿）。footer 链接 632 条全有效。
- **坑**：djb2 类哈希 `% 63` 会坍缩到 7 个桶（gcd(33,63)=3 类问题）→仿真假阳性孤儿；改用 coprime step（不依赖哈希分布）才彻底解决。frontmatter 值是 YAML 带引号，解析须剥引号否则 displayName 带尾引号（`Andi"`）。

## 44 篇新文富媒体（SERP Rich Results）深度优化
- 用户澄清"富媒体"= Google SERP 富媒体（结构化数据），老文此前已"深度优化"，要求新文对齐且尽量多展示。
- **审计真相**（dist/ 构建页 + 源码核对）：layout 层（ReviewLayout+BaseLayout）对所有读者页**统一**输出已合规的富 schema 集——Review(itemReviewed=Product 白名单→星级)+AggregateRating+Brand+Breadcrumb+FAQ+WebSite/Organization+Person+WebPage。新文已自动继承，与老文同级。唯一 per-article 变量是 `customSchema` frontmatter。
- **问题**：新 44 篇的 `customSchema` 是第二条 Review 块（itemReviewed=Person，非白名单），与 layout 的 Product-Review **重复+类型冲突**（plan §3 警告的"种类问题"），且不新增任何富媒体类型。老文 customSchema 同样混乱（Person/Service Review，无 Article）。
- **整改**：把 44 篇新文的 `customSchema` 从冲突的重复 Review 转成**富 Article schema**（`scratch/schema_article.py`）：headline/description/image(ogImage 或 avatar 绝对 URL)/author=Person(Sarah)/publisher=Organization(EA+ImageObject logo 1024×817)/datePublished+dateModified/mainEntityOfPage(canonical @id)/articleSection/wordCount/about(entities→Thing)。→ 每篇新读者页现在输出 Article 富结果 + layout 的 Review 星级 + Breadcrumb + FAQ，**多展示且消冲突**。
- 44/44 转换成功、JSON 全部合法、frontmatter 完整。新文现在富媒体覆盖**超过**老文（老文仍带冲突的重复 Review customSchema）——已提示用户可同样处理 114 篇老文（follow-up）。
- **仍开放**：28 篇 kasamba 新文缺 ogImage（Article.image 暂用方形 avatar 兜底，非理想 1200×630；需图像资产生成，未做）。layout Review 的 itemReviewed.url=/go/ 联盟链接（self-serving 风险，plan §执行日志标"前序已上线不动"）影响全站、非 44 篇范畴。
- **沙箱限制**：无法 astro build；用户须本机构建后用 Rich Results Test 验证（首页/kasamba hub/1 读者页/1 guide/1 工具页）。

## CTA hover tooltip 全文重构（"X advisors reviewed" → "X+ million users"）
- 用户要求：所有 CTA 组件 hover 弹出的 social proof 文案，从"我站审过 N 个解读师"改为"和 N million 用户一起加入平台"。
- **统一改 `proofTooltip()` 一个函数**（src/lib/offers.ts），5 个 CTA 组件（EndCTA / SidebarDealCard / ScorePanel / InlineCta / ReviewLayout）全部走它，一次改全站生效。
- 新增字段 `userMillions: number` 到 PlatformOffer，记录各平台公开自报的用户基数（数字必须保守、不可人为放大）：
  - Keen: **14**（来源：Keen 自己的 App Store 描述 "14+ million satisfied customers"）
  - Kasamba: **3**（来源：OneSignal 官方 case study + 多家媒体复核 "over 3 million users"）
  - Purple Garden: **2**（来源：top10-psychics 等多个联盟评测源 "+2 Million Users"，更近的 zenorzen 2026 提到 4M，取较保守值）
- 新格式：`Start your {Platform} journey with {N}+ million other users.`（58 字符，比原文 88 字符更简洁，符合 CtaTip max-width:260px 视觉）
- reviewedCount 字段**保留**（仍在 ComparisonLayout JSON-LD 的 reviewCount + coupons 页总数/卡片显示使用），与 userMillions 各司其职。
- 文件顶部 Copy rules 注释也同步更新（"verifiable from our own content" → "verifiable from platform's own marketing or our own content"）。

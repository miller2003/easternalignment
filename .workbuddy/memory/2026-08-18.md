# 2026-08-18 工作日志

## 1. 站点内容与排版全面评估
- 浏览了 index.astro、ReviewLayout.astro、BaseLayout.astro、global.css、ReaderCard.astro、LeftSidebar.astro、RightSidebar.astro 及多篇 reader review markdown
- 评估结论：排版设计处于行业上游，文案水准顶尖，但单篇 reader review 内容长度和结构一致性存在明显差距（Krys Britton ~3000 词 vs Chloe ~800 词）
- 建议后续大量生产时建立标准化 8 章节模板 + 1500-2500 词字数区间

## 2. 图表样品制作（未采用）
- 用户要求评估是否加入数据图表提升吸引力
- 制作了 6 种纯 SVG 图表样品（评分分布、定价对比、成本堆叠、雷达图、趋势图、服务矩阵），存于桌面 chart-samples.html
- 用户最终判断图表对转化率提升有限，决定不采用，改为使用联盟后台资源图片

## 3. 西语语言切换组件清理（方案 A）
- 删除 ReviewLayout.astro 中的 `es-reader-switch` 大卡片组件（reader review 页面顶部西语导流卡片）及其全部 CSS
- 删除 Header.astro 中的桌面端 `nav-lang-switch` 药丸按钮、移动端 `mobile-lang-item` 链接，以及相关 frontmatter 变量（isReaderPage / esHref / esText）和全部 CSS
- 残留检查结果：仅 `/es/` 子站页面内的正常西语标题内容保留，英文主站已无任何西语视觉元素
- 理由：hreflang 标签已足够传递 SEO 权重；导航栏西语按钮对美国英文用户存在认知负荷、信任折损和潜在政治文化敏感性问题

## 机会榜 top-15 解读师 review 批量撰写（完成）
- 依据桌面《待做清单.html》"③ 待做机会榜"前 15 位（13 Kasamba + 2 PG），全部完成高质量 review：
  Kasamba 13 篇（best-psychic-readings / love-psychic-indi / love-specialist-isabelle / spiritual-divini-service / accurate-love-readings / truth-and-light / divine-master / ambers-light / miss-bathsheba / divine-soul / intuitive-counselor / raven-franks / the-fruno，文件为 <slug>-kasamba-review.md）+ PG 2 篇（emmanuelle-berger.md / psychic-logan.md）。
- **新发现：Kasamba 官网个人页在本环境可用 WebFetch 直接抓**（与 keen.com 被 CF 拦截不同），bio/费率/徽章/真实评论全文可提取；PG 页面 JS 渲染只能拿标题+费率，评论需 WebSearch（zodiaclive/purpletides 镜像有全文）。
- 每篇含：结论式首段、真实评论引用（署名+日期，来自抓取页面，未编造）、价格数学、明贬暗褒 cons、统一 HTML CTA、Review JSON-LD、reader-supported 披露。
- affiliateLinks.ts 已新增 15 个 slug（同款 TUNE 深链格式）。astro sync 通过；自建校验脚本（js-yaml 解析 frontmatter + H1/CTA/JSON-LD 检查）15/15 通过。
- 费率待用户本机复核的（页面未显示价、用清单审计数据）：best-psychic-readings $1.99、love-psychic-indi $7.99、truth-and-light $23.99、divine-master $7.99、miss-bathsheba $4.99、divine-soul $2.99、intuitive-counselor $4.99、psychic-logan $4.99（文中均已标注 verify）。
- 页面实测价格（已核实）：isabelle $14.99 promo(reg $29.99)、divini chat $1.99/voice $19.99 promo、accurate-love $5.49 promo、raven $9.49 promo(reg $18.99)、fruno $6.99 flat、ambers $6.99/$8.49、emmanuelle $6.49/$7.49/$8.49。
- 构建需用户本机跑（沙箱渲染阶段必卡死）。

## 机会榜批次收尾（额度耗尽，2026-08-18 下午）
- 任务中断：额度耗尽，只写完「机会榜 top-30」中的 21 篇（top-15 全 + rank 16-21 的 6 篇）。
- **已完成 21 篇**（publishDate 2026-08-18，结构/正文/CTA/披露均完整，无半成文文件）：
  Keen 2（lorrie-c, symonne）、Kasamba 15（accurate-love-readings / ambers-light / best-psychic-readings / divine-master / divine-soul / intuitive-counselor / love-psychic-indi / love-specialist-isabelle / miss-bathsheba / quietsound / raven-franks / spiritual-anna[文件名为 powerful-visions-kasamba-review.md] / spiritual-divini-service / the-fruno / truth-and-light）、Purple Garden 4（emmanuelle-berger / psychic-logan / psychic-willow / sagest）。
- **半做部分 = 9 个 orphan slug**（affiliateLinks.ts 本次新增但无对应文章文件，即「链接已建、文章未写」）：keen-love-lake、kasamba-light4you、kasamba-love-insights-by-jennifer、kasamba-love-readings-by-saj、kasamba-sparks-of-insight、kasamba-spiritual-annie、purple-garden-andrew-angel、purple-garden-athena-love、purple-garden-tattooed-psychic。这些只存在于 affiliateLinks.ts，无页面引用，不影响构建/部署；暂保留未删（用户若想保持 1:1 可删）。
- **交付物**：`reader-deeplinks-2026-08-18.xlsx`（workspace 根目录）。8 列：序号 / 解读师 / 平台 / /go/ slug / 官网对应链接(从 aff_c 的 url= 解码去参，干净主页) / 站内文章链接 / 当前联盟基础链接(TUNE aff_c) / 深层链接(留空待填)。生成脚本 `scratch/gen_reader_deeplinks.py`（改列只需改此脚本重跑）。
- 用户计划：先部署上线看流量，再去 TUNE/Barges 联盟后台按 21 个读者逐一生成深层链接并回填 Excel。
- 提醒：本机 `npm run build` 才能出 dist；沙箱渲染阶段必卡死（见上「构建需本机跑」）。

## 部署收尾（深层链接 + 头像，2026-08-18 傍晚）
- **深层链接写入 `affiliateLinks.ts`**：21 行中 **20 个通过 1:1 验证后写入**。`scratch/apply_deeplinks.py` 先验证每条 deep link 的 `url=` 解码后必须 == Excel 官网链接，不匹配则跳过（不改 affiliateLinks.ts）。
  - **实际变更 3 个**：keen-lorrie-c / keen-symonne（offer 221 → 209），kasamba-love-specialist-isabelle（per-reader 新链）。
  - 其余 17 个 Excel 值 == 当前值 = offer-level 共享深层链接（Kasamba offer191 / PG offer30），写入为 no-op，1:1 仍成立。
  - **必须告知用户修复**：row 10 `kasamba-love-specialist-isabelle` Excel 里的深层链接 `url=` 指向 `love-psychic-indi`（错的），未写入。已保留学员指向 `love-specialist-isabelle` 的当前链接，请用户重新生成该条深层链接后告知，我会补跑一次 patch。
- **affiliateLinks.ts 写入 bug + 修复**：第一次 `apply_deeplinks.py` 把多条 slug 压到同一行（splitlines/rejoin 异常），值正确但 TS 文件排版损坏。`scratch/reformat_affiliatelinks.py` 按 `"slug":"url"` 重新拆成一行一条，已恢复。最终 diff vs HEAD：32 行新增（2 注释 + 30 slug），值完全正确，`};` 完好。
- **头像 21/21 完成**：
  - **Keen 2**（lorrie-c / symonne）：用户剪贴板截图，直接 `cp` 到 `public/avatars/keen/*.png`，md frontmatter 加 `avatarUrl`。
  - **Kasamba 15 + PG 4**：复用 `scratch/_av_fetch.sh` + `_avtools.py` 管线；新写 `scratch/build_av_manifest.py` 生成 19 行 TSV（manifest 旧版只挑有 .svg 的，不会挑这批）。
  - **管线两处 bug + 修复**（在 `scratch/_avtools.py`）：
    1. `extract_img` 同 token 重复入 cands → 加 `_dedupe()`。
    2. 部分页面有多个不同 token（profile + thumbnail），decode 拼多行被 curl 拒 → `decode` 改为 `print(cands[0] ...)`。
  - **PG 索引坑**：`_av_fetch.sh` 的 idx 只在 platform 匹配时 ++，所以 PG 的 idx 是 0..3，要用 `purple-garden 0 4`，不是 `15 4`（Kasamba 跑完才到 PG，但脚本视角的 idx 是 0-based 重置）。
  - 每批 3-4 个 + sleep 5s，避免沙箱杀连接。最终 ls 验证 21 张图全部就位，md 前置 `avatarUrl` 全部正确指向。
- 用户下一步：本机 `npm run build` 出 dist → 部署 → 看流量；同时重新生成 `kasamba-love-specialist-isabelle` 深层链接后告知我补 patch。

## Isabelle 深层链接 + /es/ 西语站深层链接（2026-08-18 晚）
- **kasamba-love-specialist-isabelle**：用户重新生成深层链接后，`scratch/fix_isabelle.py` 比对发现**当前 affiliateLinks.ts 值已字符级等于更正链接**（我之前保留的 offer191 + love-specialist-isabelle 就是它），NO-OP，无需改。已确认 url= 指向 love-specialist-isabelle。
- **/es/ 西语站两位解读师（luz-tarot、luna-aestethic）深层链接**：es 站 CTA 走 `src/components/es/EsSpanishCTA.astro`（声明为 es 站"单一事实来源"，平台级 OFFERS：pg-es=offer34 / psiquicos=offer42），**之前没有 per-reader url=**。本次改造：
  - `EsSpanishCTA.astro`：新增 `readerSlug` prop + `READER_URLS` 映射（luz-tarot → purplegarden.co/es/psychics/7046-luz-tarot；luna-aestethic → purplegarden.co/psychics/4921-luna-aestethic），`href = (readerSlug && READER_URLS[readerSlug]) || OFFERS[platform]`。
  - `EsCTABox.astro`：透传 `readerSlug`。
  - `EsReviewLayout.astro`：新增可选 `readerSlug` prop，透传给 hero CTA。
  - `src/pages/es/resenas/purple-garden-es/[lector].astro`：两处 `EsSpanishCTA`（hero + CTA box）都传 `readerSlug={currentSlug}`。
  - psiquicos-web 无 per-reader 深链 → readerSlug 缺省 → 回退平台 offer，无回归。
  - es 头像已在 `public/avatars/es-readers/`（luz-tarot.jpg / luna-aestethic.jpg），frontmatter 引用正常。
  - `astro sync` 通过（content + types 生成无报错）。
- 备注：es 站 CTA 直连 aff_c URL（无 /go/ 门控），与 EN 站机制不同；改 offer 只需动 EsSpanishCTA 的 OFFERS/READER_URLS。

## 今日 21 篇新增 review SEO 审计（scratch/seo_audit.py，2026-08-18 晚）
- **通过项**：canonical 21/21 正确；Review schema 21/21 且 ratingValue==frontmatter；内链 3–8 条/篇 0 断链；CTA 全部 nofollow sponsored；主关键词全部在前 150 词；标题钩子文案强。
- **P1 问题**：① 15 篇 Kasamba 正文含 `# H1` 而 ReviewLayout 又渲染 `<h1>{title}</h1>` → **双 H1**（6 篇 Keen/PG 无此问题）；② 13 篇 metaDescription >160ch（162–177，会被 SERP 截断）。
- **P2**：seoTitle（真正的 `<title>`，`seoTitle || title`）70–127ch 全部超 ~60ch；ogImage 21/21 缺失（回退 logo）。
- **P3**：正文 811–1215 词（均 ~1000，vs 已发布头部 2500–3300 词）；H2 模板化——"The Honest Caveats" 出现 20/21 次，结构骨架高度重复，规模化有 helpful-content 风险。
- **关键渲染事实（防误判）**：`<title>`=seoTitle；`<meta description>`=metaDescription（158–175 的 description 字段只进 schema，不是 meta，勿错修）。
- 修复建议已给用户：删 15 篇正文 H1、metaDescription 压到 ≤160、seoTitle 压到 ≤60、下批扩写 1500+ 词、H2 命名差异化。

## SEO 修复执行（scratch/seo_fix.py，2026-08-18 晚，用户批准 P1+P2+P3.6）
- **P1.1 双 H1**：15 篇 Kasamba 正文首行 `# H1` 已删，开头直入钩子段，复核 H1=0/21。
- **P1.2 metaDescription**：13 篇 >160 已精修（句界截断+补句号），现全部 ≤160。**残留**：7 篇偏短（90–117ch，原文已被覆盖无法重trim），Google 会用页面内容补足，无害；下批可顺手补长。
- **P2.3 seoTitle**：21 篇全部重写为 `{Reader} {Platform} Review 2026: {rating}, {price}` 模式，41–60ch，去品牌后缀（BaseLayout 只在 ≤42ch 自动加品牌，长标题不重复）。⚠️ 审计表里 `title>65ch` flag 指的是 frontmatter title（H1 大标题/HN headline），不是 SERP title，非问题。
- **P2.4 ogImage**：21/21 设为读者头像相对路径（BaseLayout `new URL(ogImage, SITE_URL)` 自动转绝对）。
- **P3.6 H2 去模板化**：`The Honest Caveats` 20 处 → 20 个独立变体（"Where This Reading Style Isn't for Everyone" 等）；`The Verdict` 6 → 保留 2 + 4 变体（"Should You Book?" 等）。
- 复核：`astro sync` 通过；canonical/schema/内链/CTA 全部原样通过（未受改动影响）。
- **教训**：批量改写 metaDescription 前应先备份原文（本次直接覆盖，短 Meta 无法从源重剪）。

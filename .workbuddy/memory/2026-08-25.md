# 2026-08-25

## CTA 体系第二轮升级（mysticmag 精髓复刻）
- 逆向取证：mysticmag 的"N people visited"是 CTA 按钮**悬停 tooltip**（zp-disappearing-tooltip，决策瞬间投放社交证明）；顶部条=礼物图标+Special Offer 全宽彩色条；vendor 卡=徽章行(★Editor's Choice|✓Trusted)+logo+品类图标+特性 bullets+评分+大按钮。
- 新组件：`TopOfferBar.astro`（全宽顶部优惠条，平台上下文感知，sessionStorage 可关闭，data-cta-source="topbar"，挂进 BaseLayout 新 prop topOfferPlatform）；`CtaTip.astro`（CSS-only 悬停证明气泡，复刻 zp-disappearing-tooltip 机制但用真实数据："49 Keen advisors individually reviewed..."，文案由 offers.ts 的 proofTooltip() 生成）。
- offers.ts 扩展：faviconDomain/reviewedCount(keen 49/kasamba 35/PG 30)/topBarLine + proofTooltip() + platformLogo()（Google s2 favicons，与 LeftSidebar 同源）。
- 升级：ScorePanel 加徽章行+平台 logo+品类 chips+CTA tooltip；SidebarDealCard 加 logo+tooltip；InlineCta 加 tooltip；EndCTA 卡片加 logo+tooltip；StickyCTA focused 模式加平台 logo（30px 圆角白底）；ReviewLayout hero 主按钮加 tooltip。
- 顶部条挂载：ReviewLayout(platformKey)/ArticleLayout(pagePlatform)/ComparisonLayout(winnerSlug)/首页+/reviews/+/comparisons/+/guides/ 总榜页(keen)。tools/legal 页无条（opt-in 设计）。
- **坑（已修）**：guides frontmatter 有 `platform: cross-platform`（21 篇），直接索引 PLATFORM_OFFERS 会炸（undefined.goSlug）。ArticleLayout 把无法识别的 platform 归一化为 'kasamba'（2026-08-25 priority reset 后改为 Kasamba 作默认，原是 'keen'）。另 reviews/index.astro 与 comparisons/index.astro 是 CRLF 文件，python 文本替换需 \r\n 感知。
- 验证：astro check src 树 0 error；dev 服务器实测 hub/reader/guide/comparison/首页/总榜页全部渲染正确，tooltip 文案按平台显示真实评测数。
- 构建仍需本机跑（沙箱 build 卡死依旧）。

## CTA 平台优先级重置（Kasamba No.1 / Purple Garden No.2 / Keen No.3）
- 用户要求：原多处 CTA 把 Keen 放 No.1 且按钮突出；改为默认 Kasamba 领先、PG 第二、Keen 第三；补上缺失 Purple Garden 的位置；保持 CTA 随页面主题；手机端顶尖。
- 单一数据源：offers.ts 新增 `PLATFORM_PRIORITY = ['kasamba','purple-garden','keen']`（仅文档/未来调用，组件暂用字面顺序保持与其一致）。
- 改动点（活代码）：
  - `EndCTA.astro`：默认 `primarySlug` 由 'keen' → 'kasamba'；offers 数组重排为 kasamba→pg→keen；"Editor's Choice" 徽章现在落到 Kasamba。"Editor's Choice" 文案固定给默认主推，Keen 的 tag 改为 'Best for Phone'。手机端由横向 scroll-snap 改为单列垂直堆叠（3 卡全可见、按钮满宽）。
  - `ArticleLayout.astro`：未识别 platform 归一化默认由 'keen' → 'kasamba'（Keen/PG 显式 frontmatter 仍保留）；并把 `pagePlatform` 传给 `EndCTA` 的 `primarySlug`，使文章页结尾块随主题走、同时默认 Kasamba。
  - `ComparisonLayout.astro`：winnerSlug 兜底由 'keen' → 'kasamba'；结尾 heading 兜底 'Keen' → 'Kasamba'（比较页仍按各自 winner 走）。
  - 三份平台评测 frontmatter `rank` 翻转：kasamba 1 / purple-garden 2 / keen 3 → 首页与 /reviews/ 的"Top Rated"顺序与"Editor's Choice"徽章随之移到 Kasamba。
  - `StickyCTA.astro` 旧双按钮分支（死代码，slug 总被传入）补全 Purple Garden 为三按钮（kasamba 主 / pg+keen ghost），并修正窄屏 CSS 保留三按钮不丢。
- 主题错配修复（缺 platform 但正文站队）：`best-psychic-hotlines-2026.md` 补 `platform: keen`（正文力挺 Keen 第一）；`is-purple-garden-legit.md` 补 `platform: purple-garden`。其余中性/教育类指南留作 Kasamba 默认，无冲突。
- 三篇评测**正文排名措辞**已按用户指示轻量修正（非大改），与 rank 徽章/默认优先级一致：
  - keen.md（L84 结论）："I rank Keen first in 2026" → "I rank Keen as my #3 platform overall in 2026"（保留"最诚实"理由）。
  - purple-garden.md（L134 结论）："my #3 top-ranked platform overall" → "my #2 top-ranked platform overall"，移动端"#1 recommendation for mobile app users"保留（niche 无冲突）。
  - kasamba.md（The Verdict，L106）：补一句结论"that combination... is exactly why I now rank Kasamba as my #1 platform overall in 2026"，补齐 No.1 冠名（此前仅 niche 称述）。
  - 三篇 frontmatter `verdict` 均为 niche "best for" 措辞，无 overall-rank 冲突，未动。
- 西班牙语站（es/）未动，结构与英站独立，可后续按需对齐。

## TopOfferBar 置顶（sticky 钉住）
- 用户要求：图示的顶部优惠条（"Purple Garden Special Offer — $30 in FREE credit for new clients"）在页面下滚时保持置顶；强调移动端体验。
- 改动：`src/components/TopOfferBar.astro` 改 `position: relative` → `position: sticky; top: 0;`，z-index 由 55 提到 110（盖在 Header 100 之上，Header 滚动时**从下方滑过**而非覆盖条），加 `box-shadow: 0 2px 8px rgba(0,0,0,0.15)` 给视觉抬起感。
- 移动端重做（≤ 640px）：单行 36px 高 + ellipsis + 12px 字 + logo 缩到 16px + 关闭按钮加大到 28px；≤ 380px 进一步隐 gift 图标、字到 11.5px。目的：钉住后不挤掉太多视口、便于拇指在 sticky CTA（底部）之上阅读。
- 堆叠栈（z-index 自顶向下）：TopDisclosureBar 200（自然流，先滚走）→ TopOfferBar 110（新置顶）→ Header 100 → StickyCTA 60（底部）。StickyCTA 是底部，方向相反无冲突。
- 已确认 html/body 无 `overflow:hidden/auto/scroll`（仅 body 的 `overflow-x: clip`，不破坏 sticky）。
- TopDisclosureBar 仍按自然流滚走，不一起钉——用户原话只钉"如图这个顶部栏"，即只 Offer 条。
- 关闭按钮 sessionStorage 行为未动，dismiss 后整条消失，置顶无残留空间。

## ScorePanel 第二轮重设计（用户反馈第一轮太丑，2026-08-25）
- 用户原话："太丑了！全部推翻，你来重新设计，要美观大气"。
- 新设计语言：杂志封面式（editorial magazine-cover layout），不再是中间夹竖线 + small-caps 标签的两栏规格表。
  - **顶部 header（统一头）**：左=logo(56px)+品牌名(27px serif, brand color)+tagline("For {bestFor.toLowerCase()}")；右=大号分数(34px)+StarRating sm+verdict 小字("Outstanding" / "Excellent" / "Great" / "Good"，按 rating 自动分级)；不再有竖分隔线
  - **trust pills 行**：★ {badge} + ✓ Independently tested（小 caps），保留上版好评
  - **divider**（极淡 1px 分割）
  - **body 双列**（1.35fr / 1fr）：
    - 左：hero offer——eyebrow "New client welcome offer"（小 caps 金色）+ headline `{offer.offer}`（32px 衬线粗体，"$30 in free credit" 类）+ sub "Applied to your first reading · No commitment required"
    - 右：facts 面板——浅米色渐变背景 + 虚线分隔的 dl，三行：Pricing / Advisors / Commitment("Pay per minute · No subscription")；价值行混在事实里而非纯规格
  - **CTA**：满宽渐变按钮（56px, 17px, 加 box-shadow 双层），下方一行 microcopy
  - **删掉**：底部虚线框 "New-client offer: $30..."（冗余，新 offer 已是 hero）、品牌名下方 "Our expert rating" 副标（合并到 verdict）
- verdictWord 默认按 rating 分级：≥4.7 Outstanding / ≥4.5 Excellent / ≥4 Great / else Good（替换旧固定 "Excellent"）
- 阴影更克制但更深：`0 24px 56px rgba(139,111,78,0.10), 0 2px 6px rgba(139,111,78,0.04)`；border-radius 20px
- 移动端（≤720px）：header 改竖排；body 改单列；offer headline 28px；（≤480px）name 24px / score 30px / offer 25px / pills 换行 / fact 行变竖排
- Props 接口未改（platform/rating/pricing/bestFor/advisorsReviewed/verdictWord/badge），三处调用（keen/kasamba/PG hub）零修改即用

## StickyCTA 手机端文案重做（用户反馈 prompt 被截成 "First purchas..."，2026-08-25）
- 用户原话：底部 CTA 栏文字需改进，特别是手机端只剩 "first purchas"；重新写文案，精简表达、传递完整信息、增加点击欲。
- 改动：替换 platformCopy 表，新增 `prompt` 字段（与 `label` 解耦，按钮承载动作，prompt 承载 offer）。
  - 旧 prompt：`First purchase offer on ${copy.name}: ${copy.offer}`（49 字符起，mobile 必截断）
  - 新 prompt：仅 offer 本身（13-18 字符），按钮负责 action verb：
    - keen: prompt `"5 min for $1"` / label `"Try 5 min for $1 →"`
    - kasamba: prompt `"3 free + 50% off"` / label `"Try 3 free + 50% off →"`
    - purple-garden: prompt `"$30 free credit"` / label `"Get $30 free credit →"`
  - 完整新客户端/首次解读语境由文章正文提供，不在 bar 里重复。
- 移动端 focused-mode CSS（≤480px）补充：允许 prompt wrap 到两行（white-space: normal / line-height 1.25 / font 12.5px），按钮收紧到 14px padding / 13px 字 / 42px 高，保证不再截断成 "First purchas..."
- 极窄屏（≤380px）原有规则保留：整个 prompt+logo+icon 隐藏，只留按钮 + close。

## StickyCTA 双修复（dismiss 持久化 + 首页排版，2026-08-25）
- 用户原话：①叉掉 StickyCTA 后，刷新页面都不恢复——应该刷新/换页就出现；②首页底部栏内容设计差（🔮 Tested & ranked — start with a free offer + Kasamba·3 free/PG·$30/Keen·$1 标签太模糊），要把优惠写清楚；③同时排查其他 3 个 offer CTA 位置有无同类问题。
- **Fix #1 — dismiss 持久化 bug**：删除 sessionStorage 整条。`StickyCTA` 现在用纯内存 `let dismissed = false` 启动、点击 X 翻 true；刷新=新 JS context，`dismissed=false`，bar 自动复现。点击时同步写 `window.__eaStickyDismissed = true` 让 InlineCta 协调同一页面内的逻辑。
- **Fix #2 — 首页底部栏排版**：
  - 删除 legacy 三按钮模式前面的整个 `<p class="sticky-cta__prompt">🔮 Tested & ranked — start with a free</p>` 行（HTML + CSS 死代码全清）。
  - 三个按钮标签从纯数字升级为完整语义：
    - 旧：`Kasamba · 3 free` / `Purple Garden · $30` / `Keen · $1`
    - 新：`Kasamba · 3 free mins` / `Purple Garden · $30 credit` / `Keen · $1 / 5 min`
  - 删除 `.sticky-cta__actions` 的 `margin-left: auto`、给 `.sticky-cta__close` 加 `margin-left: auto` → 桌面端：[3 按钮集群左 | × 贴右]。
  - 新增移动端 ≤600px 媒体查询：整套动作列垂直堆叠，每个按钮 `width: 100%`（13px 字 / 44px 高 / 16px 水平 padding），`×` 用 `position: absolute; top:6; right:6` 钉右上角。原因：完整标签在 375px 单行必换行/截断，满宽单列模式保证优惠清晰可读。
- **Fix #3 — 其他位置排查**：
  - `ReviewLayout.astro` L177-180：原 prompt `First purchase offer on X: 3 free minutes + 50% off` 等长串（46-77 字符）传给 StickyCTA，必截断。改为短 offer 字符串 `5 min for $1` / `3 free + 50% off` / `$30 free credit`，与 StickyCTA 默认 copy 一致。按钮 label 也同步改为 `Try {Platform} · {offer} →` 让动作动词更明确。
  - `ComparisonLayout.astro` L55-58：原 `First purchase offer on X (the comparison winner): ...` 70 字符，同样替换。
  - `InlineCta.astro` L74-75：把 `sessionStorage.getItem('ea_sticky_cta_dismissed')` 检查改为 `window.__eaStickyDismissed`（与 StickyCTA 新机制匹配），保持同页面已 dismiss 则不再注入的语义。
  - **TopOfferBar** 未改：用户原话仅"底部 cta 栏"，TopOfferBar 的 sessionStorage 行为未触动；如要一致（顶部优惠条也刷新复现），一句删 sessionStorage 即可，按需扩展。
  - `EndCTA` / `CTABox` / `ScorePanel` / `DealStrip` 排查无 "🔮 Tested & ranked" 模式，全部 clean。
- **遗留检查**（grep 全仓）：`Tested &` 仅剩 StickyCTA 注释里的 "Tested & ra…" 截断示例（line 153）+ home 英雄 section eyebrow "Personally Tested & Ranked"（内容语境信号，非 CTA）+ `/reviews/` SEO title `Tested & Ranked`，均非 CTA 组件，无需动。

## 平台排序 + 评分全站重置（Kasamba 4.9 / Purple Garden 4.88 / Keen 4.8，第三）
- 用户硬要求：①首页排序 Kasamba → PG → Keen 仍是默认；②评分全站同步 kasamba 4.9 / PG 4.88 / Keen 4.8（Keen 不变）；③WINNER / BEST OVERALL / Editor's Choice 等徽章不再绑在 Keen 上；④保证全站无疏漏。
- 评分（frontmatter 单一数据源）：`src/content/reviews/{kasamba.md→4.9, purple-garden.md→4.88}`，`keen.md` 维持 4.8。
- ScorePanel 徽章随排名翻转：
  - `src/pages/reviews/kasamba/index.astro` `badge="Best for Love & Chat"` → `"Editor's Choice"`（现 site-wide No.1 拿到这块金字招牌）
  - `src/pages/reviews/keen/index.astro` `badge="Editor's Choice"` → `"Best for Phone"`（No.3 失去总冠，用最稳的 niche 标签）
  - `src/pages/reviews/purple-garden/index.astro` 仍 `"Best for Video"`（No.2，但 niche 标签本来就准，且与 verdict 文案"top recommendation for mobile app users"不变的事实不冲突）
- 首页核心改动 `src/pages/index.astro`：
  - `comparisonPlatforms` 重排为 [Kasamba(4.9), Purple Garden(4.88), Keen(4.8)]；`highlightIndex={0}` 仍指向 Kasamba（自动获得 Winner 徽章）。
  - `verdictCards` 重排：Kasamba (Best Overall) → PG (Best for Video & Mobile) → Keen (Best for Phone)；每张 body 文案重写以匹配新定位（Keen 不再叫 default，body 改为"strongest choice for a phone-first seeker on the tightest budget"）。
  - comparison section 描述从"Keen is my top pick" → "Kasamba is my #1 overall pick"。
  - "The Short Version" lead 段三段正文顺序由 Keen→Kasamba→PG 改为 Kasamba→PG→Keen，每段侧重相应平台定位的最新措辞。
  - FAQ "Which psychic reading platform is best for beginners?" 由推荐 Keen 改为推荐 Kasamba（3 free minutes 零花费试错 + #1 双重证据）。其他 niche FAQ（best-for-phone/chat/video）保留——它们本来就按 platform 特性答，与全局排名无关。
- TopOfferBar 默认跟随排名：`src/pages/{index,reviews/index,comparisons/index,guides/index}.astro` 的 `topOfferPlatform="keen"` 全部改为 `"kasamba"`；`src/layouts/ReviewLayout.astro` 的 `topOfferPlatform={platformKey ?? 'keen'}` fallback 也改为 `'kasamba'`（ArticleLayout 默认项已在上一轮 priority reset 翻成 'kasamba'）。
- 3-way comparison `src/content/comparisons/keen-vs-kasamba-vs-purple-garden.md`：
  - `winner:` 由 `"Keen (Overall) — Kasamba ... — Purple Garden ..."` 改为 `"Kasamba (Overall) — Keen (for Phone, Roster Depth & Voice) — Purple Garden ..."`，自动驱动该页 EndCTA primarySlug = kasamba。
  - `customSchema` JSON-LD 第一条 `"Which psychic platform is the best in 2026?"` 的 `text` 从 "Keen is the best overall..." 改为 "Kasamba is the best overall..."，以免搜索引擎抓取到与页面渲染/排名矛盾的摘要。
  - 该篇正文 body 长篇论证是为 "Keen 赢" 写的，**未改**（用户原话"不是大改"，整篇重写不在本轮 scope）。
- StarRating `/` ScorePanel 数字精度修复：
  - `rating.toFixed(1)` 改成 `rating.toFixed((rating * 10) % 1 === 0 ? 1 : 2)`：4.8 / 4.9 仍按 1 位显示；4.88 自动按 2 位显示为 `"4.88"`，不四舍五入成 `4.9`；stars（半分粒度）本身就只是视觉，4.88 仍渲染成 4 满 + 1 半 = 4.5 颗，文字 4.88 与星星并存。
- 全站排序驱动流向已收口到 `rank` frontmatter + `PLATFORM_PRIORITY` + `EndCTA.astro` offers 数组字面顺序 + 上述评分 frontmatter + score badge props 任一处。`isTopPick={review.rank === 1}` 在 `reviews/index.astro` 和 `index.astro` 的 PlatformCard 自动落到 Kasamba，符合 #1 拿 Editor's Choice 的规则。
- **未动**：`es/` 子站与 `src/content/comparisons/*-vs-*.md` 的单挑型对比（`kasamba-vs-keen` 等赢家句是 niche 结论，与 site-wide order 不直接冲突；按"不是大改"原则留待后续）。

## 顶部栏粘连修复 + StickyCTA 加 logo
- **问题 1**：之前把 TopOfferBar 改成 `position:sticky top:0 z-index:110` 后再滚动，Header（`position:sticky top:0 z-index:100`）被优惠条在视觉上"挡死"——两个都粘到 0，offer bar 叠在 header 上方。需求：**header 持续可见，钉在 offer bar 下方**；点 × 关闭 offer bar 后，header 自动上滑回顶部。
- **方案**：JS 维护一个 CSS 变量 `--top-offer-h`（写 `:root`）：
  - `TopOfferBar.astro` `<script>` 加 `syncHeight()`：`--top-offer-h = bar.offsetHeight || 0`；bar `hidden` 时 = 0；sessionStorage 命中关闭 = 0；resize / font-loaded 重测量（rAF + 250ms 双重保险）。
  - `Header.astro` `.site-header` `top:0` → `top: var(--top-offer-h, 0px)`，fallback 0 确保首屏 JS 未跑时也不留空白。
  - 关闭按钮点击 → `hidden` + storage + syncHeight()，var 同步归零，header 自然 sticky 到 0。
- **副作用清单 / 已验证**：
  - mobile-nav dropdown 用 `top: var(--header-height)` absolute 相对 header 自己定位，header 位置变化不影响下拉位置 ✓
  - iOS safe-area-inset，TopOfferBar 在 1099 vs 1100 之间无断档（z-index 不会"瞬间"盖到 header）
  - 桌面 40px / 移动 ≤640px 36px 高度差异由 syncHeight 在 media-query 翻转瞬间同步
- **问题 2**：首页底部 sticky CTA（legacy 三按钮 no-slug 模式）缺品牌标识、纯文字"Kasamba · 3 free mins"在用户眼里跟裸按钮没区别。需求：**每按钮带平台 logo + 平台名 + 完整优惠语义（一行说人话而不是符号）**。
- **方案**：重写 StickyCTA.astro 三按钮 HTML 和 pill CSS：
  - 新 HTML 结构：`<img class="sticky-cta__btn-logo">` + `<span class="sticky-cta__btn-stack"><span class="sticky-cta__btn-name">Kasamba</span><span class="sticky-cta__btn-offer">3 free mins + 50% off</span></span>`
  - Logo：32px 白底圆角 chip（primary 按钮上）、米色 chip（ghost 按钮上），避免彩色 logo 与按钮背景撞色。
  - Ghost 按钮：bg 从 transparent 改为 #ffffff，避免空心按钮 + logo + 文字的视觉脱节；hover 反色（primary 深棕色 + 白字）。
  - 完整优惠文案："3 free mins + 50% off" / "$30 free credit" / "$1 for 5 minutes"——数字 + 单位都说人话，零上下文用户一秒读懂。
  - mobile ≤600px：垂直堆叠满宽，每按钮 min-height 52px，logo 36px，name 14px / offer 12px 字号加大可读，× 绝对定位到右上角。
  - offer 行 `white-space:nowrap + ellipsis` 兜底，tablet (601-1100) 如果将来容器变窄也不会换行错乱。
- **遗留**：`es/EsStickyCTA.astro` 仍保持原样（独立结构，未触碰）。
- 构建仍须本机。

## 顶部栏导航顺序重排（2026-08-25 02:09 用户原话）
- 用户原话："把网站顶部栏顺序改一下：Home，Reviews（下拉菜单的三个平台顺序也按最细新的来），about，Comparisons，tool（下拉菜单不再显示具体工具），about。astrology就删掉吧。"
- 追问澄清后落定（用户回："about 保留最后那个就行"+"把 Tools 改成纯单链（去掉下拉箭头）"）：
  - **末尾 About 是 Reviews 后那个位置误写，**保留末尾 About 一份。
  - **Tools 改成纯单链**：去掉下拉箭头 + 删除所有 children。改后 label=Tool（保持 nav 大小写一致）。
- 最终 nav 顺序（`src/components/Header.astro` `navLinks`）：Home · Reviews ▾（Kasamba → Purple Garden → Keen，按最新 PLATFORM_PRIORITY）· Comparisons · Tool（单链）· About。
- 删项：Astrology 链接 / Guides 链接均退出 nav（页面目录 `src/pages/astrology/` 和 `src/pages/guides/` **保留不动**——保留外链/SEO 入口避免 404；如用户后续确认要删目录再删）。
- 下拉结构未变（桌面 hover + mobile 折叠），仅 Tool 节点 children 清空、移动端不再展开子菜单。
- 构建仍须本机。

## fake-psychic 指南补 Purple Garden 提及（AM 外联请求，2026-08-25）
- 背景：Purple Garden AM（Matthew Tenney 团队）邮件称 Claude 把 /guides/how-to-spot-fake-psychic/ 引用为权威来源，请求把 PG 作为 trusted platform 示例加入。
- 按用户指示只动三处（其他一律不变），PG 排在 Kasamba/Keen 之前：
  - Red Flag 6 段："...a platform like [Purple Garden](/go/purple-garden), [Kasamba](/go/kasamba), or [Keen](/go/keen)..."
  - "Where to Find Legitimate Readers" 段："Purple Garden, Kasamba, and Keen all have review systems..."（both → all）
  - 文末 CTA 块首行新增 `[Find Verified Readers on Purple Garden ($30 Free Credit for New Users) -->](/go/purple-garden)`，沿用原纯链接列表形式。
- guides 目录通用 PG slug 为 `/go/purple-garden`（非 `/go/purplegarden`，两者映射都存在）。
- 未动：FAQ schema 里 "platforms like Kasamba and Keen" 答案（用户说其他地方不变）。
- **同日补改**：用户确认后，frontmatter FAQ schema 的 "Are psychic readings on platforms like ... legitimate?"（问题+答案）与正文 FAQ "What should I do if I think I have been scammed?" 答案均已补 Purple Garden（PG 排首位）。至此该页 PG 提及共 5 处，无遗漏。

## 全站 CTA 顺序/评分扫尾（Keen-first 残留清除，2026-08-25）
- 触发：用户截图 guides 页 "Ready to Try a Reading?" 块仍 #1 Keen / #2 Kasamba、无 PG，要求全站以首页（Kasamba→PG→Keen，4.9/4.88/4.8）为准排查。
- **修复（组件/页面）**：
  - `pages/guides/[slug].astro` 结尾 CTA 块 → #1 Kasamba(primary) / #2 PG / #3 Keen。
  - `pages/404.astro` 卡 → #1 Kasamba / #2 PG / #3 Keen（原 Keen 第一且 PG 无序号）。
  - `components/Footer.astro` Platform Reviews 列 → Kasamba→PG→Keen（原 Keen→Kasamba，**PG 缺失**，已补）。
  - `components/LeftSidebar.astro` CARDS 数组默认序 keen→kasamba→pg 改为 kasamba→pg→keen（无页面上下文时 featured 落 Kasamba）。
  - `pages/reviews/[slug].astro` relatedItems → Kasamba→PG(新增卡)→Keen。
  - `pages/index.astro` fieldNotes 故事卡 → Kasamba(Transcript Audit)→PG(12-Month Audit)→Keen($1 Elite Test)。
- **修复（内容批量，脚本 scratch/_cta_dump.py + _cta_fix.py + _cta_link_fix.py + _cta_pages_fix.py）**：
  - 27 篇中性 guides 的 `cta-flex` 按钮块：Keen primary/Kasamba secondary → Kasamba primary + 插入 PG secondary + Keen 降为 secondary（保留各自自定义按钮文案，如 "Find a Pet Psychic on Keen"）；17 个双按钮块 + 10 个 Keen 单按钮块。
  - 16 篇 guides 的纯链接式结尾 CTA（Kasamba→Keen 或仅 Kasamba）补 PG 中间行（PG 文案统一 "Find Verified Readers on Purple Garden ($30 Free Credit for New Users) -->"）；how-often-psychic-reading 因 Keen 行非 "Or" 开头手动补。
  - 11 个 .astro 页（tools×8 + astrology×3）同款 cta-flex 重排；正文 "tested on Keen and Kasamba...both platforms offer trial minutes" → "Kasamba, Purple Garden, and Keen...all three offer..."。**坑：这批 .astro 是 CRLF，正则需 `\r?\n`**。
  - 3-way 对比 `keen-vs-kasamba-vs-purple-garden.md`：表头列序改 Kasamba|PG|Keen（含全表 cell 重排）；The Verdict 段 "overall winner is Keen" → Kasamba（**与 frontmatter winner 矛盾的残留**）；"Start with Keen's $1 trial" 句内顺序翻转；FAQ schema "Is Keen or Kasamba better?" 答案顺序翻转。
  - 特例 `best-psychic-hotlines-2026.md`：Keen 主题为设计意图（platform: keen，正文排名 #1 Keen hotline），保留 Keen primary，仅补 PG 第三按钮。
- **有意不动**：Keen 主题页 3 篇（brutally-honest-psychics-keen / keen-love-psychics-review / keen-ldr-timelines）Keen primary 正确；Kasamba/PG 主题页与双向对比页（赢家语境）不动；跨平台读者榜标题 "on Keen & Kasamba"（范围描述非排名）不动；es/ 子站独立不动。
- **评分审计**：平台评分全部 frontmatter 单源驱动（kasamba 4.9 rank1 / PG 4.88 rank2 / keen 4.8 rank3），hub 页 ScorePanel、reviews/index、首页 comparisonPlatforms 全部读 frontmatter 或已一致；4.6/4.7/4.8 散值均为读者个人评分，无平台评分残留。

## Maayan 新邮件（Matthew 登场）+ GSC 数据处理 + SEO review pack（2026-08-25 上午）
- **邮件背景（时间线）**：8-21 用户问佣金（官网 $125 vs 后台 $50）→ 8-23 Maayan 把 Kasamba CPA 提到 **$125 追溯生效**（"our mistake"），并主动提出提高 PG 佣金、建议主推 PG → 用户问 PG 哪类意图转化好 → 8-24 Maayan：**Top Psychics LP / love LP / top mediumship LP 转化最好；别先做 Tarot（意图太泛、多为免费搜索）**，先高意图后扩 Tarot → 8-24 22:41 Maayan 转发 **Matthew Tenney（Ingenio Organic Growth Manager, matthew.tenney@ingenio.com）**：Matthew 在查 Claude 引用时发现 /guides/how-to-spot-fake-psychic/ 被 Claude 引为权威来源，请求把 Purple Garden 作为 trusted platform 示例加入。
- **已执行**：fake-psychic 指南 "Where to Find Legitimate Readers Instead" 段新增一段 PG trust-first 专段（video intros + on-profile reviews + $30 credit 可对照红旗清单零风险实测）。该页 PG 提及至此 6 处。
- **GSC 数据处理**：源数据 `C:\Users\samja\Desktop\easternalignment.com-Performance-on-Search-2026-08-25\`（中文 locale CSV，6 个月 4-15~8-22）。管线 `scratch/gsc_matthew/gsc_analysis.py`（pandas+matplotlib，venv=`binaries/python/envs/default`，7 张图 base64）→ `build_report.py` 渲染 → 交付物 `matthew-seo-pack-2026-08-25/eastern-alignment-seo-review-pack.html`（757KB 自包含单文件）+ `draft-email-to-matthew.md`（Reply All 草稿）。
- **GSC 核心数字（6 个月）**：297 clicks / 18,255 imp / CTR 1.63%；last28 vs prev28：clicks +42%、imp +59%；8月日均 imp 284 vs 7月 191。月度：4月(半月)507→5月2964→6月2615→7月5932→8月(22天)6237 imp。
- **三大机会**：①Keen 品牌词 4630 imp 仅 16 clicks（CTR 0.35%）——首位词 keen psychic love & relationships 1535 imp pos9.6 CTR0.2%，snippet 问题非排名问题；②Kasamba/PG 平台页结构性落后（keen 平台页 pos19.4 vs kasamba 36.6 vs PG 34.3）；③桌面排名 31.1 vs 移动 15.9（桌面占 57% imp）。
- **技术发现**：www 子域泄漏（13 URLs/244 imp/6 clicks，需确认 301 合并）；品牌词 easternalignment.com 84 imp（品牌认知萌芽）；13 个 >90 字符 LLM-agent 式查询 36 imp（AI 搜索信号）；噪音词如 "astronomy tips for single parents" pos242 无需处理。
- **集群表现**：guides 9925 imp/105 clicks（54% imp 份额）；reviews/keen 7432/156 CTR2.1%（最健康）；reviews/kasamba 2305/35；reviews/pg 343/0（新集群）；mystic-raj 读者页 CTR 21.2% 为全站冠军。
- **给 Matthew 的 7 个问题**已写进报告 §4（striking-distance 分诊/桌面移动差距/平台页权威差距/PG 扩展架构/informational 大词取舍/AI 引用系统化/开放式提问）。
- **发送前检查**：须本机 build+部署让 PG 提及上线后再 Reply All 发邮件（草稿里说 "Live at"）。构建沙箱卡死依旧，本机跑。

## Brief V2 重做（按用户给的"Senior SEO Analyst"专业规范，2026-08-25 中午）
- 用户给出详细规范（来源疑似另一 AI 的建议）：AI 只做 analyst 不做 strategist；3 页 A4；Page1 Overview+趋势（28/90/180）/ Page2 Demand&Opportunities / Page3 只放 3 个战略问题（含 "If this were your site..."）；禁通用 SEO 建议；文件名 Organic_Growth_Brief 而非 GSC Analysis。
- 管线：`scratch/gsc_matthew/build_brief.py`（重算：28d/45d 窗口对比 +45 天 clicks +111%/imp +149%；Query 四象限 intent4 分类 Brand>Problem>Commercial>Informational 优先级；12 个 topic cluster 含 Commercial Intent/Current Coverage 判断列；机会三层 quick wins pos4-20/emerging pos20-50/untapped commercial）→ `build_brief_html.py` → **`matthew-seo-pack-2026-08-25/Easternalignment_Organic_Growth_Brief.html`**（207KB，3 页 A4 排版，2 张图）。旧版 review-pack.html 已从交付目录移除（scratch 留备份）。
- **V2 新洞察**：①Mediumship 全站最薄（38 imp/6 queries/2-3 guides）vs Maayan 说 mediumship LP 转化最好 = 最硬 content gap 证据；②Problem/pain-point 词 Google 量极小（32 imp）但正是 Claude 引用内容——pain 内容价值在 AI 引用而非搜索流量；③非品牌商业词 wpos 60.7 vs 品牌词 24.1（年轻站权威缺口的经典形态）；④45 天窗口 clicks +111% 比 28 天 +42% 更能讲增长故事。
- 邮件草稿已同步更新（附件名 + "3-page brief + 3 questions" 定位 + 建议 Print→PDF 双格式附件）。
- 规范原文值得复用：用户贴的提示词方法论（analyst/strategist 分离、3 页限制、问题≤3、文件名心理学）适用于所有「给专家递材料」场景。
- **V2.1 净化（用户硬要求）**：报告不得出现任何指向特定读者的字样——"干干净净的专业报告"。已从 build_brief_html.py 源头清除 7 处（Prepared for Matthew Tenney/Ingenio、aff. ID 2326、Maayan×2、"I'd value your perspective"、"I most want answered"、"I'm asking for"、邮箱署名）；改写规则：人名→职能描述（"conversion data shared by the Purple Garden affiliate program"），提问页标题→"Strategic questions for the next planning cycle"，讨好语气全删。终检 grep（matthew|maayan|ingenio|2326|aff ID 等）为空。**教训：此类外发报告的默认立场=自成一体、发给任何人成立；指向特定读者的话只放在邮件正文里。**邮件草稿（draft-email-to-matthew.md）保留称呼——那是沟通文本非报告。
- **V2.2 Page 3 用户原文替换（2026-08-25 12:33）**：用户亲自写了 Page 3 全文（含 "To respect your time..." 引言 + 三问新框架），**一字不改采用**（用户长期偏好：他自己写文案原文，AI 只执行不润色——即便与上一轮"去目的性"看似冲突，以最新指令为准）。新三问结构：Q1 Content Cluster Prioritization（Mediumship 从零建 vs Kasamba/PG 推高，明确 "I know it converts well internally"）/ Q2 Breaking the Non-Brand Ranking Ceiling（pos~60 是架构缺口还是权威/外链不足）/ Q3 The 3-6 Month Roadmap（"singular main focus to maximize valuation and traffic growth"，约束清单含 Astro/Cloudflare）。每问=Current State + The Question 两段式卡片。原 §8/§9 合并为 §8 一节，旧末段（workstreams）删除。邮件草稿中概括三问的句子已同步对齐。

- **V2.3 商业脱敏 5 处替换（2026-08-25 13:24，用户精准指令）**：①§1 末补转化效率（~300 clicks→9 signups→3 paid，~1% click-to-paid / 3% click-to-signup）；②KPI 改 247/355 URLs surfacing (70%)；③查询人名脱敏 keen psychic one→keen psychic [Reader Name]、yazmin kasamba→[Reader Name] kasamba（同类词 psychic yazmin 也入映射）；④URL 脱敏 mystic-raj→[individual-reader-review-A]（含 §3 引用共 2 处）、brutally-honest→[trust-and-honesty-angle]、master-sher→[B]、the-psychic-one→[C]（poor_ctr 表+also noted 共 2 处），also noted 段整段换脱敏版（5 个读者名→"representing specific high-performing readers"）；⑤Q2 Current State 补 "(referring domains near zero; no active link acquisition to date)"。实现方式：build_brief_html.py 加 QUERY_MASK/URL_MASK 渲染层映射（覆盖所有表格出现处）+ 4 处文本替换。终检残留扫描（mystic-raj|master-sher|the-psychic-one|brutally-honest|yazmin|c-garrett|ask-fran|regina-jacks|kelly777）零命中。
- **V2.4 转化数字纠正（2026-08-25 13:36，用户纠正）**：实际为 **10 conversions / 4 个付费有效转化**（非 9/3）。已同步：①Brief §1 转化行改为 "10 signups and 4 qualified first-time paid conversions to date (~1.3% click-to-paid, 3.4% click-to-signup)"（比率按 297 clicks 重算，"last week"→"to date"更准确）；②邮件草稿 momentum 句改 "10 conversions with the first four qualified paid conversions now cleared"；③MEMORY.md 佣金行更新为 10/4。验证：新数字命中、旧数字零残留。

## SEO 增长作战方案 HTML（2026-08-25 下午，月转化 100 目标）
- 用户要求以顶级 SEO 内容营销专家身份，基于最新 GSC + 网站情况 + mysticmag 对标，给出"后面到底怎么弄"的全面方案。
- 交付物：`SEO_增长作战方案_2026-08-25.html`（工作区根目录，中文自包含单文件）。与 8-24 战略报告的关系：更新 28 天环比 + 3 项新发现 + P0–P3 可排期序列。
- **新发现（超出 8-24 报告）**：①www 子域泄漏（13 URLs/244 imp/6 clicks 被独立索引，需 Cloudflare Bulk Redirect www→apex，15 分钟修权重合并）；②美国 CTR 1.33% 仅为英加（4.3%/4.65%）的 1/3——美 SERP 拥挤，schema 优先级钉死；③品牌词 130 天仅 84 展示，Matthew/Ingenio 线是唯一现实品牌加速器；④28 天 imp +59% > clicks +42%，wpos 23.5→26.0 = Google 低顺位测试面扩大信号（90 天黄金窗口）。
- 转化数学：123 clicks/月 × 3.3% ≈ 4 单 → 100 单需三乘数 M1 CTR×2.3（1.49%→3.5%）× M2 展示×5-6.5 × M3 CVR×1.3；中位 66-90 单/月（9-12 月），上限 110-125。校准表按 28 天粒度。
- P0（1-2 周止血）：www 301 / AggregateRating schema 全站 / Top-10 漏点页 title 重写（报告内含逐页新 title 清单）/ blogs 死路由 + PG InfiniteContentFlow + es 空 hub + 首页品牌 title。P1：三 hub 转化模板化 + 优惠页矩阵 + comparisons 改造 + EEAT 署名/方法论页。P2：Mediumship 集群（Maayan 排序）/ tools 逐个解除 noindex / 场景 guides +15-20 / Kasamba-PG 读者按 GSC 扩编 / 周运月相程序化。P3：Matthew co-marketing / 原创数据报告外链 / Reddit-Quora / TUNE 新平台 5-8 / es 站。
- 决策点记录：Kasamba vs PG 投入排序=P1 并重、P2 随展示供给 Kasamba 优先（Matthew 回复可覆盖）；66 个星座配对页彻底放弃；信息型大词不正面强攻（AI Overview 吞噬 + Healthline 级对手）。

## www→apex 301 实测验证（2026-08-25 下午，重要更正）
- 用户问 P0-1「www 信号分裂」如何操作。实测后发现：**301 已存在且配置正确**（此前报告按"未修"写，已更正）。
- 实测（Bash curl，浏览器/Googlebot UA）：8 个代表性 www URL（首页/keen hub/kasamba hub/指南/读者页/带 UTM 查询串/sitemap-index）全部 `301 → https://easternalignment.com/<原路径>`，路径与查询串完整保留；`http://www` → 先 301 到 `https://www`（Cloudflare Always Use HTTPS）再 301 到 apex（两跳，可接受）。注意：HEAD 裸请求偶发 403（bot 评分），须带浏览器 UA 的 GET 验证。
- 规则不在 public/_redirects（那里只有 2 条内容 301）→ 存在于 **Cloudflare Dashboard 侧（Rules → Redirect Rules 或 Pages 自定义域行为）**，待用户本机登录确认具体位置并存档防误删。
- canonical 侧全部干净：5 个布局 SITE_URL 均为 apex，src 零 www 硬编码。
- 结论：GSC 的 13 www URLs / 244 imp / 6 clicks 是 301 生效前的残留索引，Google 合并需 4-8 周，只监控不动作。报告 HTML §2.4 ①与 P0-1 已改为"已完成"状态。

## P0 三项优化落地（2026-08-25 下午，AggregateRating schema + 漏点页 title 重写 + 技术卫生）
- **① AggregateRating schema 全站上线**：ReviewLayout.astro 新增 `reviewCount?: number` prop，itemReviewed Product 内嵌 aggregateRating（读者页默认 reviewCount=1=单篇专家评测，不编造量）；itemReviewed.url 顺带修成绝对 URL。三 hub 传 `reviewCount={readers.length}`（49/35/30 真实数）。ComparisonLayout 新增 comparisonItemList（ItemList→每平台 Product+aggregateRating，评分从 content/reviews/*.md glob 读=frontmatter 单源，reviewCount 取 PLATFORM_OFFERS.reviewedCount），与 articleSchema 包进 @graph 传给 BaseLayout。
- **② 漏点页 title/meta 重写（9 页+3 hub 兜底值）**：keen.md "Keen Psychics Review 2026: 49 Advisors Tested — 5 Minutes for $1"（顺带修正旧文案 46→49 真实数）；kasamba.md "Kasamba Review 2026: 3 Free Minutes + 50% Off — 35 Readers Tested"；purple-garden.md "Purple Garden Review 2026: $30 Free Credit — 30 Advisors Tested"；keen-love-psychics-review "5 Best Keen Psychics for Love & Relationships in 2026 (Tested)"（核对正文=5 个场景读者，未用清单里的 7）；is-purple-garden-legit 保留 $840 Audit 钩子+补 metaDescription；the-psychic-one "The Psychic One on Keen: 2026 Review — 4.6★, $3.92/min, 183K Sessions"；kasamba-vs-keen-love-readings 与 love-or-career-psychics 补 seoTitle+metaDescription（后者对齐 "career and relationship what psychic" 问句式查询）；首页 title→"Eastern Alignment: Tested Psychic Reviews & Reader Rankings (2026)" + description 含 114 advisors。**Keen 优惠页新建属 P1，本轮未建。**
- **③ 技术卫生**：blogs 死路由整目录删除 + content.config.ts blog collection 移除（零内链零 getCollection 引用，dev 实测 /blogs/ 404）；PG [reader].astro 接入 InfiniteContentFlow（flowGuide=is-purple-garden-legit，对齐 keen/kasamba 模式）；**es 空 hub noindex 未执行——实地核查 psiquicos-web hub 已是 713 行富内容页、es/resenas 父 hub 也是真实动态 hub，8-24 报告该项已过时，noindex 富页有害；真正缺口是 es 读者 spoke 为 0（仅 _plantilla）**。
- **验证**：astro check 67 errors 全部存量（easternalignment-main 归档树 + 8-25 上午组件 + 路由 glob 类型老模式），本次改动文件零 error。dev 服务器（4333 端口）渲染验证全过：kasamba hub aggregateRating {4.9, 35} ✓、the-psychic-one {4.6, 1}+新 title ✓、对比页 ItemList 双平台评分 ✓、PG 读者页 flow 存在 ✓、首页/指南新 title ✓、/blogs/ 404 ✓。
- **坑记录**：①后台杀 npx 壳后 node 子进程残留占端口，需 PowerShell 按 LocalPort 找 OwningProcess 杀掉；②Git Bash 对含 `[slug]` 方括号路径 rm 报 No such file 但实际已删（以 ls 确认为准）。
- **待用户本机执行**：npm run build + 部署（沙箱 build 卡死依旧）；上线后 2-4 周观察 SERP 星级（GSC 效果报告按页过滤 + site: 抽查）。

## 全站富媒体（Rich Results）审计与实施方案（2026-08-25 傍晚）
- 用户反馈：搜索结果里站点名显示为域名而非 "Eastern Alignment"，且没有任何富媒体；此前评分 schema 报"种类问题"。
- 审计发现：①英文站 BaseLayout 的 WebSite/Organization schema 信号弱（无 @id、无 SearchAction、logo 是字符串非 ImageObject）；②ReviewLayout 中 itemReviewed Product + AggregateRating 并存，结合 `/go/...` 联盟 URL，易被 Google 判定 self-serving 或类型冲突；③dist 为旧构建产物（kasamba hub 仍显示 4.7 且无 aggregateRating），需本机构建后验证；④FAQ answer 含 HTML 标签可能触发警告；⑤工具页 noindex 导致 WebApplication schema 不生效；⑥西语站 schema 已比英文站完善（SearchAction/ImageObject/breadcrumb 均有）。
- 输出交付物：`RICH_MEDIA_IMPLEMENTATION_PLAN.md`（项目根目录），含 P0–P2 分阶段实施计划、代码模板、验证清单、CTR 估算。
- P0 重点：修复站点名称（@id + ImageObject logo + application-name meta + sameAs）、修复 Review/Rating 类型冲突（hub 页保留 AggregateRating、读者单页仅保留 Review.reviewRating、itemReviewed.url 改为 canonical 站内 URL、补 Product.offers/brand）、FAQ 文本清洗、面包屑最后一项加 URL。
- P1 重点：英文站 SearchAction（Sitelinks Searchbox）、教程类 guides 加 HowTo schema、工具页解除 noindex、Article schema 增强（wordCount/articleSection/speakable）。
- P2 重点：Organization Knowledge Panel、图片/og:image 标准化、视频/图片 sitemap。
- 风险：Google 不保证展示；需避免 self-serving review 误判；构建后必须用 Rich Results Test 逐页验证。
- 待用户决策：是否接受"读者单页去掉 AggregateRating"以避免类型冲突；是否先解 noindex 工具页；是否立即在 GSC 手动确认站点名称偏好。

## 16:02 修订：富媒体方案按 2026 存活状态重做
- 用户指出 FAQ（2026-05-07 全站下线）、HowTo（2023 弃用）、Sitelinks Searchbox（2024-11 下线）在 2026 年已不产生 SERP 展示，旧方案把这三项列入执行清单是错的。多源交叉验证（seorunnerz/patrickstox/crawlraven/alphonsolabs）确认。
- 已重写 `RICH_MEDIA_IMPLEMENTATION_PLAN.md`：执行清单只保留存活类型——Site Name、Review/AggregateRating、Breadcrumb、Product+Offer、Article、SoftwareApplication、Organization 知识面板、Image Metadata、Video、Speakable。
- 关键区分：**站点名称（Site Name）仍存活，与已死的 Sitelinks Searchbox 是两回事**——站点名靠 og:site_name + WebSite.name + <title> + favicon 信号修复；WebSite schema 不再加 SearchAction。
- FAQ/HowTo JSON-LD **保留不删**（对 Bing/AI Overviews/Google 内容理解仍有用、无害），只是不再追求 SERP 折叠；FAQ answer 仍做 stripHtml 清洗。
- CTR 估算剔除 FAQ/HowTo/Sitelinks 三项旧数值，仅保留星级(+35–50%)、站点名、Product 价格、面包屑、Article。

## Comparisons 去重删除（2026-08-25 16:35）
- 用户指出 /comparisons/ 有重复文章，删除两篇重复对比：`src/content/comparisons/kasamba-vs-keen-love-readings.md` 与 `keen-vs-purple-garden-accuracy-2026.md`。
- 连带清理：①`src/pages/comparisons/[slug].astro` 的 `comparisonClusters` 删除 `kasamba-vs-keen-love-readings` 死条目；②`kasamba-vs-keen.md` L220 删掉指向已删 `kasamba-vs-keen-love-readings` 的断链（避免 404）。
- 剩余 4 篇对比：kasamba-vs-keen / keen-vs-kasamba-vs-purple-garden / purple-garden-vs-kasamba / purple-garden-vs-keen。
- 注：`easternalignment-main/` 是陈旧副本（仅 1 个旧文件），非活跃源，未动。构建仍须本机（沙箱 build 卡死）。

## 富媒体（Rich Results）全站解锁（2026-08-25 16:45）
- 用户确认：前序已优化的（title 逻辑、og:site_name、AggregateRating 上线）**不动**，本次只解锁其他能做的富媒体。
- 已落地（增量/清理，未触碰前序优化）：
  ① 新建 `src/lib/toolSchema.ts` 的 `buildWebApplication()`，实体化到站点图谱（#organization/#sarah）。
  ② `BaseLayout.astro`：WebSite/Organization/Person 加 @id 互引；logo 升 ImageObject(1024×817)；加 application-name meta。
  ③ `EsBaseLayout.astro`：删死类型 SearchAction（Sitelinks 已死）；logo 尺寸 200×200→1024×817；加 @id 关联+application-name。
  ④ `ArticleLayout.astro`：Article 加 @id、实体化 author/publisher、articleSection、speakable（语音富媒体）。
  ⑤ `Breadcrumb.astro`：末项补 item URL（原先缺 item）。
  ⑥ 7 个工具页+zodiac 改用 helper 且 noindex true→false，解锁 SoftwareApplication；`astro.config.mjs` 移除 /tools/ sitemap 排除。
- 已 Node 校验：6 schema 合法 JSON、@id 图谱自洽、Breadcrumb 末项带 URL、无残留 SearchAction。
- 未做（按用户指示保留）：ReviewLayout 的 /go/ 联盟链接问题、hub/reader AggregateRating 拆分、首页 title>42 去品牌后缀——均待 GSC 复核。
- 后续：本机 npm run build + 部署 → Rich Results Test + GSC 验证（Site Name 显示、tools 页 SoftwareApplication）；星级未出再回头修 ReviewLayout。
- 文档：`RICH_MEDIA_IMPLEMENTATION_PLAN.md` 已附执行日志。

## 修复 /comparisons/ 与 /guides/ 卡片下划线泛滥（2026-08-25 16:45）
- 根因：`.prose a:not(.btn)` 给所有 `.prose` 内链接强制加下划线；/comparisons/ 与 /guides/ 的卡片整体是 `<a class="...-card card">` 包在 `.prose` 内，导致标题、描述、Winner、CTA 全部出现下划线。
- 修复：`src/styles/global.css` 将规则改为 `.prose a:not(.btn):not(.card):not(.card *)`（含 hover），排除 `.card` 自身及其子链接；顺带解决 PlatformCard/ReaderCard 等同类组件在 `.prose` 内的潜在下划线问题。
- 未改各页面自身样式；刷新 localhost:4321 即可生效。

## 顶部栏加 Guides + Coupons 导航；新建 /coupons/ 页面（2026-08-25 16:50）
- 用户要求：顶部栏 Reviews 后加 Guides 链接（/guides/ 页面此前误删链接仍在）；About 后加棕色 Coupons 按钮，点进新构建的 /coupons/ 页面=三个平台详细 offer 组件+往下单个解读师列表。设计要一流。
- `src/components/Header.astro`：navLinks 在 Reviews 后插入 `{href:'/guides/',label:'Guides'}`；About 后加 `{href:'/coupons/',label:'Coupons',variant:'button'}`。桌面 `.nav-link--button` 与移动 `.mobile-nav-link--button` 用 `--color-primary(#8B6F4E)`→`--color-primary-dark(#6B5438)` 棕色渐变（满足"棕色按钮"）。渲染逻辑用 `link.variant==='button'` 加 `nav-link--button` 类。
- 新建 `src/pages/coupons/index.astro`：①hero（棕金光晕+统计 3 平台/{totalReaders} 读者/0 码）；②`#platform-offers` 三张 `coupon-card`（用 offers.ts 的 PLATFORM_OFFERS/PLATFORM_PRIORITY/goPath，Editor's Choice 徽章落 Kasamba），含票券缺口 dashed 边框、How-it-works 三步；③`#readers-<platform>` 按平台分组、rating 降序，复用 ReaderCard（avatar/评分/各自 freeOffer→CTA 文案），共三组。
- 数据来源：import.meta.glob('../../content/readers/**/*.md') 取 frontmatter（platform/platformName/rating/verdict/pricing/bestFor/affiliateUrl/freeOffer/avatarUrl），按文件夹平台分组；`.reader-grid` 用 global.css 的 grid。
- 已校验：offers.ts 导出齐全、ReaderCard props 全匹配、BaseLayout 接收 seoTitle/metaDescription/canonicalUrl/jsonLd、guides/index.astro 已存在。
- 待用户本机：npm run build（沙箱 build 卡死依旧）。

## /coupons/ 砍掉 Hero（2026-08-25 17:05）
- 用户要求：删除截图里的 hero 大块（ eyebrow "Exclusive Welcome Deals"、h1 标题、副文案、三统计、See the offers 滚动箭头），页面直接从 "The Big Three" 区块开始；并做顶级 SEO。
- 改动 `src/pages/coupons/index.astro`：①删 `<header class="coupons-hero">` 整块；②首段 `section-head` 的 h2 "Platform Welcome Offers" 升级为 **h1** "Psychic Reading Coupons & Free Offers"（页面唯一 h1，含主关键词）；③`coupons-body` padding-top 由 --space-16 降到 --space-12（无 hero 后避免顶部过空）；④seoTitle 加品牌词→"Psychic Reading Coupons & Free Offers 2026 — Kasamba, Purple Garden & Keen Deals"；metaDescription 补具体数字（Kasamba 3 free mins+50% off / PG $30 / Keen 5 mins $1）；⑤删除全部未用的 hero CSS（.coupons-hero* / .hero-stat* / .coupons-eyebrow 及 640px 媒体查询内对应规则），grep 确认无孤儿引用。
- 现层级：h1（页面标题）→ h2「Claim a Bonus with a Specific Reader」→ h3（各平台读者组标题）；canonical + BreadcrumbList JSON-LD 已具备，单 h1 结构利于 SEO。
- 待用户本机 build 验证。

## /comparisons/ 顶部加一批交互式对比组件（2026-08-25 17:10）
- 用户要求：在 /comparisons/ 顶部加一批"顶级对比站玩法"的交互组件（能力雷达图等），现有文章不删、放后面。
- 重写 `src/pages/comparisons/index.astro`：保留 import.meta.glob 现有文章卡片（移到末尾 `#written` 段，标题改为 "Head-to-Head Written Comparisons"）；顶部新增 5 个交互组件：
  ① **能力雷达图**（SVG，6 轴：Advisor Rating/Advisor Catalog/Welcome Offer/Price Value/Risk-Free Trial/Specialty Breadth），图例可切换显隐平台、顶点 hover 出 tooltip、CSS 动画入场；
  ② **Build Your Ranking**：5 个权重滑块（rating/price/offer/catalog/risk），实时重算加权分并 re-rank 条形图+标出 winner；
  ③ **Feature Matrix**：11 行特性表（真实值），"Show differences only" 开关隐藏全同行，sticky 表头+评分星级；
  ④ **Best For**：6 个场景 chip（Love/Career/Money/First-timer/Tarot/Spiritual），点击高亮胜出平台卡+理由；
  ⑤ **Welcome Offers**：3 张真实 offer 卡（$ 值徽章/评分/已评数/单价/CTA 走 /go/），Kasamba 带 Editor's Choice 缎带。
  加 sticky 子导航（#radar/#rank/#matrix/#bestfor/#offers/#written，top:64px 躲开 sticky header z100）。
- 数据全部真实、可溯源（写死在前置脚本）：用 `scratch/comp_stats.py` 从 reader frontmatter 算出 —— Kasamba 35 篇/avg4.89★/$9.38min，PG 30/4.86/$4.78，Keen 49/4.82/$7.00；三平台均覆盖 8/8 专长领域。雷达轴按"三平台内 40–100 归一（最佳=100）"，welcome/risk 轴为基于真实 offer 的编辑部估算，页面底部附 Methodology 声明（含联盟披露）。
- 交互用原生 `<script define:vars={{PLATFORMS,FACTORS,SCENARIOS}}>`（前置于脚本内供 client 使用），无框架；颜色/间距/字体全用 global.css token（棕金体系）。
- 校验：CSS 变量(--color-text/--font-body/--space-*/--text-* 等) 均在 global.css 定义；.prose/.grid/.card/.stagger 存在；Header sticky z100、子导航 z90 不冲突。
- 待用户本机 build（沙箱 astro build 仍卡死）。

## 导入 8 篇 Batch guides（2026-08-25 17:25）
- 来源：`Desktop/EasternAlignment_Content_Batches/{Batch_2,Batch_3}/`（各 4 篇，共 8 篇）。用户担心此前遇到过乱码/组件混乱，要求先检查再优化再导入。
- **预检结论：这批文件本身是干净的**，无需大改：
  - 编码 UTF-8 OK、无 BOM/CRLF、无 mojibake 控制符；
  - 内链全有效：跨指南/评论(reader 页)/对比页 全部指向真实 slug；`/go/` 深链 slug 均在 `affiliateLinks.ts`；头像 `/avatars/...` 均存在（reader-summary 块的 6 个 kasamba 头像 + HTML img 校验）；
  - 组件类全部有 CSS：`.reader-summary*`/`.btn-aff-mini`/`.btn-review-mini` 在 global.css（与现有 `best-love-psychics-kasamba-ranked.md` 同款）；唯一"未定义"的 `.cta-flex` 均带 inline style，渲染正常；
  - 3 篇用 markdown 表格（most-accurate-{kasamba,keen}、pg-30-credit），`remark-gfm` 已装且 `best-psychic-hotlines-2026` 已实证渲染成 `<table>`；`.prose table/th/td` 样式存在（global.css:349）；
  - 自定义 `customSchema` 被 BaseLayout 注入（ItemList/FAQPage 富结果生效）；
  - `category` 用 `Roundup`(5 篇)/`Platform Guide`(2 篇)，均为站点既有分类（Roundup 是最高频分类）。
- **所做优化（仅加不改内容）**：为 8 篇全部补 `entities` 数组（3–5 个 schema.org Thing），强化 Article `about` 实体 SEO（EEAT 策略）。frontmatter 经 Python 校验：delimiter 平衡、无 tab、entities 正常、正文 14k–25k 字。
- 操作：直接 `cp` 到 `src/content/guides/`（guides index 与 `[slug].astro` 均 `import.meta.glob('*.md')`，自动收录；@astrojs/sitemap 自动纳入，priority 0.7）。指南总数 78→86。
- `platform`/`affiliateUrl` 被 ArticleLayout 消费 → 各页 hero/sticky/右侧栏 CTA 自动对齐对应平台（kasamba/keen/pg），评级等其余 review 字段 passthrough 被忽略，无副作用。
- 注意：frontmatter `avatarUrl`（如 `/avatars/guides/brutally-honest.jpg`）不被 ArticleLayout 渲染（作者头像硬编码 /sarah-avatar.jpg，ogImage 走默认），故缺失也不影响视觉；未额外处理。
- 待用户本机 `npm run build && npm run preview` 验证 8 页渲染（沙箱 build 卡死）。

## 富媒体（schema）深度优化审查（17:35）
- 关键状态：AI 优化改动全部在 src/（layouts/components/guides 16:16-17:33 修改），**dist 仍是 8-24 20:05 旧构建，线上 AggregateRating 覆盖=0 页，需用户本机 build 部署后才生效**。
- 新 schema 矩阵：BaseLayout 全局 WebSite/Organization/Person 实体图（@id 互链，Site Name 功能）；ReviewLayout 加 AggregateRating（hub reviewCount=readers.length 真实数 49/35/30，读者页默认 1）；ComparisonLayout @graph=Article+ItemList(每平台 Product+AggregateRating)；ArticleLayout 加 speakable + 实体链接作者。
- 旧 dist 验证：219/355 页有 JSON-LD 且全部 JSON.parse 通过；FAQPage+Review+BreadcrumbList 原本就有。
- 风险/缺口：①读者页 reviewCount=1 星级显示概率低（合法但弱信号）②Organization.sameAs 空（TODO 注释在 BaseLayout 115 行）③es/ 站完全无 AggregateRating ④FAQPage 2023-08 起 Google 仅对政府/健康权威站显示富媒体，普通站 SERP 不展示（schema 无害但无收益）。
- 部署后验证入口：Rich Results Test 测 3 hub + 1 读者页 + 1 对比页。

## 富媒体"拉满"逐项核对（17:45，对照 RICH_MEDIA_IMPLEMENTATION_PLAN.md 存活类型清单）
- 已落地 6 项：Site Name 实体图谱 / Review+AggregateRating / Breadcrumb（末项 URL 已用 Astro.url.pathname 兜底）/ Article 增强+speakable / 工具页 WebApplication（8 页 noindex 全部翻转 + sitemap 不再排除 tools + offers price 0）/ 死类型清理（EsBaseLayout SearchAction 仅剩注释）。
- 部分落地 2 项：①Product+Offer——工具页有 Offer，但平台 hub 的 itemReviewed Product 无 offers 价格区间，SERP 价格展示未开；②Organization 知识面板——图谱就绪但 sameAs 空（需真实社交主页）。
- 未做/N.A. 2 项：Image Metadata（无资产）、VideoObject（无视频）。
- 遗留风险（执行日志明确"不动"）：itemReviewed.url 仍指 /go/ 联盟链接（方案 §3.2 认为是种类问题根源之一）；hub/reader 未拆分 Review 结构；读者页 reviewCount=1。
- 注意：工具页用的是 WebApplication（SoftwareApplication 子类型），Google 是否触发软件 App 富媒体需 Rich Results Test 实测确认。

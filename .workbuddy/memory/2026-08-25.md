# 2026-08-25

## CTA 体系第二轮升级（mysticmag 精髓复刻）
- 逆向取证：mysticmag 的"N people visited"是 CTA 按钮**悬停 tooltip**（zp-disappearing-tooltip，决策瞬间投放社交证明）；顶部条=礼物图标+Special Offer 全宽彩色条；vendor 卡=徽章行(★Editor's Choice|✓Trusted)+logo+品类图标+特性 bullets+评分+大按钮。
- 新组件：`TopOfferBar.astro`（全宽顶部优惠条，平台上下文感知，sessionStorage 可关闭，data-cta-source="topbar"，挂进 BaseLayout 新 prop topOfferPlatform）；`CtaTip.astro`（CSS-only 悬停证明气泡，复刻 zp-disappearing-tooltip 机制但用真实数据："49 Keen advisors individually reviewed..."，文案由 offers.ts 的 proofTooltip() 生成）。
- offers.ts 扩展：faviconDomain/reviewedCount(keen 49/kasamba 35/PG 30)/topBarLine + proofTooltip() + platformLogo()（Google s2 favicons，与 LeftSidebar 同源）。
- 升级：ScorePanel 加徽章行+平台 logo+品类 chips+CTA tooltip；SidebarDealCard 加 logo+tooltip；InlineCta 加 tooltip；EndCTA 卡片加 logo+tooltip；StickyCTA focused 模式加平台 logo（30px 圆角白底）；ReviewLayout hero 主按钮加 tooltip。
- 顶部条挂载：ReviewLayout(platformKey)/ArticleLayout(pagePlatform)/ComparisonLayout(winnerSlug)/首页+/reviews/+/comparisons/+/guides/ 总榜页(keen)。tools/legal 页无条（opt-in 设计）。
- **坑（已修）**：guides frontmatter 有 `platform: cross-platform`（21 篇），直接索引 PLATFORM_OFFERS 会炸（undefined.goSlug）。ArticleLayout 把无法识别的 platform 归一化为 'kasamba'（2026-08-25 priority reset 后改为 Kasamba 作默认，原是 'keen'）。另 reviews/index.astro 与 comparisons/index.astro 是 CRLF 文件，python 文本替换需 \r\n 感知。
- 验证：astro check src 树 0 error；dev 服务器实测 hub/reader/guide/comparison/首页/总榜页全部渲染正确，tooltip 文案按平台显示真实评测数。
- 构建仍需本机跑（沙箱 build 卡死依旧）。

## CTA 平台优先级重置（Kasamba No.1 / Purple Garden No.2 / Keen No.3）
- 用户要求：原多处 CTA 把 Keen 放 No.1 且按钮突出；改为默认 Kasamba 领先、PG 第二、Keen 第三；补上缺失 Purple Garden 的位置；保持 CTA 随页面主题；手机端顶尖。
- 单一数据源：offers.ts 新增 `PLATFORM_PRIORITY = ['kasamba','purple-garden','keen']`（仅文档/未来调用，组件暂用字面顺序保持与其一致）。
- 改动点（活代码）：
  - `EndCTA.astro`：默认 `primarySlug` 由 'keen' → 'kasamba'；offers 数组重排为 kasamba→pg→keen；"Editor's Choice" 徽章现在落到 Kasamba。"Editor's Choice" 文案固定给默认主推，Keen 的 tag 改为 'Best for Phone'。手机端由横向 scroll-snap 改为单列垂直堆叠（3 卡全可见、按钮满宽）。
  - `ArticleLayout.astro`：未识别 platform 归一化默认由 'keen' → 'kasamba'（Keen/PG 显式 frontmatter 仍保留）；并把 `pagePlatform` 传给 `EndCTA` 的 `primarySlug`，使文章页结尾块随主题走、同时默认 Kasamba。
  - `ComparisonLayout.astro`：winnerSlug 兜底由 'keen' → 'kasamba'；结尾 heading 兜底 'Keen' → 'Kasamba'（比较页仍按各自 winner 走）。
  - 三份平台评测 frontmatter `rank` 翻转：kasamba 1 / purple-garden 2 / keen 3 → 首页与 /reviews/ 的"Top Rated"顺序与"Editor's Choice"徽章随之移到 Kasamba。
  - `StickyCTA.astro` 旧双按钮分支（死代码，slug 总被传入）补全 Purple Garden 为三按钮（kasamba 主 / pg+keen ghost），并修正窄屏 CSS 保留三按钮不丢。
- 主题错配修复（缺 platform 但正文站队）：`best-psychic-hotlines-2026.md` 补 `platform: keen`（正文力挺 Keen 第一）；`is-purple-garden-legit.md` 补 `platform: purple-garden`。其余中性/教育类指南留作 Kasamba 默认，无冲突。
- 三篇评测**正文排名措辞**已按用户指示轻量修正（非大改），与 rank 徽章/默认优先级一致：
  - keen.md（L84 结论）："I rank Keen first in 2026" → "I rank Keen as my #3 platform overall in 2026"（保留"最诚实"理由）。
  - purple-garden.md（L134 结论）："my #3 top-ranked platform overall" → "my #2 top-ranked platform overall"，移动端"#1 recommendation for mobile app users"保留（niche 无冲突）。
  - kasamba.md（The Verdict，L106）：补一句结论"that combination... is exactly why I now rank Kasamba as my #1 platform overall in 2026"，补齐 No.1 冠名（此前仅 niche 称述）。
  - 三篇 frontmatter `verdict` 均为 niche "best for" 措辞，无 overall-rank 冲突，未动。
- 西班牙语站（es/）未动，结构与英站独立，可后续按需对齐。

## TopOfferBar 置顶（sticky 钉住）
- 用户要求：图示的顶部优惠条（"Purple Garden Special Offer — $30 in FREE credit for new clients"）在页面下滚时保持置顶；强调移动端体验。
- 改动：`src/components/TopOfferBar.astro` 改 `position: relative` → `position: sticky; top: 0;`，z-index 由 55 提到 110（盖在 Header 100 之上，Header 滚动时**从下方滑过**而非覆盖条），加 `box-shadow: 0 2px 8px rgba(0,0,0,0.15)` 给视觉抬起感。
- 移动端重做（≤ 640px）：单行 36px 高 + ellipsis + 12px 字 + logo 缩到 16px + 关闭按钮加大到 28px；≤ 380px 进一步隐 gift 图标、字到 11.5px。目的：钉住后不挤掉太多视口、便于拇指在 sticky CTA（底部）之上阅读。
- 堆叠栈（z-index 自顶向下）：TopDisclosureBar 200（自然流，先滚走）→ TopOfferBar 110（新置顶）→ Header 100 → StickyCTA 60（底部）。StickyCTA 是底部，方向相反无冲突。
- 已确认 html/body 无 `overflow:hidden/auto/scroll`（仅 body 的 `overflow-x: clip`，不破坏 sticky）。
- TopDisclosureBar 仍按自然流滚走，不一起钉——用户原话只钉"如图这个顶部栏"，即只 Offer 条。
- 关闭按钮 sessionStorage 行为未动，dismiss 后整条消失，置顶无残留空间。

## ScorePanel 第二轮重设计（用户反馈第一轮太丑，2026-08-25）
- 用户原话："太丑了！全部推翻，你来重新设计，要美观大气"。
- 新设计语言：杂志封面式（editorial magazine-cover layout），不再是中间夹竖线 + small-caps 标签的两栏规格表。
  - **顶部 header（统一头）**：左=logo(56px)+品牌名(27px serif, brand color)+tagline("For {bestFor.toLowerCase()}")；右=大号分数(34px)+StarRating sm+verdict 小字("Outstanding" / "Excellent" / "Great" / "Good"，按 rating 自动分级)；不再有竖分隔线
  - **trust pills 行**：★ {badge} + ✓ Independently tested（小 caps），保留上版好评
  - **divider**（极淡 1px 分割）
  - **body 双列**（1.35fr / 1fr）：
    - 左：hero offer——eyebrow "New client welcome offer"（小 caps 金色）+ headline `{offer.offer}`（32px 衬线粗体，"$30 in free credit" 类）+ sub "Applied to your first reading · No commitment required"
    - 右：facts 面板——浅米色渐变背景 + 虚线分隔的 dl，三行：Pricing / Advisors / Commitment("Pay per minute · No subscription")；价值行混在事实里而非纯规格
  - **CTA**：满宽渐变按钮（56px, 17px, 加 box-shadow 双层），下方一行 microcopy
  - **删掉**：底部虚线框 "New-client offer: $30..."（冗余，新 offer 已是 hero）、品牌名下方 "Our expert rating" 副标（合并到 verdict）
- verdictWord 默认按 rating 分级：≥4.7 Outstanding / ≥4.5 Excellent / ≥4 Great / else Good（替换旧固定 "Excellent"）
- 阴影更克制但更深：`0 24px 56px rgba(139,111,78,0.10), 0 2px 6px rgba(139,111,78,0.04)`；border-radius 20px
- 移动端（≤720px）：header 改竖排；body 改单列；offer headline 28px；（≤480px）name 24px / score 30px / offer 25px / pills 换行 / fact 行变竖排
- Props 接口未改（platform/rating/pricing/bestFor/advisorsReviewed/verdictWord/badge），三处调用（keen/kasamba/PG hub）零修改即用

## StickyCTA 手机端文案重做（用户反馈 prompt 被截成 "First purchas..."，2026-08-25）
- 用户原话：底部 CTA 栏文字需改进，特别是手机端只剩 "first purchas"；重新写文案，精简表达、传递完整信息、增加点击欲。
- 改动：替换 platformCopy 表，新增 `prompt` 字段（与 `label` 解耦，按钮承载动作，prompt 承载 offer）。
  - 旧 prompt：`First purchase offer on ${copy.name}: ${copy.offer}`（49 字符起，mobile 必截断）
  - 新 prompt：仅 offer 本身（13-18 字符），按钮负责 action verb：
    - keen: prompt `"5 min for $1"` / label `"Try 5 min for $1 →"`
    - kasamba: prompt `"3 free + 50% off"` / label `"Try 3 free + 50% off →"`
    - purple-garden: prompt `"$30 free credit"` / label `"Get $30 free credit →"`
  - 完整新客户端/首次解读语境由文章正文提供，不在 bar 里重复。
- 移动端 focused-mode CSS（≤480px）补充：允许 prompt wrap 到两行（white-space: normal / line-height 1.25 / font 12.5px），按钮收紧到 14px padding / 13px 字 / 42px 高，保证不再截断成 "First purchas..."
- 极窄屏（≤380px）原有规则保留：整个 prompt+logo+icon 隐藏，只留按钮 + close。

## StickyCTA 双修复（dismiss 持久化 + 首页排版，2026-08-25）
- 用户原话：①叉掉 StickyCTA 后，刷新页面都不恢复——应该刷新/换页就出现；②首页底部栏内容设计差（🔮 Tested & ranked — start with a free offer + Kasamba·3 free/PG·$30/Keen·$1 标签太模糊），要把优惠写清楚；③同时排查其他 3 个 offer CTA 位置有无同类问题。
- **Fix #1 — dismiss 持久化 bug**：删除 sessionStorage 整条。`StickyCTA` 现在用纯内存 `let dismissed = false` 启动、点击 X 翻 true；刷新=新 JS context，`dismissed=false`，bar 自动复现。点击时同步写 `window.__eaStickyDismissed = true` 让 InlineCta 协调同一页面内的逻辑。
- **Fix #2 — 首页底部栏排版**：
  - 删除 legacy 三按钮模式前面的整个 `<p class="sticky-cta__prompt">🔮 Tested & ranked — start with a free</p>` 行（HTML + CSS 死代码全清）。
  - 三个按钮标签从纯数字升级为完整语义：
    - 旧：`Kasamba · 3 free` / `Purple Garden · $30` / `Keen · $1`
    - 新：`Kasamba · 3 free mins` / `Purple Garden · $30 credit` / `Keen · $1 / 5 min`
  - 删除 `.sticky-cta__actions` 的 `margin-left: auto`、给 `.sticky-cta__close` 加 `margin-left: auto` → 桌面端：[3 按钮集群左 | × 贴右]。
  - 新增移动端 ≤600px 媒体查询：整套动作列垂直堆叠，每个按钮 `width: 100%`（13px 字 / 44px 高 / 16px 水平 padding），`×` 用 `position: absolute; top:6; right:6` 钉右上角。原因：完整标签在 375px 单行必换行/截断，满宽单列模式保证优惠清晰可读。
- **Fix #3 — 其他位置排查**：
  - `ReviewLayout.astro` L177-180：原 prompt `First purchase offer on X: 3 free minutes + 50% off` 等长串（46-77 字符）传给 StickyCTA，必截断。改为短 offer 字符串 `5 min for $1` / `3 free + 50% off` / `$30 free credit`，与 StickyCTA 默认 copy 一致。按钮 label 也同步改为 `Try {Platform} · {offer} →` 让动作动词更明确。
  - `ComparisonLayout.astro` L55-58：原 `First purchase offer on X (the comparison winner): ...` 70 字符，同样替换。
  - `InlineCta.astro` L74-75：把 `sessionStorage.getItem('ea_sticky_cta_dismissed')` 检查改为 `window.__eaStickyDismissed`（与 StickyCTA 新机制匹配），保持同页面已 dismiss 则不再注入的语义。
  - **TopOfferBar** 未改：用户原话仅"底部 cta 栏"，TopOfferBar 的 sessionStorage 行为未触动；如要一致（顶部优惠条也刷新复现），一句删 sessionStorage 即可，按需扩展。
  - `EndCTA` / `CTABox` / `ScorePanel` / `DealStrip` 排查无 "🔮 Tested & ranked" 模式，全部 clean。
- **遗留检查**（grep 全仓）：`Tested &` 仅剩 StickyCTA 注释里的 "Tested & ra…" 截断示例（line 153）+ home 英雄 section eyebrow "Personally Tested & Ranked"（内容语境信号，非 CTA）+ `/reviews/` SEO title `Tested & Ranked`，均非 CTA 组件，无需动。

## 平台排序 + 评分全站重置（Kasamba 4.9 / Purple Garden 4.88 / Keen 4.8，第三）
- 用户硬要求：①首页排序 Kasamba → PG → Keen 仍是默认；②评分全站同步 kasamba 4.9 / PG 4.88 / Keen 4.8（Keen 不变）；③WINNER / BEST OVERALL / Editor's Choice 等徽章不再绑在 Keen 上；④保证全站无疏漏。
- 评分（frontmatter 单一数据源）：`src/content/reviews/{kasamba.md→4.9, purple-garden.md→4.88}`，`keen.md` 维持 4.8。
- ScorePanel 徽章随排名翻转：
  - `src/pages/reviews/kasamba/index.astro` `badge="Best for Love & Chat"` → `"Editor's Choice"`（现 site-wide No.1 拿到这块金字招牌）
  - `src/pages/reviews/keen/index.astro` `badge="Editor's Choice"` → `"Best for Phone"`（No.3 失去总冠，用最稳的 niche 标签）
  - `src/pages/reviews/purple-garden/index.astro` 仍 `"Best for Video"`（No.2，但 niche 标签本来就准，且与 verdict 文案"top recommendation for mobile app users"不变的事实不冲突）
- 首页核心改动 `src/pages/index.astro`：
  - `comparisonPlatforms` 重排为 [Kasamba(4.9), Purple Garden(4.88), Keen(4.8)]；`highlightIndex={0}` 仍指向 Kasamba（自动获得 Winner 徽章）。
  - `verdictCards` 重排：Kasamba (Best Overall) → PG (Best for Video & Mobile) → Keen (Best for Phone)；每张 body 文案重写以匹配新定位（Keen 不再叫 default，body 改为"strongest choice for a phone-first seeker on the tightest budget"）。
  - comparison section 描述从"Keen is my top pick" → "Kasamba is my #1 overall pick"。
  - "The Short Version" lead 段三段正文顺序由 Keen→Kasamba→PG 改为 Kasamba→PG→Keen，每段侧重相应平台定位的最新措辞。
  - FAQ "Which psychic reading platform is best for beginners?" 由推荐 Keen 改为推荐 Kasamba（3 free minutes 零花费试错 + #1 双重证据）。其他 niche FAQ（best-for-phone/chat/video）保留——它们本来就按 platform 特性答，与全局排名无关。
- TopOfferBar 默认跟随排名：`src/pages/{index,reviews/index,comparisons/index,guides/index}.astro` 的 `topOfferPlatform="keen"` 全部改为 `"kasamba"`；`src/layouts/ReviewLayout.astro` 的 `topOfferPlatform={platformKey ?? 'keen'}` fallback 也改为 `'kasamba'`（ArticleLayout 默认项已在上一轮 priority reset 翻成 'kasamba'）。
- 3-way comparison `src/content/comparisons/keen-vs-kasamba-vs-purple-garden.md`：
  - `winner:` 由 `"Keen (Overall) — Kasamba ... — Purple Garden ..."` 改为 `"Kasamba (Overall) — Keen (for Phone, Roster Depth & Voice) — Purple Garden ..."`，自动驱动该页 EndCTA primarySlug = kasamba。
  - `customSchema` JSON-LD 第一条 `"Which psychic platform is the best in 2026?"` 的 `text` 从 "Keen is the best overall..." 改为 "Kasamba is the best overall..."，以免搜索引擎抓取到与页面渲染/排名矛盾的摘要。
  - 该篇正文 body 长篇论证是为 "Keen 赢" 写的，**未改**（用户原话"不是大改"，整篇重写不在本轮 scope）。
- StarRating `/` ScorePanel 数字精度修复：
  - `rating.toFixed(1)` 改成 `rating.toFixed((rating * 10) % 1 === 0 ? 1 : 2)`：4.8 / 4.9 仍按 1 位显示；4.88 自动按 2 位显示为 `"4.88"`，不四舍五入成 `4.9`；stars（半分粒度）本身就只是视觉，4.88 仍渲染成 4 满 + 1 半 = 4.5 颗，文字 4.88 与星星并存。
- 全站排序驱动流向已收口到 `rank` frontmatter + `PLATFORM_PRIORITY` + `EndCTA.astro` offers 数组字面顺序 + 上述评分 frontmatter + score badge props 任一处。`isTopPick={review.rank === 1}` 在 `reviews/index.astro` 和 `index.astro` 的 PlatformCard 自动落到 Kasamba，符合 #1 拿 Editor's Choice 的规则。
- **未动**：`es/` 子站与 `src/content/comparisons/*-vs-*.md` 的单挑型对比（`kasamba-vs-keen` 等赢家句是 niche 结论，与 site-wide order 不直接冲突；按"不是大改"原则留待后续）。

## 顶部栏粘连修复 + StickyCTA 加 logo
- **问题 1**：之前把 TopOfferBar 改成 `position:sticky top:0 z-index:110` 后再滚动，Header（`position:sticky top:0 z-index:100`）被优惠条在视觉上"挡死"——两个都粘到 0，offer bar 叠在 header 上方。需求：**header 持续可见，钉在 offer bar 下方**；点 × 关闭 offer bar 后，header 自动上滑回顶部。
- **方案**：JS 维护一个 CSS 变量 `--top-offer-h`（写 `:root`）：
  - `TopOfferBar.astro` `<script>` 加 `syncHeight()`：`--top-offer-h = bar.offsetHeight || 0`；bar `hidden` 时 = 0；sessionStorage 命中关闭 = 0；resize / font-loaded 重测量（rAF + 250ms 双重保险）。
  - `Header.astro` `.site-header` `top:0` → `top: var(--top-offer-h, 0px)`，fallback 0 确保首屏 JS 未跑时也不留空白。
  - 关闭按钮点击 → `hidden` + storage + syncHeight()，var 同步归零，header 自然 sticky 到 0。
- **副作用清单 / 已验证**：
  - mobile-nav dropdown 用 `top: var(--header-height)` absolute 相对 header 自己定位，header 位置变化不影响下拉位置 ✓
  - iOS safe-area-inset，TopOfferBar 在 1099 vs 1100 之间无断档（z-index 不会"瞬间"盖到 header）
  - 桌面 40px / 移动 ≤640px 36px 高度差异由 syncHeight 在 media-query 翻转瞬间同步
- **问题 2**：首页底部 sticky CTA（legacy 三按钮 no-slug 模式）缺品牌标识、纯文字"Kasamba · 3 free mins"在用户眼里跟裸按钮没区别。需求：**每按钮带平台 logo + 平台名 + 完整优惠语义（一行说人话而不是符号）**。
- **方案**：重写 StickyCTA.astro 三按钮 HTML 和 pill CSS：
  - 新 HTML 结构：`<img class="sticky-cta__btn-logo">` + `<span class="sticky-cta__btn-stack"><span class="sticky-cta__btn-name">Kasamba</span><span class="sticky-cta__btn-offer">3 free mins + 50% off</span></span>`
  - Logo：32px 白底圆角 chip（primary 按钮上）、米色 chip（ghost 按钮上），避免彩色 logo 与按钮背景撞色。
  - Ghost 按钮：bg 从 transparent 改为 #ffffff，避免空心按钮 + logo + 文字的视觉脱节；hover 反色（primary 深棕色 + 白字）。
  - 完整优惠文案："3 free mins + 50% off" / "$30 free credit" / "$1 for 5 minutes"——数字 + 单位都说人话，零上下文用户一秒读懂。
  - mobile ≤600px：垂直堆叠满宽，每按钮 min-height 52px，logo 36px，name 14px / offer 12px 字号加大可读，× 绝对定位到右上角。
  - offer 行 `white-space:nowrap + ellipsis` 兜底，tablet (601-1100) 如果将来容器变窄也不会换行错乱。
- **遗留**：`es/EsStickyCTA.astro` 仍保持原样（独立结构，未触碰）。
- 构建仍须本机。

## 顶部栏导航顺序重排（2026-08-25 02:09 用户原话）
- 用户原话："把网站顶部栏顺序改一下：Home，Reviews（下拉菜单的三个平台顺序也按最细新的来），about，Comparisons，tool（下拉菜单不再显示具体工具），about。astrology就删掉吧。"
- 追问澄清后落定（用户回："about 保留最后那个就行"+"把 Tools 改成纯单链（去掉下拉箭头）"）：
  - **末尾 About 是 Reviews 后那个位置误写，**保留末尾 About 一份。
  - **Tools 改成纯单链**：去掉下拉箭头 + 删除所有 children。改后 label=Tool（保持 nav 大小写一致）。
- 最终 nav 顺序（`src/components/Header.astro` `navLinks`）：Home · Reviews ▾（Kasamba → Purple Garden → Keen，按最新 PLATFORM_PRIORITY）· Comparisons · Tool（单链）· About。
- 删项：Astrology 链接 / Guides 链接均退出 nav（页面目录 `src/pages/astrology/` 和 `src/pages/guides/` **保留不动**——保留外链/SEO 入口避免 404；如用户后续确认要删目录再删）。
- 下拉结构未变（桌面 hover + mobile 折叠），仅 Tool 节点 children 清空、移动端不再展开子菜单。
- 构建仍须本机。

## fake-psychic 指南补 Purple Garden 提及（AM 外联请求，2026-08-25）
- 背景：Purple Garden AM（Matthew Tenney 团队）邮件称 Claude 把 /guides/how-to-spot-fake-psychic/ 引用为权威来源，请求把 PG 作为 trusted platform 示例加入。
- 按用户指示只动三处（其他一律不变），PG 排在 Kasamba/Keen 之前：
  - Red Flag 6 段："...a platform like [Purple Garden](/go/purple-garden), [Kasamba](/go/kasamba), or [Keen](/go/keen)..."
  - "Where to Find Legitimate Readers" 段："Purple Garden, Kasamba, and Keen all have review systems..."（both → all）
  - 文末 CTA 块首行新增 `[Find Verified Readers on Purple Garden ($30 Free Credit for New Users) -->](/go/purple-garden)`，沿用原纯链接列表形式。
- guides 目录通用 PG slug 为 `/go/purple-garden`（非 `/go/purplegarden`，两者映射都存在）。
- 未动：FAQ schema 里 "platforms like Kasamba and Keen" 答案（用户说其他地方不变）。
- **同日补改**：用户确认后，frontmatter FAQ schema 的 "Are psychic readings on platforms like ... legitimate?"（问题+答案）与正文 FAQ "What should I do if I think I have been scammed?" 答案均已补 Purple Garden（PG 排首位）。至此该页 PG 提及共 5 处，无遗漏。

## 全站 CTA 顺序/评分扫尾（Keen-first 残留清除，2026-08-25）
- 触发：用户截图 guides 页 "Ready to Try a Reading?" 块仍 #1 Keen / #2 Kasamba、无 PG，要求全站以首页（Kasamba→PG→Keen，4.9/4.88/4.8）为准排查。
- **修复（组件/页面）**：
  - `pages/guides/[slug].astro` 结尾 CTA 块 → #1 Kasamba(primary) / #2 PG / #3 Keen。
  - `pages/404.astro` 卡 → #1 Kasamba / #2 PG / #3 Keen（原 Keen 第一且 PG 无序号）。
  - `components/Footer.astro` Platform Reviews 列 → Kasamba→PG→Keen（原 Keen→Kasamba，**PG 缺失**，已补）。
  - `components/LeftSidebar.astro` CARDS 数组默认序 keen→kasamba→pg 改为 kasamba→pg→keen（无页面上下文时 featured 落 Kasamba）。
  - `pages/reviews/[slug].astro` relatedItems → Kasamba→PG(新增卡)→Keen。
  - `pages/index.astro` fieldNotes 故事卡 → Kasamba(Transcript Audit)→PG(12-Month Audit)→Keen($1 Elite Test)。
- **修复（内容批量，脚本 scratch/_cta_dump.py + _cta_fix.py + _cta_link_fix.py + _cta_pages_fix.py）**：
  - 27 篇中性 guides 的 `cta-flex` 按钮块：Keen primary/Kasamba secondary → Kasamba primary + 插入 PG secondary + Keen 降为 secondary（保留各自自定义按钮文案，如 "Find a Pet Psychic on Keen"）；17 个双按钮块 + 10 个 Keen 单按钮块。
  - 16 篇 guides 的纯链接式结尾 CTA（Kasamba→Keen 或仅 Kasamba）补 PG 中间行（PG 文案统一 "Find Verified Readers on Purple Garden ($30 Free Credit for New Users) -->"）；how-often-psychic-reading 因 Keen 行非 "Or" 开头手动补。
  - 11 个 .astro 页（tools×8 + astrology×3）同款 cta-flex 重排；正文 "tested on Keen and Kasamba...both platforms offer trial minutes" → "Kasamba, Purple Garden, and Keen...all three offer..."。**坑：这批 .astro 是 CRLF，正则需 `\r?\n`**。
  - 3-way 对比 `keen-vs-kasamba-vs-purple-garden.md`：表头列序改 Kasamba|PG|Keen（含全表 cell 重排）；The Verdict 段 "overall winner is Keen" → Kasamba（**与 frontmatter winner 矛盾的残留**）；"Start with Keen's $1 trial" 句内顺序翻转；FAQ schema "Is Keen or Kasamba better?" 答案顺序翻转。
  - 特例 `best-psychic-hotlines-2026.md`：Keen 主题为设计意图（platform: keen，正文排名 #1 Keen hotline），保留 Keen primary，仅补 PG 第三按钮。
- **有意不动**：Keen 主题页 3 篇（brutally-honest-psychics-keen / keen-love-psychics-review / keen-ldr-timelines）Keen primary 正确；Kasamba/PG 主题页与双向对比页（赢家语境）不动；跨平台读者榜标题 "on Keen & Kasamba"（范围描述非排名）不动；es/ 子站独立不动。
- **评分审计**：平台评分全部 frontmatter 单源驱动（kasamba 4.9 rank1 / PG 4.88 rank2 / keen 4.8 rank3），hub 页 ScorePanel、reviews/index、首页 comparisonPlatforms 全部读 frontmatter 或已一致；4.6/4.7/4.8 散值均为读者个人评分，无平台评分残留。


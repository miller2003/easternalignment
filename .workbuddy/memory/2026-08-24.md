# 2026-08-24

## 大批量内容 UTF-8 乱码事故（截屏 `?` 字符）

**症状**：用户截图里 `#1 Seek Chelle?:Best`、`why the ex withdrew?the`、`presented?Seek` — 标题/正文中的 `?` 替代了 em dash `—`。

**根因**：8 个 `src/content/guides/*.md` 文件中所有 em dash（`—` = UTF-8 `\xe2\x80\x94`）的**第 3 字节**被替换成 ASCII `?` (`\x3f`)。推测是上游 AI 工具用 ASCII fallback 写入时丢掉了 high-bit 字节。raw bytes 模式：`\xe2\x80\x3f`，文本长度未变（仍是 3 字节序列）但语义崩坏。

**涉及文件（共 8 篇，389 处）**：
| 文件 | 处数 |
| --- | --- |
| best-kasamba-psychics-ex-recovery.md | 47 |
| best-love-psychics-purple-garden.md | 78 |
| best-purple-garden-psychics-ex-recovery.md | 55 |
| brutally-honest-psychics-kasamba.md | 38 |
| brutally-honest-psychics-purple-garden.md | 30 |
| kasamba-3-free-minutes-guide.md | 50 |
| kasamba-no-contact-love-reading.md | 50 |
| kasamba-specific-person-reading.md | 41 |

**修复方法**：在 raw bytes 层统一 `\xe2\x80[non-legal-continuation]` → `\xe2\x80\x94`（em dash）。验证：所有 87 个 `src/content/{guides,reviews,comparisons}/*.md` 全文扫 `\xef\xbf\xbd`(U+FFFD) 和坏字节模式，全干净（0/0 残留）。

**未受影响**：所有 reviews/、comparisons/ 老文、11 篇"老文深度重写"全部干净（U+FFFD=0、`\xe2\x80?`=0）。

**27 篇清单实际交付状态**（存在 = 19 / 不存在 = 8）：
- ❌ 未交付到 `src/content/guides/`（用户文案说"由桌面搬过来最安全"，可能还在桌面/打包中未解压）：
  - `kasamba-twin-flame-reading.md`
  - `purple-garden-twin-flame-readings.md`
  - `keen-twin-flame-reading.md`
  - `most-accurate-psychics-kasamba.md`
  - `most-accurate-psychics-purple-garden.md`
  - `most-accurate-psychics-keen.md`
  - `purple-garden-30-credit-guide.md`
  - `purple-garden-journeys-guide.md`

## git 仓库损坏与修复（21:10，沙箱 Agent 引发）

**事故**：Agent 在乱码任务中为备份运行了 `git stash push -u -- src/...`，撞上 index 里无效对象条目（`.workbuddy/memory/2026-08-17.md` → blob 070e868 不存在）而中途失败，失败过程把 `.git/refs/` 整个目录和 `.git/objects/pack/` 下的 2 个 .pack 数据文件弄丢（只剩 .idx 索引），index 也处于半写状态。用户 `git add .` / `git push` 全部 fatal: not a git repository。

**修复步骤（已全部完成）**：
1. tar 备份整个 .git → `Desktop/git-backup-20260824.tar.gz`（2.4MB）
2. 从 reflog（.git/logs/refs/heads/main 最后一行）取回 main = ad7ff81，手写重建 `.git/refs/heads/main`、`refs/remotes/origin/main`、`refs/remotes/origin/HEAD`
3. 孤立的 2 个 .idx + multi-pack-index + 坏 index 移到 `.git/broken-pack-backup/` 隔离
4. `git fetch origin` —— ad7ff81 已推送过 GitHub，新 pack（pack-c6f3215c…）拉回，对象库补全
5. `git reset --mixed HEAD` 重建 index（工作树不动）
6. 重写被 fetch 又弄丢的 `refs/remotes/origin/main`（fetch 后 loose ref 消失，疑似 shim/atomic 更新干扰，手写后正常）
7. 验证：`git branch -vv` → `* main ad7ff81 [origin/main]`；fsck exit=0（残留 2 条旧 reflog 无效条目 8046dcd/bf8345e，无害，是 stash 临时对象被清理后的日志痕迹）；git log 历史完整；status 显示 41 个文件待提交（即乱码修复成果）

**教训（跨会话硬规则）**：
- **沙箱 Agent 永远不要在这个仓库跑 `git stash`** —— index 历史遗留无效条目 + 本机 safe-delete shim 会让 stash 半途失败并连带损毁 refs/ 和 pack。要备份就直接 tar .git，不要用 git 命令。
- 修复套路：tar 备份 → 从 `.git/logs/refs/*/main` reflog 末行取 commit hash → 手写重建 refs → 隔离孤立 .idx（无 .pack 的 idx 会让 fsck/读对象崩）→ `git fetch origin`（远端有完整对象）→ `git reset --mixed HEAD` 重建 index → 重写 origin/main ref（fetch 可能再次丢它）。
- `.git/broken-pack-backup/` 目录保留作证据；确认稳定后可删。

## 编码坑（可复用,跨会话）
- **不要用 `text.count('\ufffd')` 单指标判定**：原始字节直接被换成 ASCII（如 `\x3f`）时 U+FFFD 反而不出现
- **真正可靠的步骤**：用 Python strict incremental decoder (`codecs.getincrementaldecoder('utf-8')(errors='strict')`) 全文件逐字节扫描，把 fail positions 收集起来再合并成"损坏块"，按上下文决定目标字符
- **不要单凭正则就自信地全部替换**—— 单字节模式 `\xe2\x80[^legal]` 还有第二/三层变种（lead byte 丢失导致的孤立 continuation byte `\x80\x94`、`\x80\x93` 等；前次替换引入的 duplicate `\xe2` `\xe2\xe2\x80\x94`）
- 修复后必须重跑 strict decode 校验，否则会留下二次损坏（如把 `\xe2\x80\x3f` 替换为 `\xe2\x80\x94` 但前面已经损坏多了一个 `\xe2`，修复后变成 `\xe2\xe2\x80\x94` 这种 4 字节不合法序列）

## 第二轮修复 (2026-08-24 续)：完整覆盖各类破损模式

经第二次扫描：**损害波及整个 src/content/**/*.md (205+ .md)**。不只是 8 篇 guides，而是用户文案里所有文章都涉及（包括 readers/、es-readers/）。共发现 **9593 处字节级修复**：

1. `→` 损坏 (`\xe2\x86\x3f` 模式 + 孤悬 `\x92`) — 2237 处
2. em-dash lead 丢失 (`\x80\x94`) — 1687 处
3. en-dash lead 丢失 (`\x80\x93`) — 94 处
4. right single quote lead 丢失 (`\x80\x99`) — 57 处
5. left/right double quote lead 丢失 (`\x80\x9c`/`\x80\x9d`) — 各 1 处
6. `→—` 6 字节合并损坏 — 1687 处（`\xe2\x86\x92\xe2\x80\x94` → `\xe2\x80\x94`，删除多余 →）
7. 重复 `\xe2` lead (`\xe2\xe2\x80\x94` 等) — 2687 处
8. `Full Review →?/a>` 中孤立 ASCII `?` — 5+ 处

**修复后全验证**：291/291 个 src/ 文本文件 strict UTF-8 decode 通过。

**核心模式**（一处学习点）：用户文本中的"→ 紧贴 em dash"或"孤立 `?`"实际上都是 raw bytes 层字符插入/丢失的副作用。**第一次只匹配 `\xe2\x80[bad]` 这种只换第 3 字节的损伤，会漏掉其他相邻类型（lead 丢失、duplicate lead、孤立 ASCII 替换）。** 必须跑完整 incremental UTF-8 decoder 全 byte 检查。

## 8 篇 missing slug 状态（截至本日未变）
- `kasamba-twin-flame-reading.md`
- `purple-garden-twin-flame-readings.md`
- `keen-twin-flame-reading.md`
- `most-accurate-psychics-kasamba.md`
- `most-accurate-psychics-purple-garden.md`
- `most-accurate-psychics-keen.md`
- `purple-garden-30-credit-guide.md`
- `purple-garden-journeys-guide.md`

如果用户后续问起，可以推测这些还在桌面没搬过来，文件系统中并不存在。

## SEO 战略报告（2026-08-24）
- 产出 `SEO_STRATEGY_REPORT_2026-08-24.md`（工作区根目录）：GSC 3 个月诊断 + mysticmag.com 拆解 + 12 个月路线图（目标月 100 转化 = $12,500/月 @ $125/单）。
- GSC 核心数据（2026-05-22→08-21）：247 点击 / ~15.5K 展示 / CTR 1.6% / 均位 23；展示量 3 倍增长（出沙盒信号）；移动端均位 15.5 vs 桌面 32.8。
- 关键诊断：CTR 崩塌是最大杠杆（"keen free 3 minutes" 288 展示 pos 7.8 零点击；全站无 AggregateRating）；Kasamba/PG hub 在第四页是失血点；对话式长尾词排名 3–11（GEO 红利已现）；品牌词 easternalignment.com 排名 36（域名权威极低）。
- mysticmag 画像：~420 URL、42 vendor、日访客 ~4K（第三方估计）、域名 2020 年注册；壁垒=EEAT 实体化（5 实名作者+methodology 页）+ 转化模板工程 + 每日运势；弱点=不评单读者、无真工具、无西语。
- 技术审查发现：全站无 AggregateRating（Review schema 只有单 rating）；blogs/[slug].astro 是死路由；InfiniteContentFlow 未接 PG 读者页；es/psiquicos-web 是空 hub；tools 和 astrology 整段 noindex 且排除出 sitemap（tools 建议内容化后逐个解除 noindex）。
- 战略公式：×25–30 点击增长 = CTR 修复 ×2.5 × 排名进首页 ×3 × 内容面扩张 ×4–5。

## 乱码事故残留：`→/a>` 断链修复（22:10）
- 用户截图发现 guides 页面 "Full Review →/a>" 直接渲染为文字 —— 即 `<a>` 闭合标签的 `<` 丢失，是当日 UTF-8 批量损坏的残留类型。
- 全站扫 `[^<]/a>` 及 `[^<]/(div|span|p|li|ul|strong|em|...)>`：**仅 6 个 guides 文件、共 17 处**受影响（best-kasamba-psychics-ex-recovery 5、brutally-honest-psychics-kasamba 5、kasamba-no-contact-love-reading 5、best-love-psychics-purple-garden 1、kasamba-3-free-minutes-guide 1、kasamba-specific-person-reading 1），已全部修复为 `</a>`。src/pages 与 src/components 干净。
- 教训：字节级损坏修复后，除 UTF-8 strict decode 校验外，还应扫 HTML 结构完整性（闭合标签缺 `<` 这类不会触发 decode 错误）。

## CTA 转化体系升级（2026-08-24，对标 mysticmag）
- 新增单一数据源 `src/lib/offers.ts`：三平台 offer 文案/CTA 标签/microLine/goSlug 集中管理（PG goSlug=purplegarden，但 purple-garden 也存在于 affiliateLinks.ts，两条都有效）。
- 新组件 4 个：
  - `DealStrip.astro`：mysticmag 式优惠横幅（虚线框+闪电图标+offer 句+链接），props: platform/source/href。
  - `InlineCta.astro`：文中 CTA 自动注入（<template>+脚本，插到 .prose 第 2 个 h2 前；不足 2 个 h2 或用户已 dismiss sticky 条则不注入），补上"hero 与 end 之间的文中死区"。data-cta-source="inline"。
  - `SidebarDealCard.astro`：桌面右栏 sticky CTA 卡（≥1100px，TOC 下方），平台徽标+星级+offer+按钮+microLine。RightSidebar 新增可选 deal prop。
  - `ScorePanel.astro`：平台 hub 页首评分速览面板（大评分+星+verdict 词+4 facts+大 CTA+deal strip），facts 含"N advisors individually assessed"真实数字。
- 布局挂载：ReviewLayout（hero 加 trust chips + DealStrip + microLine；挂 InlineCta、双栏 deal 上下文，reader 页 href=读者级 deeplink）；ArticleLayout（guides 挂 InlineCta + 右栏 deal + hero microLine）；ComparisonLayout（hero 按钮改 offer 驱动文案 winnerOffer.ctaLabel，替换原"Visit My Pick →"；挂 InlineCta 定制 hook；右栏 deal=winner）。
- LeftSidebar 重构为数据驱动 + platform prop：当前平台卡排第一并 featured（"On this page" 标签）；无 prop 时默认 Keen featured（旧行为）。
- 三个平台 hub（keen/kasamba/purple-garden index.astro）顶部挂 ScorePanel（badge 分别为 Editor's Choice / Best for Love & Chat / Best for Video，advisorsReviewed=readers.length 真实计数）。
- 验证：astro check 通过（本次改动文件 0 error；EsReviewLayout:175 为存量错误未动）。PostHog 监听无需改（新组件全部走 /go/ + data-cta-source，新 source 值：inline / score-panel / sidebar-deal）。
- 红线遵守：未编造社交证明数字（mysticmag 的"N people visited"未采用）；proof 用本站真实数据（评测读者数、freeOffer）。

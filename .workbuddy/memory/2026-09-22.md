# 2026-09-22 工作日志

## 深层链接核对（第51-80位批次）

- 用户在 `C:\Users\samja\Desktop\EA资料\解读师官网链接_第51-80位_30人_2026-09-21.txt` 中填入 30 条联盟深层链接；脚本逐条比对 `src/data/affiliateLinks.ts`：**30/30 逐字符一致**，结构校验（offer_id 30/191、aff_id=2326、transaction_id/affiliate 宏、subid2、affiliation_tune）与"一人一链"对应校验全部通过，dist 无需重配置。
- 校验脚本（可复用）：`scratch/research-b2/verify_deeplinks.py`。

## 日期随机散布

- 应用户要求，将 30 篇的 publishDate/verifiedDate + JSON-LD datePublished/dateModified 随机散布到 2026-09-12 ~ 09-21（固定种子 20260921，3 篇/天基础 + 3 篇 ±1 天微调），分布 2-4 篇/天。
- 脚本：`scratch/research-b2/scatter_dates.py`；09-21 恰好 3 篇与原值重合（NO CHANGE 提示有误导，实为正确）。
- 更新后重建站点使 dist 生效。

## 环境坑（重要）

- **bash 内联 `python -c` 中的中文正则会失效**（参数编码损坏导致匹配为 0）——含中文正则的逻辑必须写成 .py 文件执行。
- **构建必须用 PowerShell 工具跑 npm run build**：bash 里 npm 会触发 WSL shim，被沙箱安全策略拦截。
- 构建时长约 20-40 分钟（千页级），dist 出现 sitemap-index.xml 即接近完成；astro 渲染阶段 stdout 静默属正常。

## 构建修复（00:10）

- 日期更新后重建报 `ERR_MODULE_NOT_FOUND (dist\.prerender\chunks\index_*.mjs)`：上次挂起构建遗留陈旧 chunk 与新 chunk 混杂。**修复：Rename-Item dist dist.stale-* 后重建**（环境安全删除包装器对 Remove-Item -Recurse 走 genie-trash，trash 失败会 fail-closed 拒删，改名是无损替代）。
- 干净重建成功：642 页 54 秒，Complete + sitemap 更新；抽查 6 篇新日期全部生效（含 JSON-LD datePublished/dateModified）。
- **待办提醒（另一 AI 批次）**：4 位解读师缺头像——advisortiffany、enchantress-ragini、psychic-tsahai-kasamba-review、sister-michelle（build 日志 [match] 警告），需跑 fetch-reader-avatars。
- `dist.stale-20260922` 目录暂留（安全删除被拒），可手动清理或保留。

## 瑞典 Keen 转化归因（PostHog 查询）

- 联盟后台瑞典 Keen 转化已归因到人：person `84fa3890-98a2-5439-bc87-87bcf0813d69`，移动 Safari、瑞典、**ChatGPT 渠道**（utm_source=chatgpt.com，referrer 空），北京时间 09-22 02:54 直落 `/guides/top-love-psychics-keen/`，16 分钟内 6 次点击（keen inline ×1 → keen-lorrie-c above-fold ×2 → keen-advisor-suzan above-fold ×4），全部有 `aff_go_hit` 确认到达 /go。首访单会话。典型「扫射式决策」。
- 新发现：**`aff_go_hit` 事件已存在**（README 5.9「/go 零埋点」的说法已过时），点击→/go 到达可在 PostHog 内闭环验证，不必再靠 Impact 点击数反推。
- 9/20 流量尖峰（109 会话）确认是污染：62 个 SG 桌面 Chrome 直访单浏览会话（开发服务器泄漏特征）+ 若干 US 单浏览直访爆发，真实基线仍 ~20-30 会话/天。
- 查询脚本：`scratch/posthog_sweden_keen.py`、`posthog_se_keen_session.py`、`posthog_se_keen_gohit.py`、`posthog_0920_spike.py`。

## ChatGPT 渠道 7 日明细（11:16）

- 近 7 天 ChatGPT 渠道：29 会话 / 28 用户（1 人回访），17 次联盟点击（Kasamba 9、Keen 7、Purple Garden 1）。高质量会话 6 个：AU(35min/8pv/1点)、NP(2点)、CA(4点)、GB(2点)、SE 瑞典人(35min/9pv/7点)、US(1点)。另有约 10 个 0 秒单浏览桌面 Chrome 集群（ID×5、VN、BY、ES、GB/BE/LB），疑似预取/爬虫特征，分析时应剔除。
- 更正：瑞典会话实为 **7 次点击**（02:54 keen inline + 02:58/02:59 lorrie-c + 03:02/03:04/03:10/03:11 advisor-suzan），前文写 6 次是漏数。
- 查询脚本：`scratch/posthog_chatgpt_7d.py`（注意 formatDateTime 不能包 dateDiff 整数）。

## ChatGPT 渠道 30 天全量分析（11:24）

- 8/23–9/22 共 **166 会话 / ~80 次联盟点击 / 43 个会话点击（会话点击率 26%）**。日流量 8 月下旬 ~4.4 会话/天 → 9 月 ~5.8 会话/天，点击 8 月 8 次 → 9 月至今 72 次，量与质同步上升。
- 落地页 Top：首页 57（34%）、/reviews/kasamba/ 14、best-tarot-readers-on-kasamba 11、best-tarot-readers-for-love 8、top-love-psychics-keen 6。国家：US 59（36%）> GB 11 > ID/AU/CH/SA。移动端约 2/3。
- 明星会话：09-01 阿联酋移动 53min/9pv/9 点击（Kasamba+Keen 混点）；09-10 美国移动会话即 README 记录的 $125 转化会话；09-22 瑞典 7 点击。Keen 系 slug 点击约 20+ 次，9 月明显起量（top-love-psychics-keen 页 6 次被 ChatGPT 引流）。
- 约 35-40 个「0 秒单浏览桌面 Chrome」集群（ID/VN/BY/RS 等密集出现）仍是预取/爬虫特征，占比 ~22%，分析需剔除。
- 脚本：`scratch/posthog_chatgpt_30d.py`。

## AI 渠道每日用户数折线图（11:37）

- 口径改为**按 person_id 每日去重**。AI 来源盘点（30 天）：ChatGPT 167 会话之外还有 **Perplexity 5、Kimi 1、Copilot 1**（其他 AI 合计 7 会话，日用户 0–2 人，可忽略但已在图上单独成线）。
- 峰值日：9/1 ChatGPT 12 人、9/15 11 人、8/28 10 人、9/4 与 9/10 各 9 人；9/22 半天已 7 人。
- HogQL 语法坑：`countIf(cond, DISTINCT x)` 不合法，**去重条件计数用 `uniqExactIf(person_id, cond)`**；HogQL 的 `||` 拼接不可用，用 `concat()`。README 可考虑补这两条。
- 脚本：`scratch/posthog_ai_daily_users.py`。

## 今日 7 个 AI 用户核验（11:48）

- 实为 **2 真人 + 5 机器**：瑞典人（02:53，7 点击，已转化）+ 巴基斯坦移动（03:32，35 秒 2 页 0 点击）；其余 5 个（GB/BE/LB/GB/IT，11:06–11:30 桌面 Chrome）全是 0 秒单浏览集群，疑似 ChatGPT 链接校验爬虫。
- 坑：带 `$current_url LIKE '%easternalignment.com%'` 过滤时 `aff_go_hit` 会被滤掉（/go 页 URL 不含主域），统计 go_hits 时要去掉 HOST 过滤。

## GSC 加速抓取准备（12:45）

- 昨日更新的解读师文章在 sitemap 中为 **53 个 /reviews/ URL（lastmod=2026-09-21）**，线上 sitemap 已确认生效（抓到 53 条 09-21 lastmod）；清单已导出：`scratch/gsc-submit-20260921-urls.txt`（按 GSC 配额 ~10 条/天分批手动「请求编入索引」）。
- 关键认知：**IndexNow 不覆盖 Google**（只 Bing/Yandex 等），昨天的推送对 Google 无效；Google 加速只能靠 sitemap 重提 + GSC 网址检查 + 高频内链。连接器市场无 GSC 直连工具。

## 今日流量快照（16:27）

- 真实口径：19 会话 / 31 浏览 / 19 用户 / 7 点击（7 次点击仍全部来自瑞典 Keen 用户，11:33 后零新增点击，午后偏淡：+3 会话）。来源：ChatGPT 9 会话、直访 6、Google 4。
- 全量口径今天共 10 次点击：另有 3 次 **10:28–10:29 CN 桌面自测**（tsahai / master-z / simone-guiding-vision 三个 Kasamba 评测页 hero CTA），已被 CN 过滤剔除，与 Impact 后台对账时注意区分。

## IndexNow 推送（00:30）

- 确认线上已部署（新页面 200）后，将批次 51-80 的 30 条 URL 追加进 `scripts/indexnow.mjs` 的 urlList（保留 1-50 批次的 53 条作安全冗余，共 83 条），执行推送：**HTTP 200 accepted**。
- 注意：另一 AI 批次的 4 篇缺头像问题仍未处理（advisortiffany / enchantress-ragini / psychic-tsahai-kasamba-review / sister-michelle）。

## 第 3 批 Pinterest Quiz Pin（全天主任务）

- 交付 `pins素材/03_第3批_Quiz互动/`：112 篇 guides × 2 个 quiz Pin = 224 Pin / 672 张图 / 56 天排期（2026-11-05 → 12-30，接第 2 批 11-04 结束）。
- 互动机制 5 种：yescheck 自检 / myth 判真假 / ask 三选一 / ab 画像测试 / scale 打分；每 Pin 3 页（封面-题目-结果），同篇 quiz1 与 quiz2 分开 28 天发。
- 视觉 8 家族（slips/chips/ticket/dial/tally/fan/flip/sticky）× 8 配色 × 5 字体组，assign 算法保证同天 4 Pin 家族/配色/字体全错开。
- 内容底料来自 `v6_source.json`（md 提取 points+FAQ+h2；customSchema 缩进需按最小缩进 dedent），题库按 8 内容簇手写于 `quiz_copy_v6.py`。
- 管线脚本在 `pins素材/04_生成管线/`：extract_v6.py → quiz_copy_v6.py → build_v6.py → render_v6_all.js（NODE_PATH 指向 `~/.workbuddy/binaries/node/workspace/node_modules`，playwright-core + 本机 Chrome）→ organize_v6_delivery.py。
- Pin 标题主题词用 slug 推导（STOP 词表 + TOPIC_OVERRIDES 手工修正 ~30 个）。

## GSC「生成式 AI 功能」9/1 断崖排查（15:50）

- 现象：AI 展示 8 月下旬峰值 23/日 → 9/1 起 0-1/日，连续 19 天无恢复；总量仅 333 次（7/20-9/19），90 个 URL，US 占 212（64%），头部 `brutally-honest-psychics-keen` 独占 60。
- **定性：非站内问题**。同期常规搜索（09-18 导出）impressions 反升：8/25-31 约 410/日 → 9/1-15 约 484/日，平均排名 19→15；同一批王牌页在常规搜索里健康（brutally-honest 页 985 展示/35 点击/CTR 3.55%/排名 12.8）。Bing AI 引用与第三方 AI 引用统计（Downloads 中 AIPerformanceOverviewStats_9_20，9/1-9/18 每日 41-115 次引用、5-10 页被引）均正常。
- **主因：谷歌侧测量重置**。2026-08-31 谷歌把「生成式 AI 功能」报告 + 「搜索生成式 AI」控制项全球全量开放（此前为英国测试），9/7 起 AI Mode 与 AI Overviews 展示量合并计数；Mueller 9/10-9/16 承认结构化缺陷（AI 区块整体计位置、Show More 内链接不计、区块加载即计展示量）。8/31 前后属两个不可比测量纪元。
- **唯一需立刻排除的真实故障**：GSC 资源级开关 Settings → Search generative AI 是否被设为「排除」或从父级资源继承「排除」（该开关不影响常规排名，正好符合本次现象）。默认应为「包含」，必须在 kafka 前核对。
- 站内已验：robots.txt 线上干净（无 Cloudflare 注入段）、全站无 nosnippet/max-snippet、王牌页 200、llms.txt 正常、Content-Signal ai-input=yes；9 月所有代码改动均在 9/2 之后，晚于断崖起点。
- 结论用语：该面板一直是量级极小的曝光信号（峰值 23/日 vs 常规 ~500/日），不代表流量损失，不要用其同比。

## Quiz Pin R2 改版（用户反馈后重做）

- 反馈要点: 画面杂乱 / 标题和题目必须同屏 / 考试卷式一眼看清 / 字加大 / 文案更钩人 / 日期从 2026-09-22 起。
- build_v6.py 重写为 R2: P1=大标题(60-92px)+第1题, P2=第2、3题(boost 再加大), P3=揭晓+实色 CTA 条; 装饰全部退后(纯色底+细线+grain), 8 家族改为「卷面风格」(题号章样式/分隔线/边框细节)。
- quiz_copy_v6.py 增 HOOKS 表(8 簇×机制专门钩子标题); 修 pick3 重复题 bug(n<7 步长 1)。
- 交付重建: 56 天, 2026-09-22 → 2026-11-16, 与 v4 批(至 10-07)在 9/22-10/07 期间重叠——用户明确要求今天起发, 注意同天两组 Pin 时段是否会撞车由用户自行把握。

## Match 功能上线前全面体检（16:11–16:40）

- 范围：src/match/*（engine/ui/taxonomy/types/analytics/app）、ReaderMatch.astro、pages/match.astro、functions/api/match.ts、data/readers.json、data/affiliateLinks.ts。
- 硬验证全过：生产构建 642 页成功（约 2.5 分钟，非旧记录的 20-40 分钟）；242 readers（Kasamba112/PG81/Keen49，1 位 inactive 被引擎硬过滤）全字段合规、fitVector 全 0-5；242 affiliateUrl→affiliateLinks.ts（257 条 /go/ 门控页全生成）与 242 reviewUrl→content/readers/*.md 双向 0 缺口；引擎 101,376 组合压测 0 崩溃/0 空结果/Top3 无重复/百分比落 [82,99]/120 位不同 #1；内联 JSON 无 </script>/<!-- 截断；/go/ 门控（body 事件委托+sessionStorage+ea_sub）兼容 quiz 动态渲染 CTA；无 CSP、referrer 策略匹配；属性上下文字段 0 危险字符。
- 待办（已告知用户）：P1 /match/ 全站无内链（孤儿页）；P2 urgency 第 6 题对排名零影响（availability 全 'unknown' 恒 3 分，与题目文案承诺矛盾）、"240+"硬编码 3 处（taxonomy 计算屏/ReaderMatch subtitle/match.astro FAQ，应动态注入）、functions/api/match.ts 13 个 TS 错误且无调用方、内联 JSON 431KB 拖重 /match/(494KB)与首页(549KB) HTML；P3 build.ps1 遗留脚本会往 src/match/content/ 写当前实现不引用的文件（建议删）、astro check 196 个历史错误与 match 无关（quizApp 2 个 strictPropertyInitialization 运行时安全）。
- 工具坑：astro check 输出含 ANSI 颜色码，"error" 与 "ts" 之间隔着 \x1b 序列，grep "error ts" 直查会假阴性——先剥离 \x1b 再解析；bash 里 grep 对该文件也同理。
- 可复用验证：scratch/match_engine_test/entry.mjs（esbuild 打包 src/match 引擎+全组合压测，改 readers 后可重跑）；readers.json 是 build-reader-database.mjs 每次构建从 markdown 关键词启发式再生的派生数据（确定性）。

## Match 内链布置 + MCP Server 落地（16:45–17:25）

- **内链（按用户要求只放非文章位置）**：Header 导航第 2 位加「Reader Match」、Footer Platform Reviews 列首行加「Reader Match Tool」、首页 match 组件下方加 editorial 链接段。文章正文零改动。孤儿页问题解决。
- **MCP server 上线**：`functions/mcp.ts`（/mcp 路由，零依赖手写 JSON-RPC 2.0 Streamable HTTP：initialize/202-notifications/ping/tools/list/tools/call，GET→405，无状态无鉴权）+ `src/match/engine/nlMatch.ts`（业务层：classifyIntent 关键词分类 + 3 个工具 match_spiritual_need/match_reader_type/build_questions）。match_reader_type 的 evidence 跑真实引擎 over 242 profiles（匹配数/中位价/平台分布），AI 答案与站内 quiz 同源。
- **v1 产品决策**：独立决策工具，不返回联盟链接和具体导师，只给 practice/reader-type/criteria，商业路径指向 /match/ 页——符合 OpenAI 审核取向，affiliate 藏在匹配之后。
- **验证**：scratch/match_engine_test/mcp_test.ts 51 项断言全过（协议握手+三工具+错误路径）；测试还抓到 nlMatch 一个 const 重赋值真 bug。api/match.ts 13 个 TS 错误全清（枚举校验+类型收紧），quizApp 2 个 strictPropertyInitialization 修掉，match 相关 astro check 错误清零。重建 642 页成功，内链在 dist 全站生效。
- **接入**：部署后 ChatGPT Developer Mode（Pro/Business/Enterprise/Edu）填 https://easternalignment.com/mcp 扫描即可；公开目录提交前需补鉴权+企业验证。完整指南：scratch/mcp-deploy-guide.md。

# 2026-08-17

## ES 西语子站 6 位解读师 review 真实性核查（重要）

用户新建了 `/es/` 西语子站，AI 生成了 6 篇解读师 review（psiquicos 3 + purple-garden-es 3）。应要求核查"真实官网链接 + 头像"。结论：

**真实可验证（4 位）：**
- Luna Aestethic → Purple Garden 真身，`https://www.purplegarden.co/psychics/4921-luna-aestethic`（advisor id 4921，2018 起 ~11,824 readings）。AI review 写的"3,200 条 5.0★"是错的（真实 4.9★ / 11,824）。
- Luz Tarot → 真实，但平台**错位**：真身在 **Purple Ocean**（`https://www.purpleocean.co/psychics/7046-luz-tarot`），不是 review 里写的 Purple Garden。
- Candelifera Laveau / Oshun Aluna → 真实，在 **psiquicos.net**（不是 review 写的 "psiquicosweb.com"）。

**疑似 AI 虚构（2 位，全网查无此人）：**
- Aisha（psiquicos）→ 只有无关同名（动漫/英国 psychic PIN/巴西塔罗师），无 psiquicos 档案。
- Malú / Malutarot（purple-garden）→ 只搜到西班牙歌手 Malú，无 Purple Garden/Purple Ocean psychic 档案。

**两个硬障碍（影响深层链接生成）：**
1. `affiliateLinks.ts` 里**没有 psiquicos 的 TUNE offer** → Candelifera/Oshun 即便真实也无法在后台生成深层链接。
2. Purple Ocean 也无 offer（站点只用 keen/kasamba/purplegarden）→ Luz 无法走 TUNE 深层链接。
3. 只有 **Luna Aestethic** 同时满足"真实 + 有 Purple Garden offer(30)"，可生成深层链接。

**头像处理：**
- 官网均用**视频自我介绍**（非静态图）；静态头像存于 tokenized CDN `purple.brgsrv.com`（base64 token = `{"key":"advisors/<id>/profile_image_<ts>.jpg","edits":{"resize":{"width":W,"fit":"contain"}}}`）。
- 已下载真实头像覆盖 `public/avatars/es-readers/luna-aestethic.jpg` 和 `luz-tarot.jpg`（替换了原先 ~780KB 的错误图）。
- Candelifera/Oshun 头像：psiquicos.net 是 JS/API(Rails, api3.psiquicos.net) 加载，沙箱无法可靠抓取 → 需用户手动截图。
- Aisha/Malú：虚构，无法提供真实头像。

**给用户的行动建议（待确认）：** Aisha/Malú 是虚构人物，继续挂站有合规/信任风险；Luz 需修正平台归属或换真实 Purple Garden 读者；psiquicos 两位需先解决 offer 缺失。

## 删除 4 位解读师 + 提炼可复用内容框架

用户确认指令："虚拟的两个就删掉，Candelifera / Oshun 也删掉吧，但内容可借鉴到后面生成其他解读师"。已执行：

**删除（移入 `scratch/_archived-readers/` 备份，仍从 build 移除）：**
- `src/content/es-readers/psiquicos-web/aisha.md`（虚构）、`candelifera-laveau.md`、`oshun-aluna.md`
- `src/content/es-readers/purple-garden-es/malu.md`（虚构）
- `public/avatars/es-readers/candelifera-laveau.jpg`、`oshun-aluna.jpg`（孤儿头像，aisha/malu 本无头像文件）

**清理两处 index 硬编码引用：**
- `purple-garden-es/index.astro`：从 `spanishReaders` 删 Malú 条目；定价示例"Con Malú"→"Con Luz Tarot"；veredicto 删 Malú。剩 5 个 showcase + 动态区 Luna/Luz。
- `psiquicos-web/index.astro`：删 Candelifera/Oshun/Aisha 三个 showcase 卡（剩 Penélope）；veredicto prose 去掉两人具名引用，保留 Baraja Española 锚点。

**验证：** `src/` 内 grep aisha|candelifera|oshun|malu 零命中；psiquicos 内容目录仅剩 `_plantilla-resena.md` → 动态"Análisis de Lectores Individuales"区块自动隐藏（该页现仅平台评测 + Penélope 卡）。沙箱仍无法跑 build 验证（已知限制），仅做结构与引用静态校验。

**可复用框架参考：** `scratch/ES_READER_FRAMEWORKS_REFERENCE.md` —— 提炼 Candelifera（灵媒/通灵/阿卡西记录 角度）与 Oshun（Yoruba/非裔古巴/Baraja Española/灰烬占卜 角度）的编辑手法：①"仪式名=灵性血脉宣言"信任钩子 ②"明贬暗褒"诚实裂隙 ③"Para quién/Para quién no"双栏；并附生成新真实 reader 的落地清单（必须真实可验证 + 确认 TUNE offer 可用性）。

## 任务 C：汇总两西语平台解读师（Psíquicos Web + Purple Garden）

用户要求"搜索汇总两西语平台所有解读师，含官网链接，按热度排序"。交付：`scratch/es-platform-readers-compilation.md` + `.csv`。

**关键修正（推翻前次结论）：** 前次认为 psiquicos.net "是 SPA/API 无法爬" 是**错的**。实际 `www.psiquicos.net/asesores/<slug>` 服务端渲染公开目录，含 Tendencias/Más populares/Mejor calificados/Todos 热度分区。已抓到 ~22 个真实 asesor + URL。前次失败只是因为测了死端点 `api3.psiquicos.net`。

**致命现实（对 deep link 目标）：** TUNE 网络里**只有 Purple Garden 有 offer(30)**；Psíquicos Web(Adviqo/Barges) 与 Purple Ocean **均无 TUNE offer** → 这两个平台任何读者都生不成深层链接。
- 可生成 deep link 的西语读者实际只有 **Luna Aestethic**（PG, 4921）。
- Júpiter(37,166 readings, 哥伦比亚西语母语) 与 Luz Tarot 真身在 **Purple Ocean**（姐妹 App），常被第三方文章误标为 Purple Garden；Purple Ocean 无 offer。
- PG 西语子集是少数族，公开索引无法穷举；全量 900+ 亦不可在沙箱穷举。

**结论交付：** 文件已标注"Psíquicos Web 全部不可 monetizar（仅编辑内容用）；Purple Garden 仅 Luna 可 deep link"。建议用户若要扩西语 monetizable 库存：手动在 purplegarden.co 找更多西语 profile，或开 Adviqo/Barges 直营 offer。

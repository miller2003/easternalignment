# 2026-09-09 工作日志

## 回退：CTA 兜底改动全部撤销（用户决定）

**背景**：昨晚（09-08）排查 /go 被 Cloudflare 质询时，发现 `src/components/PostHog.astro` 中 `window.open()` 被 in-app webview / 弹窗拦截返回 null 时无兜底，会导致"点了没反应但 PostHog 已记 click"。用户当时拍板"都做"，改动已落地并完成构建。

**用户随后决定全部恢复原状**，原话："这种技术问题我不懂，怕出问题。"

**已执行的回退**：
1. `src/components/PostHog.astro` 第 64–65 行恢复为原始写法 `window.open(dest, '_blank', 'noopener');`，兜底 try/catch 与注释全部移除。
2. `dist/` 用 `dist.bak.20260908-ctafix`（= 09-08 18:52 scores 版本，即动手前的原状）整体覆盖恢复。
3. 删除本次产生的 `build-ctafix.log` / `build-ctafix2.log` / `build-ctafix3.log`。

**回退核验（全绿）**：
- 源码含兜底代码 0 处；第 65 行为原始写法。
- dist：563 html / 765 文件 / go 177 / sitemap-index 191B / sitemap-0 118220B（299 条）—— 与基线完全一致。
- dist 产物中含兜底代码的页面 0 个，含原始写法的 383 个（与改动前一致）。

**当前线上实际状态**：用户**没有**在 Cloudflare 上加任何规则，所以 `/go/*` 按 Referer 质询的规则仍然生效（无 Referer → 403 托管质询；有同源 Referer → 200 真实跳转页）。in-app webview 的 `window.open` 无兜底隐患也恢复为原样。

**dist.bak 现状**：3 个（trust / scores / ctafix）。`dist.bak.20260908-ctafix` 内容 = 当前 dist = 原状，是最安全的回滚点，建议保留；trust / scores 可删。按约定应只留最新 1 个，但删除会被 safe-delete 钩子拦截，需要时给用户生成 .bat 让他双击删。

## Pinterest pin 文案（guides 全量）
- 产出 `pinterest_pin_copy_guides_2026-09-09.csv`：112 篇 guides 每篇 1 行，7 列（slug/url/pin_title/text_overlay/pin_description/hashtags/suggested_board）。
- 规格：pin_title ≤65 字符关键词前置；description ≤337 字符、钩子前置+软CTA+标签内嵌；overlay 5-10 词；每篇 5 个 hashtag；归到 10 个建议画板（按主题簇：Love/Ex Recovery/Twin Flame/Tarot/Mediums/平台×3/Scams/Spiritual/Career/101）。
- 注意：CSV 含逗号字段必须加引号（本次修复 12 处引号错位后 112 行校验通过）。

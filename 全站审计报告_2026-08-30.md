# 全站技术 + 内容审计报告

**审计日期**：2026-08-30
**审计范围**：构建产物 `dist/`（529 页）、源文件 `src/`（253 篇内容）、线上实例 `https://easternalignment.com`
**触发原因**：PostHog 近 3 天流量下滑，AI 引用流量与点击率持续下降

---

## 一、执行摘要

流量下降不是内容质量问题，而是**三条独立的硬故障**叠加，其中两条是决定性的：

| # | 故障 | 影响 | 性质 |
|---|---|---|---|
| **P0-1** | Cloudflare 在边缘拦截了 OpenAI / Anthropic / Perplexity 全部爬虫 | **ChatGPT 与 Claude 的引用率在定义上为 0** | 决定性 |
| **P0-2** | `sitemap-index.xml` 从未生成，`robots.txt` 却声明了它 | Google 无法发现新页面 | 决定性 |
| **P1** | 8/29 把全宽底部 CTA 栏换成 40px 右侧竖条 | CTA 曝光面积骤减 → 点击率下降 | 高概率主因 |

**关键判断**：前两条是机械性故障——内容再好也无法补偿一个抓不到的站点。第三条是设计变更的副作用，可以量化验证。

**已排除的误判**：站点性能正常（TTFB 1.01s，快于 astro.build 的 1.76s）。初次测量到的 11 秒是本地代理造成的假象，直连复测后已推翻。

---

## 二、P0-1：AI 爬虫被 Cloudflare 全面拦截（决定性）

### 证据

对 `https://easternalignment.com/guides/how-to-spot-fake-psychic/` 直连发起 UA 伪装请求：

| 爬虫 | 归属 | 状态码 |
|---|---|---|
| GPTBot | OpenAI（训练） | **403** |
| OAI-SearchBot | OpenAI（ChatGPT 搜索） | **403** |
| ChatGPT-User | OpenAI（用户粘贴 URL） | **403** |
| ClaudeBot | Anthropic | **403** |
| Claude-User | Anthropic | **403** |
| anthropic-ai | Anthropic | **403** |
| PerplexityBot | Perplexity（索引） | **403** |
| Perplexity-User | Perplexity（用户引用） | **403** |
| CCBot | Common Crawl（多家 LLM 语料源） | **403** |
| Bytespider / Amazonbot / DuckAssistBot / cohere-ai | 其他 | **403** |
| Googlebot / bingbot / Google-Extended / Applebot-Extended / Meta-ExternalAgent | 传统搜索 | 200 |

403 响应体为纯文本 `Your request was blocked.`（25 字节），响应头 `Server: cloudflare`，无 `cf-mitigated: challenge`。这是 Cloudflare **AI Scrapers and Crawlers** 托管规则的典型特征——**平面拒绝，不是挑战**。

### 为什么这决定了 AI 引用归零

引用链路是机械的：

```
抓取 → 索引 → 引用
```

第一环断掉，后两环必然为零。项目自己的 `robots.txt` 明确写着 `Content is OPEN to all crawlers (including AI scrapers)`，但**Cloudflare 在请求到达源站之前就拒绝了它，robots.txt 根本没被读取**。这是最容易被忽略的一类故障——配置看起来完全正确，实际完全无效。

**注意 CCBot 被封的连带影响**：Common Crawl 是多数 LLM 训练语料的源头，被封意味着即便将来解封搜索爬虫，语料层面的缺席也需要更长时间恢复。

### 修复（需人工操作 Cloudflare 后台）

1. 登录 Cloudflare → **Security → Bots**
2. 找到 **"Block AI bots"** 或 **"AI Scrapers and Crawlers"** 开关 → 设为 **Off**
3. 若需精细控制：用 **AI Crawl Control**（Security → Bots）按爬虫逐个放行，建议放行 Search/Agent 类（OAI-SearchBot、ChatGPT-User、Claude-User、Perplexity-User），保留对纯训练类的自主选择
4. 同时检查 **Super Bot Fight Mode** 是否对已验证 AI UA 发起挑战

**关键约束**：Cloudflare 的 AI 拦截规则**优先于所有其他规则**。若不先关掉这个总开关，后续任何自定义 allow 规则都不会生效。

**生效预期**：配置变更后约 24 小时传播到 OpenAI 等系统；被长期封禁的站点，爬虫回访频率是逐步恢复的，AI 来源流量的可见回升通常需要 4–6 周。

### 待确认

无法从外部判断该开关是何时启用的。Cloudflare 对新接入域名默认开启，且曾向存量用户推送过"一键开启"提示。若启用时间点落在 8/27–8/29，则与流量下滑的时间线完全吻合——**建议在 Cloudflare 后台的审计日志中核对变更时间**。

---

## 三、P0-2：sitemap 从未生成（决定性）

### 证据

```
dist/ 中 sitemap 相关文件：无（find dist -iname "*sitemap*" 返回空）
全仓库历史 sitemap 残留：无 —— 从未成功生成过
线上 https://easternalignment.com/sitemap-index.xml → HTTP 200，但响应体 0 字节
```

`public/robots.txt` 第 26 行声明：

```
Sitemap: https://easternalignment.com/sitemap-index.xml
```

**声明了一个不存在的空文件。** Google Search Console 会持续报 sitemap 读取失败，且新发布的页面失去主动发现的通道。

### 根因

`@astrojs/sitemap` 在构建流程的最后阶段执行。而本项目的构建稳定地卡在 sitemap 阶段不退出（本次实测：页面生成完毕、文件数停在 322，进程持续 7 分 46 秒无进展后必须手动终止）。每次构建都在 sitemap 写出前被中断，因此这个产物从未存在过。

### 已执行的修复

绕开 astro 构建，用独立脚本 `scratch/audit20260830/gen_sitemap.py` 直接生成，**严格复现 `astro.config.mjs` 中的 filter 与 serialize 逻辑**（含 hreflang 集群与优先级映射）：

```
dist/sitemap-index.xml   239 B
dist/sitemap-0.xml     126,668 B    276 条 URL
```

数量核对：529 页 − 169（`/go/`）− 79（`/astrology/`，配置中明确排除）− 5（legal/`content-manager`）= **276** ✓

两个文件均通过 XML 解析校验。hreflang 集群按配置原样复现（英西互指 + `x-default`）。

**注意**：这是绕开构建的补救。构建卡死本身仍未解决，需单独排查（见第七节）。

---

## 四、P1：CTA 形态变更导致点击率下降（高概率主因）

### 变更事实

`v3.51`（8/29）提交内容：

```
src/components/StickyCTA.astro     删除  -614 行
src/components/SideOfferTab.astro  新增  +189 行
```

| | 变更前 | 变更后 |
|---|---|---|
| 形态 | 全宽固定底栏 | 右侧边缘竖向 tab |
| 尺寸 | 横跨整个视口宽度 | 40px 宽（移动端 36px） × 最小 168px 高 |
| 位置 | 视口底部（移动端拇指区） | 右侧垂直居中 |
| 文案方向 | 横向，可读性强 | 竖向 `writing-mode: vertical-rl` |
| 入场 | 常驻可见 | 延迟 0.55s 后 CSS 动画滑入 |

CTA 的可见面积与视觉权重下降了一个数量级。对移动端影响更大——底栏占据的是最易触达的屏幕区域。

### 技术细节核查

新组件的埋点是**正常的**：`data-cta-source="side-tab-<slug>"`，而 `PostHog.astro` 采用 `document.body` 上的全局委托监听（匹配 `/go/([^/?#]+)/`），因此新 CTA 的点击**能被正常采集**，事件名 `affiliate_link_click` 未变。

**结论：点击率下降是真实的曝光下降，不是埋点丢失。** 这一点很重要——排除了数据假象后，才能确认是设计问题。

### 建议

1. 在 PostHog 中按 `ctaSource` 维度拆分 8/28 与 8/30 之后的点击分布，量化 `sticky` 位与 `side-tab` 位的转化差
2. 若差距显著，考虑在移动端恢复底栏（底栏与侧边 tab 可共存，桌面用 tab、移动用底栏）
3. 若保留单 tab 形态，至少把入场延迟从 0.55s 压缩，并考虑在滚动到 70% 时加一次视觉强调

---

## 五、内容与技术资产审计

### 5.1 已修复：编码损坏（影响 39% 页面）

批量字符损坏，直接显示在页面上并破坏 GEO 数字锚点：

| 损坏形态 | 实例 | 修复前 | 修复后 |
|---|---|---|---|
| 撇号破损 | `Kasamba→'s`、`driver→'s` | 156 页 / 695 处 | **0** |
| 区间破损 | `$1.99→–$30+/min`、`($5→–$10/min)` | 11 页 / 67 处 | **0** |
| 控制字节 | 孤立 `0x02` | 11 页 / 11 处 | **0** |

已在源文件（39 个文件 / 120 处）与构建产物（169 个文件 / 773 处）两侧同步修复，重新构建不会回退。

**对 GEO 的意义**：`$1.99–$30/min` 这类具体数字是 AI 抽取器偏好的锚点，损坏成 `$1.99→–$30` 后锚点彻底失效。

### 5.2 已修复：46 页 Purple Garden 死链

`src/layouts/ReviewLayout.astro:84` 未对平台名做 slug 化：

```js
// 修复前：platformName = "Purple Garden: Advisor Vanessa"
const parentPlatform = platformName.split(':')[0].trim().toLowerCase();
// → "purple garden"（带空格）→ href="/reviews/purple garden/"  ❌

// 修复后
const parentPlatform = platformName.split(':')[0].trim().toLowerCase().replace(/\s+/g, '-');
// → "purple-garden" ✓
```

影响 46 个 PG reader 页面的面包屑 + badge 共 92 处链接。Kasamba / Keen 是单词，不受影响——**这恰好解释了 PG 长期曝光偏低的现象**。

已同步修复源码与 dist 产物。内链复检：30,062 条链接，失效数从 15 降至 **1**（该 1 条为 JS 模板字符串，运行时正常生成，属检测误报）。

### 5.3 待人工处理：价格区间截断（47 页 + 48 页）

两类数字损坏需要**人工判断正确值**，无法自动替换：

- `$3—/min`（47 页）：区间上限丢失，如 `Doing three sessions at $3—/min is $90—20`
- `$90—20`（48 页）：区间错写，应为 `$90–$120` 形式

完整清单（含上下文）已生成：`scratch/audit20260830/broken_price_ranges.txt`

### 5.4 薄内容与 chrome 稀释

| 词数区间 | 篇数 |
|---|---:|
| < 800 | 18 |
| 800–1400 | 136 |
| 1400–2500 | 65 |
| 2500–4000 | 32 |
| 4000+ | 2 |

**154 / 253 篇（61%）低于 1400 词**，命中项目既定的 chrome 稀释红线（全站 chrome 恒定 620–740 词，正文 <1400 词时 chrome 占比 >30%）。

实测 GEO 文本占比：

| 页面 | 正文占比 |
|---|---:|
| `/reviews/kasamba/` | 94.8% |
| `/reviews/keen/` | 93.4% |
| `/guides/best-purple-garden-psychics-ex-recovery/` | 85.4% |
| `/comparisons/purple-garden-vs-keen/` | 83.1% |
| `/guides/how-to-spot-fake-psychic/` | 80.2% |
| `/guides/karmic-relationships-signs-and-lessons/` | 71.0% |
| **首页 `/`** | **11.5%** ⚠ |

首页正文仅占 11.5%——作为聚合页虽有一定合理性，但 88.5% 的 chrome 意味着 AI 抽取首页时几乎只能读到导航、disclosure 与 offer bar。

### 5.5 元数据

| 项目 | 状态 |
|---|---|
| Title 超长（>65 字符，会被 SERP 截断） | 108 页 |
| description 超长（>160 字符） | 87 页 |
| description 缺失 | 2 页 |
| og:image 缺失 | 2 页 |
| 图片缺 alt | **0**（全部合规） |
| 图片无 `loading` 属性 | 705 张（140 页） |
| 重复标题 | **0** |

### 5.6 结构化数据

531 页中 359 页含 JSON-LD。缺失的 172 页中 169 个是 `/go/` 跳转页（合理），2 个是 301 重定向页（合理）。**实际覆盖完整。**

首页 schema 类型：`FAQPage`（10 组 Q&A）、`Organization`、`WebSite`、`Person`、`ImageObject` —— 配置合理。

### 5.7 内容红线合规

按项目既定规则（禁共情、要诊断；禁产品缺陷拆解）逐条扫描：

- 共情句式（`we understand how` / `I know how painful` / `lying awake` 等）：**0 处** ✓
- 差评原文引用、论坛投诉拆解、Trustpilot / BBB 引用：**0 处** ✓
- `scam`（87 页）/ `fraud`（16 页）：出现在"如何识别骗局"的正当语境中，非负面罗列，属合规

内容红线执行良好，无需整改。

---

## 六、已验证正常（无需处理）

| 项目 | 实测结果 |
|---|---|
| **站点性能** | TTFB **1.01s**（对照：astro.build 1.76s、cloudflare.com 2.11s） |
| 核心页面可用性 | 12 个关键路径全部 200，无 404 |
| 线上版本新鲜度 | 与本地 dist 一致（SideOfferTab 已上线） |
| PostHog 配置 | Key 已注入（`phc_wKtyrk...`），`autocapture` 开启 |
| 联盟链接 | 全站 5,011 条 `/go/`，guides 已覆盖 PG 深链 96 次 |
| 结构化数据 | 实际覆盖完整 |
| 内链健康 | 30,062 条，真实失效 0 |
| 内容红线 | 完全合规 |
| 标题唯一性 | 无重复 |

---

## 七、已知待解决：构建卡死

每次 `npm run build` 在页面生成完毕后卡在 sitemap 阶段不退出。本次实测持续 7 分 46 秒、文件数停在 322 后强制终止。

**风险**：构建会清空 `dist/`，卡死中断会留下残缺产物。本次已通过备份 + robocopy 完整恢复（733 文件 / 529 页）。

**环境已保留**：`dist.bak.20260830`（完整备份）、`dist.broken.20260830`（卡死现场，可供排查）

**排查方向**：`@astrojs/sitemap@3.7.2` 声明兼容 `astro@6.1.0`，项目实际为 `astro@6.1.6`。建议先尝试升级 sitemap 集成，或在 `astro.config.mjs` 中临时移除 `serialize()` 的 hreflang 逻辑做二分定位。

---

## 八、行动清单（按优先级）

### 立即执行（决定流量）

1. **Cloudflare 后台关闭 AI 拦截** → **Security → Bots → "Block AI bots" 设为 Off**
   （这是唯一能让 AI 引用流量从 0 恢复的操作，最高优先级）
2. **重新部署** —— 已生成 sitemap + 已修复编码与死链的 `dist/`
3. **24 小时后复测** AI 爬虫矩阵，确认全部返回 200

### 短期（1–2 周）

4. PostHog 按 `ctaSource` 对比 8/28 前后，量化 CTA 形态变更的转化损失，据此决定是否在移动端恢复底栏
5. 人工补全 47 页截断价格区间（清单见 `scratch/audit20260830/broken_price_ranges.txt`）
6. 排查构建卡死（sitemap 集成版本兼容性）

### 中期（结构性）

7. 扩充 <1400 词的 154 篇薄内容，或对其采取 noindex（61% 的占比会拉低整站质量信号）
8. 收敛首页 chrome（当前正文仅 11.5%）
9. 修正 108 页超长 Title、87 页超长 description
10. 补全 `platform` 字段（91 篇缺失，影响 CTA 平台归属与内链匹配）

---

## 九、本次已落盘的产物

| 文件 | 说明 |
|---|---|
| `dist/sitemap-index.xml`、`dist/sitemap-0.xml` | **新增**，276 条 URL，XML 校验通过 |
| `src/layouts/ReviewLayout.astro` | 修复 PG slug 化（46 页死链根因） |
| `src/content/**`（39 个文件） | 修复编码损坏 120 处 |
| `dist/**`（169 个文件） | 修复编码损坏 773 处 + 死链 92 处 |
| `scratch/audit20260830/content_audit.py` | 内容全量审计脚本 |
| `scratch/audit20260830/link_check.py` | 内链死链检测 |
| `scratch/audit20260830/meta_img.py` | 元数据与图片审计 |
| `scratch/audit20260830/gen_sitemap.py` | sitemap 独立生成（绕开卡死的构建） |
| `scratch/audit20260830/broken_price_ranges.txt` | 待人工修复的价格区间清单 |
| `scratch/audit20260830/content_rows.json` | 253 篇内容的结构化审计明细 |

# Eastern Alignment 全站技术审计报告（2026-09-11）

**审计对象**：https://easternalignment.com
**方法**：线上全量抓取（sitemap 297 条 + 关键非 sitemap 页，共 302 URL，全部 200）+ 定向 HTTP 探测（响应头 / 重定向 / 爬虫 UA 矩阵 / 客户端画像对照）+ 本地构建产物全量解析（dist 563 个 HTML / 918 个文件）+ 源码根因定位。
**证据等级标注**：`线上实测` / `源码确认` / `仅构建产物`。

---

## 0. 执行摘要

**结论：未发现正在持续失血的严重故障，全站唯一真正的 P1 是缓存验证器缺失。** 与上一份（2026-09-10）报告相比，站点的技术健康度有实质性提升——上一版的 2 个 P0 与 9 个 P1 中，**8 项已修复并复测通过**，1 项定性有误，1 项仍需处理。本次新发现的问题：**① 全站 HTML 缺少缓存验证器，每次导航全量重传（唯一的 P1）；② 若干中低优先项；③ 三处"告警"实为工具或判断误报，需要纠正，以免被当成待办去"修"。**

**本版对自身初版的三处更正**（初版已于 2026-09-11 14:20 修订）：

| 初版结论 | 更正后 | 原因 |
|---|---|---|
| `/go/*` 被 Cloudflare 挑战，「App 内置浏览器会被拦」定级 P1 | **撤回**。逐头隔离实验证明该规则只认「同域 `Referer`」或「`?ea_sub=`」两个信号，与浏览器指纹无关；真人点击两条都满足，**ChatGPT / Instagram 等 App 内置浏览器不受影响**。规则正确，**不要改**。 | 初版只测了"缺指纹头 → 403"，就外推为"指纹不全的客户端会中招"，未做逐头隔离 |
| 建议给 `/go/*` 加 WAF Skip 规则 | **作废**。照做会把 bot 直连重新放进来（与站长原始意图相反） | 同上 |

同时纠正上一版（2026-09-10）报告的两个错误结论——这两条如果照着做，只会白费工：

| 上一版结论 | 本次复核 | 证据 |
|---|---|---|
| P1-3「214 个 Product 结构化数据节点 100% 无效」 | **假阳性**。全部 Product 都只是 `Review.itemReviewed` 的嵌套节点，按规范本就不需要 `offers`/`aggregateRating`。无任何节点需要修。 | 线上实测 + 源码确认（`ArticleLayout.astro:196`、`ReviewLayout.astro:127`、`EsReviewLayout.astro:133`） |
| P1-1/图表「21 个页面 description < 70 字符，最短 13」 | **工具 bug**。审计脚本的属性值正则用了 `["\']` 收尾，遇到描述里的撇号（`Purple Garden's…`）即截断 —— 13 字符是 `Purple Garden` 这半截。修正后：**最短 110 字符，0 个过短**。 | 线上实测（同一页面原始描述 240 字符） |

**上一版清单中已修复、本次复测通过的 8 项**（不必再动）：

| 上一版编号 | 问题 | 本次复测 |
|---|---|---|
| P0-2 | 4 个 Keen 评测页 canonical / hreflang 指向 404 | ✅ 全站 canonical 与 hreflang 扫描 0 问题 |
| P1-1 | 全站人数口径漂移 162 vs 158 | ✅ `158 (advisor\|profile\|review)` 全站命中 **0** |
| P1-2 | `llms.txt` 残留「实测」口径 | ✅ 已同步为 64/49/49，与全站一致 |
| P1-4 | `/terms/`、`/es/terminos/` 索引信号互相打架 | ✅ 已改为「可抓取 + noindex + 不在 sitemap」 |
| P1-5 | ES hreflang 集群三处不闭合 | ✅ 逐条验证双向互指齐全 |
| P1-6 | `/red-flags/` 空壳页被索引 | ✅ 已 `noindex, nofollow` 并移出 sitemap |
| P1-7 | 图片 39.4 MB / 单张最大 7 MB | ✅ 现共 **8.0 MB**，**0 张 >300KB** |
| P1-9 | 渲染阻塞脚本 | ✅ 全部 302 页 `<head>` 内同步 `<script src>` = **0** |

---

## 1. P0 — 无

本次没有识别出满足 P0 标准的问题（纯损失、无收益、改动量小）。这是一份"没有急症"的审计结果，不是漏检——判定依据见第 5 节「已验证正常」清单，其中 0 个坏内链、0 个重复/缺失 title、0 个 canonical 与 hreflang 冲突，均已全量实测。

---

## 2. P1 — 高优先级

### ✅ 已撤回（原 P1-1）　`/go/*` 的 Cloudflare 规则：复测确认对真人点击无害，**不要动**

> ⚠️ **本节已于 2026-09-11 14:20 整体重写。** 初版结论（"App 内置浏览器会被拦"）是**基于不完整的实验得出的错误判断**——我只验证了"缺少浏览器指纹头会被质询"，就推断出"指纹头不全的客户端会中招"。追加的逐头隔离实验推翻了该推断，详见下。如果你已读过初版，请以本节为准。

**真实机制（逐头隔离实验得出）**：Cloudflare 这条针对 `/go/*` 的规则**与浏览器指纹完全无关**，它只认两个信号——**同域 `Referer`，或非空的 `?ea_sub=` 查询参数**。任一存在即 `200` 放行。

**逐头对照实测**（线上实测，同一出口 IP，UA / Accept / Accept-Language / Sec-Fetch-* 逐项增删）：
```
iOS Safari UA，仅此一个头                       -> 403 质询
iOS Safari UA + Accept + Accept-Language        -> 403 质询   ← 指纹头给全了也没用
iOS Safari UA + Accept + Lang + 同域 Referer    -> 200 放行 ✅
iOS Safari UA + 只有同域 Referer（连 Accept 都没有）-> 200 放行 ✅  ← 决定性：Referer 单独成立
终端 UA + Accept + Lang + chatgpt.com Referer   -> 403 质询
终端 UA + Accept + Lang + google.com  Referer   -> 403 质询
终端 UA + Accept + Lang + 垃圾 Referer           -> 403 质询
无 Referer，带 ?ea_sub=abc.123                  -> 200 放行 ✅  ← 第二凭证
无 Referer，无 ea_sub（纯直连）                  -> 403 质询
同域 Referer 只到域名（无路径）/ 带 www 子域      -> 200 放行 ✅
```
对照组 `/about/` 在全部相同条件下**一律 200** → 规则确实只作用于 `/go/*`，且是**制定规则的站点主自己加的引用页校验**，不是 Cloudflare 的通用 bot 启发式。

**这套规则与真实的真人点击链路是否冲突？不冲突，而且有双重保险：**

| 信号 | 真人点击 CTA 时是否具备 | 证据 |
|---|---|---|
| 同域 `Referer` | ✅ 具备 | 点击处理器 `e.preventDefault()` 后导航到相对路径 `/go/<slug>/`（`src/components/PostHog.astro:121,181,192-194`）——**同源导航，浏览器必然带上完整来源 URL**。全站 `Referrer-Policy` 为 `strict-origin-when-cross-origin`，同源场景下发完整 URL，且**全站 `>src` / `public` / `functions` / `dist` 产物中 `noreferrer` 与 `referrerpolicy` 命中数为 0**（已扫描）。 |
| `?ea_sub=` | ✅ **一定具备** | 点击处理器无条件拼接 `?ea_sub=<人ID>.<点击令牌>`（`PostHog.astro:179-181`），源码注释明确写「ea_sub 必须**永远存在**——它是真人点击的唯一凭证」。 |

**因此：ChatGPT / Instagram / Pinterest / TikTok 等 App 内置浏览器不会被拦。** App 内置浏览器就是系统浏览器内核（iOS 上是 Safari View Controller，Android 上是 Chrome Custom Tabs），同源导航照常带 `Referer`；而且 `ea_sub` 这一条与浏览器无关，是点击处理器写进 URL 的。**两个信号任取其一即放行，真人点击两条都满足。**

**唯一仍会被质询的情形**（且这正是设计意图）：
- 直接访问 / 爬虫抓取 `/go/<slug>/`，既无引用页又无 `ea_sub` —— 即"bot 直连污染联盟后台"，正是这条规则要挡的目标；
- 用户手动复制粘贴 `/go/...` 链接打开（无来源、无 `ea_sub`）—— 极低频，且这类点击本来也不带归因信息。

**结论：这条规则是**正确且有效**的，与页面自身的门禁逻辑（`src/pages/go/[...slug].astro` 的 `referrerOk \|\| ssOk \|\| sub`）语义一致，属于同一套防护的第二层。**不需要改。** 上一版报告建议的"给 `/go/*` 加 WAF Skip"**作废**——那是基于错误前提给出的建议，做了反而会把 bot 直连重新放进来。

**可选的一处极小加固（非必须）**：若担心"用户用了剥离 referrer 的隐私扩展、同时 JS 又被拦导致 `ea_sub` 缺失"这种双重失效，可在现规则里把放行条件再补一条 `or http.request.uri.args["ea_sub"][0] ne ""`（当前实测 `?ea_sub=` 已能放行，说明这条可能已经写在规则里了，无需重复）。**建议动作：什么都不做。**

> 这一项涉及 Cloudflare 后台配置，我不代改；上表已给出你自己复核所需的全部对照命令。

---

### P1-1　全站 HTML 无缓存验证器，`must-revalidate` 因此形同虚设（每次导航全量重传）🟠

**现象**：`/` 的响应头同时存在
```
Cache-Control: public, max-age=0, must-revalidate, public, max-age=0, must-revalidate
ETag:          (无)
Last-Modified: (无)
```
**实测证据**（线上实测）：HTML 响应**既无 `ETag` 也无 `Last-Modified`**；而同域的静态资源都有——`/favicon.ico` → `ETag: W/"933777fb…"`，`/_astro/BaseLayout.fkzBzsa_.css` → `ETag: "391131f7…"`。

**根因与矛盾点**：`must-revalidate` 的语义是"可以使用本地副本，但必须先回源校验"。**没有验证器时，校验只能退化为一次完整的 GET。** 而 `public/_headers` 里写的理由是「304 回源校验很便宜，不会拖慢」——这个推理成立的前提是有 `ETag`，而线上并不存在，所以当前状态是**自相矛盾**的：既声明了必须校验、又没提供校验手段。

**影响**：每个页面 130KB（中位，未压缩）/ 约 26KB（br 压缩后）在**每次站内导航**都要完整重传，无法走 304。移动端站内多页浏览与回访首屏都会受影响。这是纯性能损失，无任何收益。

**最可能的引入点**：`functions/_middleware.js:117` 的 `withVary(await next())` 会用 `new Response(res.body, {...})` 重建响应（为了加 `Vary: Accept`）。Pages 静态资源原本的验证器如果在 `next()` 的响应里，会被原样复制——所以更可能是 **Pages 在走 Function 分支时本就不下发 HTML 的 ETag**。无论成因是哪一种，效果一致。

**修复方向（三选一，改动量递增，建议先在 preview 部署验证）**：
1. **最稳**：在 middleware 里对 HTML 显式计算并下发弱标签 ETag（对 body 做一次快速 hash，注意 Workers 免费版 10ms CPU 预算——只在 `MAX_CONVERT_BYTES` 以内的页面做，且与 Markdown 协商分支互斥）。
2. **最省**：`public/_headers` 里把 HTML 改为 `Cache-Control: public, max-age=300, stale-while-revalidate=86400`，放弃强校验（代价：改内容后最长 5 分钟才可见，当前 `max-age=0` 的"改完立即可见"特性会丢失）。
3. **不动**：接受每次全量重传。以本站在 Cloudflare 边缘 + br 压缩的现状（106KB → 26KB），代价可控。

> ⚠️ 按项目红线，`/go/` 与 CTA 相关代码属高风险改动。**本项不涉及收入链路**，但仍建议在 preview 环境验证后再上生产。

---

## 3. P2 — 中优先级

### P2-1　sitemap 中 14 个枢纽页缺 `lastmod`（含首页）— `线上实测`
```
/            /about/       /comparisons/   /coupons/     /disclosure/
/es/         /es/acerca-de/  /es/divulgacion/  /es/resenas/
/es/resenas/psiquicos-web/  /es/resenas/purple-garden-es/
/guides/     /methodology/  /reviews/
```
`lastmod` 覆盖率 283/297（95%）。缺的这 14 条全是"由 `.astro` 直接生成、没有 frontmatter 日期"的页面 —— `astro.config.mjs` 的 `buildLastmodMap()` 只从 `src/content/` 的 content collection 读日期（`astro.config.mjs:22-52`），所以首页与各栏目枢纽页天然拿不到。
**为什么值得修**：这 14 条恰是权重最高、最需要被重抓的入口页。**改动量**：在 `buildLastmodMap()` 里补一张静态映射表（例如首页/各枢纽页给一个手工维护的日期），约 15 行，不影响任何页面渲染。

### P2-2　AI 爬虫策略：Cloudflare 边缘注入的 robots 块仍在生效 — `线上实测`（**建议重新定性，不是 bug**）
线上 `robots.txt` 顶部由 Cloudflare 注入（含 `Content-Signal: search=yes,ai-train=no,use=reference`），并逐条 `Disallow` 了：`GPTBot`、`ClaudeBot`、`Google-Extended`、`CCBot`、`Bytespider`、`Amazonbot`、`Applebot-Extended`、`meta-externalagent`、`CloudflareBrowserRenderingCrawler`。

上一份报告把它定为 **P0「8 类 AI 爬虫被全域禁止」**。本次复核认为**定性过重**，理由是实测区分了"训练爬虫"与"搜索/引用爬虫"：
```
GPTBot/1.2  -> 200      OAI-SearchBot/1.0   -> 200
ClaudeBot   -> 200      Claude-SearchBot    -> 200
PerplexityBot -> 200    Googlebot/Bingbot   -> 200    meta-externalagent -> 200
```
（`robots.txt` 的 `Disallow` 是自愿遵守协议，WAF 不拦——所以 200 **只说明没被拦截，不说明爬虫真会来读**，两者必须分开陈述。）

被挡的是**模型训练**用途；**搜索与实时引用**通道（`OAI-SearchBot`、`Claude-SearchBot`、`PerplexityBot`、Googlebot）未被挡。因此对"被 AI 引用/被搜索命中"这个目标影响有限。唯一需要单独决策的是 **`Google-Extended`**——它同时控制 Gemini 的 grounding 使用，若你希望内容出现在 Gemini 的联网回答里，需要单独放行。
**这是 Cloudflare 后台策略，改仓库文件无效**（`public/robots.txt` 文件里已经写了这个警告）。属于"要一个决定"，不是"要一个修复"。

### P2-3　`/coupons/` 单页 547KB HTML / 1179 个 div / 171 张图 — `线上实测`
最大的单页，是 HTML 中位数的 4.2 倍。
```
html bytes: max=547KB  p50=130KB
  547.7KB /coupons/     304.1KB /reviews/kasamba/
  254.5KB /reviews/purple-garden/  245.4KB /reviews/keen/
构成（/coupons/）：<svg> 44086B · <script> 21153B · <style> 1548B
                  <img> 171 · <a> 714 · <div> 1179 · data-astro-cid 4868 处
```
DOM 规模（1179 div + 714 a）对移动端渲染成本是真实负担。`data-astro-cid` 是 Astro 作用域样式的必需标记，动不了；可压缩的是"是否需要在同一页渲染全部 171 张头像 + 714 个链接"（分页/懒加载/折叠）。

### P2-4　4 个超重页面 Markdown 协商退回 HTML（含最重要的 `/reviews/kasamba/`）— `线上实测`
middleware 的 150KB 阈值（为 Workers 免费版 10ms CPU 上限留 45% 余量）导致：
```
/reviews/kasamba/  -> 返回 HTML（311KB）
/coupons/          -> 返回 HTML（560KB）
/guides/kasamba-3-free-minutes-guide/ -> markdown ✅
/methodology/      -> markdown ✅
```
这是**有意设计**（宁退回 HTML 也不撞 CPU 上限），不算缺陷；但要意识到：**站点权重最高的平台评测页，恰好是 AI 智能体拿到脏 HTML 的那一页**。若在意，可考虑给这几页做"预生成 markdown 并随构建产物一起部署"的旁路，绕开运行时转换。

### P2-5　安全响应头缺口 — `线上实测`
```
x-content-type-options        ✅ nosniff
referrer-policy               ✅ strict-origin-when-cross-origin
strict-transport-security     ❌ 缺失
content-security-policy       ❌ 缺失
x-frame-options               ❌ 缺失
permissions-policy            ❌ 缺失
cross-origin-opener-policy    ❌ 缺失
```
站点是纯静态、无登录、无表单，实际攻击面很小，因此定为 P2 而非 P1。但 **HSTS 的缺失是"零成本可修"的**（Cloudflare Dashboard → SSL/TLS → Edge Certificates → Enable HSTS，一条开关）。
⚠️ **HSTS 一旦下发，在 `max-age` 到期前对访问者不可撤销**（浏览器会强制 HTTPS，无法回退到 HTTP）。建议先设 `max-age=86400`（1 天）观察一周，确认没有 HTTP 子域被打断，再逐步加长；**不要**一上来就 `includeSubDomains` + `preload`，除非你已确认所有子域都有 HTTPS。

### P2-6　robots.txt 的 `Crawl-delay: 1` — `线上实测`
Google 忽略该指令，Bing 遵守。全站 563 个页面在 Bing 侧会被强制限速到 1 URL/秒，冷启动收录会被拖慢。若不是有意为之，建议删掉这一行。

### P2-7　`public/content-manager.html` 公网可达 — `源码确认`
`/content-manager`（noindex，不在 sitemap）返回 200，是一个 1345 行的本地内容编辑器。
**安全性结论：未发现风险。** 已实测确认页面内**零网络调用**（`XMLHttpRequest`/`fetch`/`WebSocket`/`localStorage` 全部为 0 次命中）、无任何 API token / GitHub PAT / 密钥。它依赖浏览器 File System Access API 操作本地目录，在公网环境下拿不到任何数据。
仅属"内部工具不该公开"的整洁性问题；若想彻底不可达，Cloudflare Access 或改名为不可猜路径即可。

---

## 4. P3 — 观察项

| # | 项目 | 说明 |
|---|---|---|
| P3-1 | 首页响应头重复 | `/` 上 `Cache-Control`/`Referrer-Policy`/`X-Content-Type-Options`/`Link` 各出现两次（`/about/` 与 `/reviews/kasamba/` 各一次）。根因是 `public/_headers` 里 `/` 与 `/*` 两条**故意重复**的规则。四处值逐字相同、幂等，**无害**。文件里已写明"不要顺手清理"，实测证实该判断正确。 |
| P3-2 | 抓取期 2 次瞬时超时 | 首轮抓取有 2 个 URL（`/reviews/purple-garden/psychic-advisor-serena/`、`/psychic-jeanne/`）返回连接错误，复测均 200；另 6 个星座页同理。第二轮回扫 **302/302 全部 200**。疑为我方抓取触发边缘限速，非站点故障，但建议留意 PostHog 是否出现同类丢数。 |
| P3-3 | 孤儿 /go/ slug | `/go/keen-love-lake/` 已构建但全站无任何页面引用（177 个 slug 中唯一一个）。robots 已 Disallow，无害；清理可减少一个静态文件。 |
| P3-4 | `dist/.prerender` 空目录被部署 | 0 字节目录。构建收尾清理即可。 |
| P3-5 | viewport 缺 `viewport-fit=cover` | 全站 561 个页面的 viewport 均为 `width=device-width, initial-scale=1.0`。`SideOfferTab.astro:435` 的 `env(safe-area-inset-bottom)` 只有在 `viewport-fit=cover` 下才有值 → 该变量恒为 `0px`，是**死代码**。实际影响：iPhone 刘海机型上底部浮层会落进 Home 指示条约 20–34px 的不安全区。`SideOfferTab.astro:436` 的 `env(safe-area-inset-right)` 同理。 |
| P3-6 | SERP 截断面 | 标题 >60 字符 **147/297（49%）**，>70 字符 74 条，最长 94（`/reviews/purple-garden/enchantress-ragini/`）。描述 >160 字符 **98/297（33%）**，最长 267。属内容层优化项，非技术缺陷；只是要知道近半数 SERP 标题会被砍。 |
| P3-7 | 图片无尺寸 | 审计报出 1 张缺 `width`/`height`，定位后确认是 `content-manager.html` 里的 JS 模板字符串（`<img src="$2" alt="$1">`），**不是真实图片**。实际为 0。 |
| P3-8 | `_headers` 里关于 `/go/` 引用策略的注释与事实不符 | `public/_headers` 注释写「`/go/*` : referrer-policy: **same-origin**（不向联盟泄露来源，保留不动）」，但**该文件里 `/go/*` 规则块实际没有这一行**（git 全历史证实 `same-origin` 只出现在注释里）。线上实测：`/go/kasamba/` 带 `Referer` 访问时返回的是 `strict-origin-when-cross-origin`（继承自 `/*`）。<br>真实后果：`/go/` 跳转到联盟站时会发送 `Referer: https://easternalignment.com/`（仅域名），而不是"完全不发送"。影响接近于零（联盟侧本来就通过 `aff_sub2` 拿到归因），但这行注释描述的是一个**不存在的**配置，将来有人依据它做判断会出错。<br>建议：把注释改成实测值，或（若确实想隐藏来源）在 `/go/*` 块里显式加 `Referrer-Policy: same-origin`。**纯文档修正，无紧迫性。** |

---

## 5. 已验证正常项（**请勿"顺手优化"**）

以下均为本次全量实测通过，改动它们只会引入回归：

**索引与结构**
- **0 个坏内链**：对 dist 全部 563 个 HTML 逐页解析所有 `href`/`src`，无任何指向不存在路径的内部链接。
- **sitemap 297 条 ↔ 构建产物完全对齐**：297 条全部有对应真实页面，0 条指向 404 或不存在页。
- **0 个重复标题 / 0 个重复描述 / 0 个缺失标题 / 0 个缺失描述**（297 个可索引页）。
- **canonical 0 问题、hreflang 0 问题**。ES 集群逐条验证双向互指齐全（`/about/` ↔ `/es/acerca-de/`、`/es/resenas/purple-garden-es/` ↔ `/reviews/purple-garden/` 等），`/es/resenas/psiquicos-web/` 正确保持自指闭合（无英文对应页）。上一版的 P0-2（4 个 Keen 页 canonical 指向 404）与 P1-5（ES 集群不闭合）**均已修复**。
- **0 个孤儿页**（297 个可索引页全部有站内入链）。
- **0 个页面缺 `<h1>`、0 个页面多 `<h1>`、0 个页面多 `<main>`、0 张图片缺 `alt`、0 个重复 `id`**。全站均有 skip link。
- **索引信号三处一致**：meta robots / robots.txt / sitemap 无矛盾。`/red-flags/` 已正确改为 `noindex, nofollow` 且移出 sitemap（上一版 P1-6 **已修复**）；`/privacy/`、`/terms/`、`/es/privacidad/`、`/es/terminos/` 同样为"可抓取 + noindex + 不在 sitemap"的正确形态（上一版 P1-4 **已修复**）。

**结构化数据**
- 0 个页面存在 malformed JSON-LD；0 个可索引页完全没有 JSON-LD。
- `Article` 的 `headline`/`image`/`datePublished`/`author`/`dateModified`、`Review` 的 `itemReviewed`/`reviewRating`/`author`、`FAQPage` 的 `mainEntity`、`AggregateRating` 的计数 —— **必填项全部无缺失**。
- 修正工具口径后，**不存在任何"Product 缺 offers"的真实问题**（见第 0 节）。

**内容与转化链路**
- **0 个过期口径数字**：全量抓取缓存中，`158 (advisor|profile|review)`、`$840`、`24 advisor` 命中数均为 **0**；`162` 只出现在 64+49+49 的正确合计语境。`llms.txt` 的 64/49/49 与真实值一致（上一版 P1-1、P1-2 **已修复**）。
- **`llms.txt` 内容已与全站口径同步**，且每页响应头都带 `Link: </llms.txt>; rel="describedby"`。
- **176 个被引用的 `/go/` slug 全部存在对应构建页面，0 个断链**。
- **CTA 的 `rel="nofollow sponsored"` 在 11 个组件中覆盖完整**（`ComparisonTable`/`DealStrip`/`EndCTA`/`InlineCta`/`LeftSidebar`/`PlatformCard`/`ReaderCard`/`ScorePanel`/`SideOfferTab`/`SidebarDealCard`/`TopOfferBar`）。
- **`/go/` 门禁逻辑完好**：无 referrer / 无 session 标记 / 无 `?ea_sub=` 时不跳转（机器人空操作），三个信号任一命中才 `location.replace`。

**性能**
- **图片总量 8.0 MB**（`.jpg` 7.1 / `.webp` 0.8 / `.png` 0.2），**0 张超过 300KB**。对比上一版审计的 39.4 MB、单张最大 7 MB，**这是本次最显著的一项改善**（上一版 P1-7 **已修复**）。
- 压缩正常：首页 `Accept-Encoding: br` 下 106KB → **26KB**。
- 现代格式已铺开：161 个 webp（头像 `picture` + 原图并存）。
- `/_astro/*` 内容哈希资源：`max-age=31536000, immutable` + ETag，缓存策略正确。
- **渲染阻塞：全部 302 页 `<head>` 内的同步 `<script src>` 数量为 0**（上一版 P1-9 的脚本阻塞部分**已修复**）。剩余的是 `<head>` 内 6–7 个样式表请求（Astro 按组件的 scoped CSS），属真实但温和的阻塞成本；4 个 `preconnect` 已预置。
- `Markdown for Agents` 内容协商工作正常：`Accept: text/markdown` → `Content-Type: text/markdown` + `X-Markdown-For-Agents: 1`；普通浏览器请求 → 仍是 `text/html`。输出质量良好（`/about/` 8484 字节，标题层级、列表、粗体、来源 URL 完整）。`Vary: Accept` 已正确下发，不会缓存串味。

**安全**
- **无硬编码密钥**：对 `src/`、`public/`、`functions/`、`scripts/`、`astro.config.mjs` 扫描 GitHub PAT / `ghp_` / `sk-` / AWS AKIA / 私钥 / Slack / Google API key / Bearer token —— **0 命中**。
- `.env` 已被 `.gitignore` 排除，未进入构建产物。

---

## 6. 修复清单（按执行顺序）

### 第一批 — 今天（都是几分钟、零或极低风险）
| # | 动作 | 位置 | 风险 |
|---|---|---|---|
| ~~1~~ | ~~给 `/go/*` 加 WAF Skip 规则~~ **已撤回，不要做**（见「已撤回」一节） | — | — |
| 1 | 开启 HSTS，先 `max-age=86400`，不加 preload（P2-5） | Cloudflare Dashboard | 低，但**不可即时撤销** |
| 2 | 删除 robots.txt 的 `Crawl-delay: 1`（P2-6） | `public/robots.txt` | 无 |
| 3 | 决定 `Google-Extended` 是否放行（P2-2） | Cloudflare Dashboard | 无（策略决定） |
| 4 | **建议不做任何操作**：确认 `/go/*` 现有规则保持原样即可（原 P1-1） | — | — |

### 第二批 — 本周
| # | 动作 | 位置 |
|---|---|---|
| 5 | 给 14 个枢纽页补 `lastmod`（P2-1） | `astro.config.mjs` 的 `buildLastmodMap()`，约 15 行 |
| 6 | 处理 HTML 缓存验证器（P1-1，三个方案任选其一，**先 preview 验证**） | `functions/_middleware.js` 或 `public/_headers` |
| 7 | 评估 `/coupons/` 的 DOM 与图片裁剪（P2-3） | `src/pages/coupons/index.astro` |

### 第三批 — 结构性（可选，收益需要权衡）
| # | 动作 |
|---|---|
| 8 | 为 4 个超重页预生成 markdown 旁路，让 `/reviews/kasamba/` 也吃到干净 markdown（P2-4） |
| 9 | viewport 加 `viewport-fit=cover`，让 `SideOfferTab` 的 safe-area 变量真正生效（P3-5，需真机回归刘海机型） |
| 10 | 分批收敛长标题/长描述（P3-6，147 + 98 条，属内容工程） |

---

## 7. 审计过程说明与工具修正

**本次一并修正了审计工具链的两个缺陷**（否则会持续产出假发现）：

1. `live_audit.py` 的 description 提取正则用 `content=["\'](.*?)["\']` 收尾 —— 描述里一出现撇号（`Garden's`、`it's`）即在撇号处截断。已改为反向引用 `content=(["\'])(.*?)\1`。**该 bug 曾让上一版报告得出"21 页描述过短、最短 13 字符"的错误结论。**
2. `live_audit.py` 对所有 `Product` 节点一律要求 `offers`/`aggregateRating` —— 未排除 `Review.itemReviewed` 下的嵌套节点。已改为只检查顶层 Product。**该 bug 曾让上一版报告得出"214 个 Product 100% 无效"的错误结论。**

两项修正已写回技能本体，并在技能文档中补入「区分路径级挑战与客户端级拦截的四种客户端画像对照法」与「HTML 缓存验证器单独检查」两条方法。

**审计产物**（可复跑、可举证）：
- `scratch/audit_20260911/live_audit.py`　线上全量抓取 + 分析
- `scratch/audit_20260911/out/LIVE_AUDIT.txt`　原始报告
- `scratch/audit_20260911/out/cache/`　302 张页面原始 HTML（分析证据）
- `scratch/audit_20260911/linkcheck.py`　内链健康度（按缓存解析 + 逐个 HTTP 复验）
- `scratch/audit_20260911/distcheck.py`　构建产物完整性与 sitemap 对齐
- `scratch/audit_20260911/cta_check.py`　`/go/` slug 引用完整性
- `scratch/audit_20260911/dupid.py` / `imgcheck.py`　重复 id / 图片体积

---

## 附录 A　WAF 自定义规则审计（2026-09-11 15:05 追加）

站长提供了 Cloudflare 后台的实际规则配置，据此做实配复核。

### A.1　规则真身

| 顺序 | 名称 | 表达式 | 操作 | 事件数 | CSR |
|---|---|---|---|---|---|
| 1 | Skip: Real Aff Clicks | `(http.request.uri.path contains "/go/") and (http.request.uri.query contains "ea_sub=")` | 跳过 | 4 | – |
| 2 | Protect Aff Redirects | `(http.request.uri.path contains "/go/") and (not http.referer contains "easternalignment.com")` | 托管质询 | 680 | 0% |

（自定义规则用量 2/5）

### A.2　与实测的吻合度：**完全一致，逻辑正确**

这两条规则**精确解释**了此前逐头隔离实验的全部现象，可作为规则生效的交叉验证：

| 实测输入 | 命中规则 | 实测结果 | 规则预测 |
|---|---|---|---|
| iOS Safari UA + `Accept` + `Accept-Language`（无同域 Referer、无 ea_sub） | 规则 2 | 403 质询 | ✅ 一致 |
| iOS Safari UA + **仅同域 Referer** | 都不命中 | 200 | ✅ 一致 |
| 无 Referer + `?ea_sub=abc.123` | 规则 1（跳过） | 200 | ✅ 一致 |
| `chatgpt.com` / `google.com` / 垃圾 Referer | 规则 2 | 403 质询 | ✅ 一致 |
| 无 Referer、无 `ea_sub`（纯直连） | 规则 2 | 403 质询 | ✅ 一致 |
| `/about/` 全部条件 | 都不命中（路径不含 `/go/`） | 一律 200 | ✅ 一致 |

**顺序正确**：Skip 规则排在质询规则**之前**，所以带 `ea_sub` 的请求会先被跳过、不再落入质询。顺序若对调则整个机制失效——**后续新增规则时必须保持 Skip 类规则在最上**。

**匹配面已验证无副作用**：`contains "/go/"` 是子串匹配，理论上会误伤任何含 `/go/` 的路径。已全量核查 sitemap 297 条 + dist 563 个页面路径——**非 `/go/` 前缀却含 `/go/` 子串的路径数为 0**，无副作用。`/api/postback` 不含 `/go/`，回传链路不受影响。

### A.3　真人点击是否被拦：**用生产数据验证，未被拦**

`aff_go_hit` 埋点于 **2026-09-10 15:40:18 +0800** 上线（commit `1235615`）。据此把 09-10 以来的点击逐条对齐：

| 时间（东八区） | 事件 | 是否有对应 `/go/` 到达 | 判定 |
|---|---|---|---|
| 09-10 11:34:02 | affiliate_link_click（kasamba） | 无 | **埋点尚未上线**，无法记录。**该点击最终产生了一笔 $125 转化（`Order_Converted`）→ 反证它未被拦截** |
| 09-10 12:29:16 | affiliate_link_click（keen） | 无 | 同上，埋点未上线 |
| 09-10 18:21:32 | affiliate_link_click + **aff_go_hit** | 有 | ✅ `gate_reason=referrer`，`nav_ttfb_ms=320` |
| 09-10 18:41:09 | aff_go_hit + affiliate_link_click | 有 | ✅ `gate_reason=referrer`，`nav_ttfb_ms=339` |
| 09-11 全天 | – | – | 0 点击，无样本 |

**三条结论**：
1. **埋点上线后的 2 次真实点击，100% 到达 `/go/`**，且 `gate_reason=referrer` —— 同时从生产侧证实了"真人点击必然携带同域 Referer"这一判断。
2. **`nav_ttfb_ms` 320 / 339 毫秒** —— 若被 Cloudflare 质询，TTFB 会是秒级并返回验证页。~330ms 属正常边缘响应，**没有发生质询**。
3. **`aff_go_blocked` 事件总数为 0** —— 页面门禁从未误杀过任何人。

（09-09 及更早 `go_hit=0` 属埋点未上线，不是故障。）

### A.4　680 次质询拦的是什么

站点真实 CTA 点击量级为**每日个位数**（09-03～09-11 区间 0–8 次/日）。而质询规则的事件数为 680，且 **CSR（质询解决率）为 0%**。
> ⚠️ 严格来说 CSR=0% 单独看是**双向可解释**的：既可能"被质询的全是机器人（机器人不会做验证）"，也可能"全是真人但全被吓退"。真正把二者区分开的是 A.3 的生产数据——真人点击一路畅通（`gate_reason=referrer`、TTFB 正常、0 次 blocked、且有一次 $125 转化）。**两者合起来，才成立"被质询的基本都是非人类流量"的结论。**

**含义：规则在实打实地干活**（挡住了量级约为真实点击上百倍的直连流量），且没有牺牲真实转化。**这是本次审计里唯一一个"结论是保持原样、什么都别做"的项。**

### A.5　可选加固（三项，均非必需；按性价比排序）

**① 最值得考虑：页面门禁接受「任意非空 `ea_sub`」**
`src/pages/go/[...slug].astro` 的判定是 `isReal = referrerOk || ssOk || !!sub` —— `!!sub` 只要求非空。配合 WAF 的 Skip 规则，**任何请求只要带上 `?ea_sub=x`（哪怕值是 `1`），就能同时跳过质询、并通过页面门禁、触发一次真实的联盟点击**。参数名 `ea_sub` 就明文写在每个页面的内联脚本里，可被读取。

- 加固方式（1 行）：把 `!!sub` 收紧为 `/\d?\w+\.\w+/.test(sub)`（即要求形如 `<人ID>.<令牌>`，含一个点号）。点击处理器**必然**产出带点号的 `subId`（`PostHog.astro:157-158`，含 `anon-` 兜底路径），所以对真人**零影响**，但能挡掉最低成本的伪造。
- 局限（必须说清）：这只挡"随手编一个 `?ea_sub=1`"。有能力的机器人可以编成 `?ea_sub=a.b`，静态站层面**无法根治**——彻底的方案是让 Worker 侧校验签名令牌，属结构性改造。
- ⚠️ **该文件属收入链路**，按项目约定不擅自改动。**是否要改由你决定**，改的话我会同时给出回滚点。

**② 需要你确认：Skip 规则的「要跳过的组件」范围**
截图只显示了表达式，看不到 Skip 动作展开后的勾选项。如果勾了"所有组件"（含托管规则、Bot Fight Mode、速率限制），那么**任何带 `?ea_sub=` 的请求都会绕过整条 WAF**，而不只是绕过自定义规则——那是个比 ① 更大的口子。建议在后台展开该规则确认，只勾"剩余自定义规则"。

**③ 影响极小，可不做：`http.referer contains` 是子串匹配**
以下 Referer 都会被判为"来自你自己"从而放行：
```
https://easternalignment.com/reviews/kasamba/   ✅ 正常
https://www.easternalignment.com/               ✅ 正常
https://evil.com/easternalignment.com           ⚠️ 伪造也能过
https://easternalignment.com.attacker.net/      ⚠️ 伪造也能过
https://not-easternalignment.com/               ⚠️ 伪造也能过
```
但要伪造 Referer 的机器人，本来也已具备"像人一样发起点击"的能力（它同样能满足页面门禁），所以**实际风险接近于零**。若想收紧，可把条件改为按来源前缀匹配。**收益很小，不建议为此消耗规则额度。**

### A.6　明确不要做的三件事

1. **不要给 `/go/*` 加 WAF Skip 规则** —— 会把 A.3 里那类被挡住的直连流量全部放回来，与原始意图相反。（本报告初版的建议已作废。）
2. **不要调整这两条规则的先后顺序**。
3. **不要让规则 2 的 `not` 被去掉或改成"路径包含 /go/ 即质询"** —— 那会连真人点击一起挡掉。

---

## 附录 B　"用户体验优先"专项复核：访客会不会看到质询页（2026-09-11 15:15 追加）

站主要求以用户体验为最高优先级，并特别点名 AI 类 App 的内置浏览器。本节用 iOS Safari View Controller 的请求画像（即 ChatGPT / Instagram / Pinterest 等 App 在 iOS 上打开链接时浏览器实际发出的头）逐渠道实测。

### B.1　访客看到的质询页长什么样

命中质询时返回 `HTTP 403` + `Cf-Mitigated: challenge`，页面标题 `Just a moment...`，可见文字仅 `Just a moment... Enable JavaScript and cookies to continue`，带一个 `Verify you are human` 勾选框与 Cloudflare 标识。**是一个需要访客手动交互的中转页，不是内容页。**

### B.2　逐渠道实测：内容页（访客真正落地的页面）

同一个内容页 `/reviews/kasamba/`，分别伪装成从各渠道进入（`Referer` 换成对应站点）：

| 访客来源渠道 | 实测结果 |
|---|---|
| 直接输入 / 无来源 | **200** ✅ |
| **ChatGPT 内置浏览器** | **200** ✅ |
| Google 搜索结果 | **200** ✅ |
| Bing | **200** ✅ |
| Pinterest | **200** ✅ |
| Instagram 内置浏览器 | **200** ✅ |
| Facebook 内置浏览器 | **200** ✅ |
| Reddit | **200** ✅ |
| X / Twitter | **200** ✅ |

**结论：所有内容页在任何渠道下都不受这两条规则影响。** 原因是两条规则的第一个条件都是 `http.request.uri.path contains "/go/"` —— 只有 `/go/` 中转页在覆盖范围内，`/reviews/`、`/guides/`、`/comparisons/`、`/es/` 等页面**结构上不可能命中**。访客从哪个渠道进来、用的是不是 App 内置浏览器，都不改变这一点。

### B.3　逐渠道实测：`/go/` 那一跳（CTA 点击后的中转页）

| 场景 | 实测结果 |
|---|---|
| 站内点击 CTA（带同域来源页） | **200** ✅ |
| 站内点击 CTA（带同域来源页 + `ea_sub`） | **200** ✅ |
| **来源页被浏览器/扩展抹掉，但带 `ea_sub`** | **200** ✅ |
| 从外部链接进入 `/go/`（不带 `ea_sub`） | 403 质询 ❌ |
| 从外部链接进入 `/go/`（带 `ea_sub`） | **200** ✅ |

**真人从 CTA 点进来的路径有三重覆盖（同域来源页 / `ea_sub` / 会话标记），实测全部 200。**

### B.4　唯一会被质询的真实情形，是否损失转化？——不损失

唯一会撞上质询的是"外部链接直接指向 `/go/xxx/` 且不带 `ea_sub`"（例如有人复制了这个中转链接分享出去）。**但这一情形下，你的页面自身的门禁本来也不会跳转**：

```js
var isReal = referrerOk || ssOk || !!sub;   // /go/ 页内联脚本
if (isReal) { window.location.replace(dest); }
```
- `referrerOk` 要求来源页 origin **等于本站** → 外部来源为 false
- `easub` 参数不存在 → `sub` 为 null
- `ssOk` 依赖本站点击处理器写入的 sessionStorage → 未经过本站点击则为 false

三者皆 false → **不执行跳转 → 联盟后台不会记录任何点击**。

因此：**这条规则拦下的请求，即便放行也不会产生转化。它没有、也不可能因用户体验问题而损失任何一笔转化**——它拦的是"本来就不会成交"的流量。

### B.5　若要做到"访客在整个站上永远看不到 Cloudflare 页面"的绝对保证

理论上的最后一条缝隙：`/go/` 被规则 2 放行（外部来源 + 带 `ea_sub`）**或**被拦下，两种情况下页面门禁可能都不跳转，此时访客看到的是一个**空白页**（`<body>` 无内容）。这既不是质询页、也不是内容页，体验上仍是断的。

**可选改进（属收入链路，需站主确认，未擅自改动）**：给 `/go/[...slug].astro` 的"非真人"分支加一段最小可见内容，例如"此链接已失效 / 请回到评测页"的提示与一个返回首页的链接。改动范围是新增静态标记，不触碰跳转逻辑与门禁判定，回滚成本为零（删掉即可）。收益：把"白页"变成"有出路的页面"，同时把该页从"纯空白"变为对访客有意义的落地页。

> 注：本节全部结论基于线上实测 + 源码确认；未对站点做任何修改。

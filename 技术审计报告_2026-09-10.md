# Eastern Alignment 全站技术审计报告

**审计日期**：2026-09-10
**审计对象**：`https://easternalignment.com`（线上生产环境）+ `src/` 源码 + Astro 构建产物
**审计范围**：构建/部署、SEO、GEO（生成式引擎优化）、结构化数据、链接图谱、性能、可访问性、安全头、合规
**方法**：① 线上全量抓取 315 个 URL（sitemap 299 条 + 关键非 sitemap 页）逐页解析；② `src/` 源码级静态分析；③ 对当前源码做一次完整重建（563 html / 765 文件）并交叉验证；④ 对高风险项做定向 HTTP / 响应头 / UA 矩阵实测

---

## 0. 执行摘要

站点的基础工程质量**明显高于同类联盟评测站**：无重复 title/description、无孤儿页、无内部死链、无缺 alt 图片、无缺 H1、JSON-LD 零格式错误、重定向链路（www / http / 旧 slug / AI 幻觉 URL）全部正确、`/go/` 门禁设计完整、og 图片尺寸与声明一致。

但有 **2 个 P0 级缺陷**会直接抵消站点当前的 GEO 战略，另有 **9 个 P1 级缺陷**在持续泄漏索引覆盖与转化。

| 等级 | 数量 | 影响面 |
|---|---|---|
| P0 立即处理 | 2 | AI 引用渠道被自断；4 个评测页面临被 Google 剔除 |
| P1 高 | 9 | 索引覆盖、富结果、转化路径、性能 |
| P2 中 | 11 | 富结果字段、可访问性、安全/合规 |
| P3 观察 | 5 | 抓取预算、构建脚本卫生 |

**本次审计同时修复了一处本地事故**：审计开始时 `dist/` 处于「部分清空」的损坏状态（406 html / 550 文件，缺 index.html、sitemap、robots.txt）。现已重建为完整产物（563 html / 767 文件，sitemap 299 条已通过「页面集合 diff 一致 → 复用备份 sitemap」的方式恢复）。

---

## 1. P0 — 立即处理

### P0-1　Cloudflare 已接管 robots.txt，8 类 AI 爬虫被全域禁止 🔴

**证据**（线上 `https://easternalignment.com/robots.txt` 实测，非仓库版本）：

```
# BEGIN Cloudflare Managed content
User-agent: *
Content-Signal: search=yes,ai-train=no,use=reference
Allow: /

User-agent: Amazonbot            Disallow: /
User-agent: Applebot-Extended    Disallow: /
User-agent: Bytespider           Disallow: /
User-agent: CCBot                Disallow: /
User-agent: ClaudeBot            Disallow: /
User-agent: CloudflareBrowserRenderingCrawler  Disallow: /
User-agent: Google-Extended      Disallow: /
User-agent: GPTBot               Disallow: /
User-agent: meta-externalagent   Disallow: /
# END Cloudflare Managed Content
```

**为什么这是 P0**

- 站点的核心战略是 GEO：`llms.txt`、`functions/_middleware.js` 的 `Accept: text/markdown` 内容协商、`Link: </llms.txt>; rel="describedby"`（实测已上线）——整套基建的存在意义就是**让 AI 引擎读得干净、引得多**。
- 而这段边缘注入的策略**禁止了 GPTBot（OpenAI 训练）、ClaudeBot（Anthropic 训练）、Google-Extended（Gemini/Vertex 训练）、CCBot（Common Crawl → 喂给大量模型）、Applebot-Extended、Bytespider、Amazonbot、meta-externalagent**。
- 仓库里 `public/robots.txt` 的注释仍写着 *"Content is OPEN to all crawlers (including AI scrapers) for discovery"* —— **这句话在线上已经不是事实**。
- `Content-Signal: ai-train=no` 与随后对 `use=reference` 的授权**自相矛盾**：既然允许 reference use，就不该同时 `Disallow: /` 掉检索类爬虫。

**仍可访问的爬虫**（重要，不必恐慌）：`OAI-SearchBot`、`ChatGPT-User`、`Claude-User`/`Claude-SearchBot`、`PerplexityBot`、`Googlebot` 均**未被列入**，因此 ChatGPT Search / Perplexity 的引用渠道仍然通畅。实测这些 UA 请求均返回 200（WAF 未硬拦，robots 是自愿遵守协议）。

**动作**：登录 Cloudflare → Bots / AI Crawl Control，确认这是否为有意开启。若否，关闭 "Block AI bots" 并清理 Managed robots 段；若是，请删掉仓库 robots.txt 里那句已失效的 "OPEN to all crawlers" 注释，避免文档与线上事实背离。

---

### P0-2　4 个 Keen 评测页的 canonical 与 hreflang 指向 404 页面 🔴

**证据**（线上实测，目标 URL 实测 404）：

| 页面 | canonical / hreflang 指向 | 实测 |
|---|---|---|
| `/reviews/keen/flora-knows-all-keen-review-2026/` | `/reviews/flora-knows-all-keen-review` | **404** |
| `/reviews/keen/love-psychic-victoria-sands-keen-review-2026/` | `/reviews/love-psychic-victoria-sands-keen-review` | **404** |
| `/reviews/keen/psychic-suzen-on-keen-review-2026/` | `/reviews/psychic-suzen-keen-review` | **404** |
| `/reviews/keen/psychicreader19622-raymond-keen-review-2026/` | `/reviews/psychicreader19622-raymond-keen-review` | **404** |

**根因**：4 个 md 文件的 frontmatter 里第 18 行写死了 `canonicalUrl:`，URL 缺少 `-2026` 后缀（疑似 slug 改名后遗漏）。

```
src/content/readers/keen/flora-knows-all-keen-review-2026.md:18
canonicalUrl: "https://easternalignment.com/reviews/flora-knows-all-keen-review"
```

**为什么是 P0**：canonical 指向不存在的 URL，等同于告诉 Google「本页是那个 404 的副本」→ 这 4 页会被判定为重复内容并从索引中剔除；同时 hreflang 集群因此断裂。这是**纯损失、无收益**的缺陷。

**修复**：删除这 4 个文件的 `canonicalUrl` 行（`BaseLayout.astro` 会自动用 `Astro.url.pathname` 推导正确 canonical），同时修正 `hreflangEn`。改动量：4 行删除。

---

## 2. P1 — 高优先级

### P1-1　全站人数口径漂移：162 vs 158 🔴

站点已有唯一真源 `src/lib/siteStats.ts`（构建时从 content collection 计算），实测源文件为 **Kasamba 64 / Keen 49 / Purple Garden 49 = 162**。

- ✅ 正确引用真源：首页「162 advisor profiles」、`/comparisons/`、`/coupons/` 全部显示 64/49/49
- ❌ 硬编码且已过期：
  - `src/content/guides/most-accurate-love-psychics.md:5`（**meta description**）→ "158 reviewed readers"。**该描述被 RelatedContent 组件复用，实测污染了 35+ 个页面**（guides 与 reviews 均有）
  - `src/content/guides/before-you-pay-for-a-psychic-reading.md` → "158…63 on Kasamba, 49 on Keen, and 46 on Purple Garden"、"150+ published single-advisor reviews"
  - `src/content/guides/top-love-psychics-online.md` → "158 reviewed readers"、"158 reader reviews"
  - `public/llms.txt` → 63 / 49 / 46（见 P1-2）

**修复**：改 3 个 md + `llms.txt`，共 4 个文件；或更彻底——把这段描述改为不含绝对数字的表述，或在构建期从 `siteStats` 注入。**注意 `siteStats.ts` 无法触达 Markdown 正文与 `public/` 静态文件，这是漂移的结构性原因。**

---

### P1-2　`llms.txt` 残留已被全站清除的「实测」口径 🔴

`llms.txt` 是全站每一页 HTTP 响应头 `Link: </llms.txt>; rel="describedby"` 的指向目标，**曝光层级最高**，而它目前：

| 行 | 内容 | 问题 |
|---|---|---|
| 3–4 | "based on **paid, first-hand testing**. Every platform review records **how many readers were tested and what was actually spent**" | 与 2026-09-08 确立的「实测表述必须含糊」红线直接冲突 |
| 8–10 | "**63 readers tested**" / "**49 advisors tested**" / "**46 advisors tested**" | 人数既过时（应为 64/49/49）又属具体实测声明 |
| 53 | "after **15+ test sessions** of 20+ rounds each" | 具体测试次数 |

**本项与 2026-09-08 的降风险改造不一致**：那次改造覆盖了 19 个 src 文件，但 `public/` 下的静态文件不在 grep 范围内，因此 `llms.txt` 成为**唯一漏网且曝光最高**的表面。

（附：`llms.txt` 的 41 条链接**全部可解析，无 404** —— 技术质量本身是好的，问题在口径。）

---

### P1-3　214 个 Product 结构化数据节点 100% 无效 🔴

**证据**：线上抓取统计 → `Product` 节点共 214 个，**覆盖情况 `{NONE: 214}`**，即全部缺失 `offers` / `aggregateRating` / `review` 三者。

来源为 comparision 页的 `ItemList`：

```json
{"@type":"ListItem","position":1,
 "item":{"@type":"Product","name":"Kasamba","url":".../reviews/kasamba/"}}
```

**Google 规则**：Product 富结果要求具备 `offers` 或 `review`/`aggregateRating` 之一，否则会报「缺少必填字段」，且不产生任何富结果。目前这 214 个节点是**纯负担**。

**修复二选一**：① 把内层 `@type` 改为 `Thing` 或 `WebPage`（保留 ItemList 语义即可）；② 补 `aggregateRating` + `offers`（本站 schema 中已有 Rating 节点可复用）。推荐 ①，改动最小、无虚假数据风险。

---

### P1-4　`/terms/` 与 `/es/terminos/` 三个索引信号互相打架

| 信号 | `/terms/` 状态 |
|---|---|
| `<meta name="robots">` | `index, follow` |
| `robots.txt` | `Disallow: /terms/` |
| sitemap | 被 filter 排除 |

同一 URL 被三个信号指向三个方向：Google **无法抓取**（robots 拦）→ **看不到** `index,follow` → 但该页**被页脚全站链接**，因此仍可能以「无标题无摘要」的裸 URL 形式出现在 SERP。

对照 `/privacy/`（`noindex` + 被 Disallow）与 `/disclosure/`（`index,follow` + 在 sitemap 中）—— 三页处理逻辑互不一致。

**修复**：法律页统一为「不 Disallow + `noindex,follow` + 排除 sitemap」，或「不 Disallow + `index,follow` + 进 sitemap」。二选一，全站一致。

---

### P1-5　ES hreflang 集群三处不闭合

| 页面 | 问题 | 根因 |
|---|---|---|
| `/reviews/purple-garden/` | 声明 `es`/`es-419` → `/es/resenas/purple-garden-es/`，但该 ES 页**不反向声明 `en`** | `src/pages/es/resenas/purple-garden-es/index.astro` 用 `EsBaseLayout` 时**未传 `hreflangEn`**；而 `astro.config.mjs` 的 sitemap 映射却声明了 es↔en，**on-page 与 sitemap 两套信号冲突** |
| `/es/resenas/purple-garden-es/luna-aestethic/` | `en` 与 `x-default` 指向**首页** | frontmatter `hreflangEn: "https://easternalignment.com/"` 填错（模板 `_plantilla-resena.md` 明确要求留空） |
| `/es/resenas/purple-garden-es/luz-tarot/` | 同上 | 同上 |

hreflang 是**全有或全无**机制：一组中任何一条不互指，Google 忽略整组。

**修复**：① PG-ES 索引页补 `hreflangEn={`${SITE_URL}/reviews/purple-garden/`}`；② 两个 md 文件把 `hreflangEn` 置空。

---

### P1-6　`/red-flags/` 空壳页被索引，且被 About 页当作内容引用

- 正文 **42 词**：*"This page is currently under construction. Content will be added here… Check back later for updates."*
- `<title>` 字面包含 **"(Incomplete)"**
- 状态：`index, follow` + **在 sitemap 中**（priority 0.7）
- 且 `src/pages/about.astro:85` 把链接描述为「Partial Negative Case Study Gallery (Incomplete) … just to give you an idea of the kind of behavior I watch out for」

**影响**：低质量页进入索引会拉低整站质量信号，且 About 页（E-E-A-T 关键页）指向一个「施工中」页面。

**修复**：`noindex` 或直接下线，并从 About 页移除该链接；内容补齐后再上线。

---

### P1-7　图片性能：39.4 MB / 单张最大 7 MB / 无现代格式 / 35% 缺尺寸

| 指标 | 实测 |
|---|---|
| 图片总量 | 174 个文件 / **39.4 MB** |
| 格式 | `.jpg` 159 + `.png` 15 —— **无 WebP / AVIF，无 `srcset`** |
| 最大单张 | `/avatars/keen/regina-jacks.jpg` = **7,059 KB** |
| 次大 | allmyangels 1,927 KB、krys-britton 1,702 KB、mystic-raj 1,702 KB、intuitive-jade 1,652 KB、master-sher 1,253 KB |
| `<img>` 总数 | 5,410（线上 5,321） |
| 缺 `alt` | **0** ✅ |
| `loading="lazy"` | 4,203（77.7%） |
| **缺 width+height** | **1,869 / 5,410 = 34.5%** → 累计布局偏移（CLS）风险 |

一张 7 MB 的顾问头像在移动端 4G 下需要约 20 秒才能下载 —— 而这发生在评测页首屏。

**修复优先级**：① 全部头像转 WebP（预计体积降 70–85%）；② 单张限制 ≤100 KB（1600px→400px 显示尺寸过采样严重）；③ 补齐 `width`/`height`。

---

### P1-8　`/go/` 变现路径被 Cloudflare Managed Challenge 拦截（403）

**实测**：

```
GET /go/kasamba/  →  HTTP 403   Cf-Mitigated: challenge
```

内容页与静态资源均返回 200，**只有 `/go/*` 被挑战**。这不是「门禁逻辑」，而是 Cloudflare 边缘在页面到达前下发的质询。

**影响**：
- 真实浏览器（JS 正常）通常能透明通过，但会叠加 1–2 秒延迟 —— 恰好落在转化关键路径上
- 非 JS / 受限环境（部分 App 内浏览器、隐私工具、老旧设备）会**硬阻**→ 直接损失佣金
- 顺带后果：`public/_headers` 里给 `/go/*` 新增的 `Cache-Control: no-store` 与 `X-Robots-Tag: noindex, nofollow` **在质询响应上无法生效**，该规则目前形同虚设（无浏览器通过质询前不可验证）

**动作**：确认该挑战是否为有意配置（若为 Bot Fight Mode / WAF 自定义规则请核对）。站点已具备量化工具：`src/pages/go/[...slug].astro` 上报 `aff_go_hit` / `aff_go_blocked`，并在 PostHog 中可直接算 **到达→放行率 = (hit − blocked) / hit**；`nav_ttfb_ms` / `click_to_go_ms` 可定位延迟来自质询还是代码。建议先看这两个指标的现有数值再决定是否放宽。

---

### P1-9　页面体积与渲染阻塞资源

| 页面 | HTML 体积 | 内容 |
|---|---|---|
| `/coupons/` | **539 KB** | 171 张图、2 个 `<main>`、8 个内联脚本 |
| `/reviews/kasamba/` | **300 KB** | 74 张图、13 个内联脚本、7 个样式表 |
| `/reviews/purple-garden/` | 247 KB | |
| `/reviews/keen/` | 243 KB | |
| 全站 HTML 中位数 | 127 KB | |

**每个页面加载 3–7 个渲染阻塞样式表**（分布：3×10 页、4×12 页、5×113 页、6×162 页、7×3 页）。
**全站 4,741 个内联 `<script>`（平均 8.4 个/页），0 个外部脚本。**

**附带影响**：`functions/_middleware.js` 的 markdown 内容协商设有 **150 KB 上限**，超过则退回 HTML。`/coupons/`、`/reviews/kasamba|keen|purple-garden/` 这 4 页因此对 AI 智能体退化为带导航与侧栏的 HTML —— 恰好是流量最大的 4 个页。

---

## 3. P2 — 中优先级

| # | 项 | 实测数据 |
|---|---|---|
| P2-1 | 标题过长 | 154 页 > 60 字符，129 页 ≥ 65，81 页 > 70，**20 页 > 80**；最长 126（`/reviews/purple-garden/tarot-instincts/`）。中位 61 |
| P2-2 | 描述长度失衡 | **91 页 > 160 字符**（最长 267）；**21 页 < 70**（最短 13 → `/guides/purple-garden-journeys-guide/`）。`/methodology/` 仅 17 字符 |
| P2-3 | Article schema 字段缺失 | `image` 缺 132 处（其中 **56 个可索引页**，如全部 comparison 页）；`datePublished`/`dateModified` 各缺 5 处（4 个可索引） |
| P2-4 | sitemap 无 `lastmod` | 299 条记录**零个 lastmod**（`astro.config.mjs` 的 `serialize` 未设置）。对依赖新鲜度重抓的评测站不利 |
| P2-5 | `/es/404/` 同时进 sitemap 又是 noindex | 应排除 |
| P2-6 | 6 个页面含两个 `<main>`（无效 HTML + 破坏地标） | `/comparisons/`、`/coupons/`、`/guides/`、`/reviews/`、`/astrology/`、`/astrology/zodiac/aries/` |
| P2-7 | 全站 **0 个 skip link** | 383 页均有 `<main id="main-content">`，但无「跳到正文」链接 |
| P2-8 | 品牌后缀不一致 | 90 页 `— Eastern Alignment`；**6 页 `\| EasternAlignment`（缺空格）**；1 页 `— Content Manager`（内部工具标题泄漏） |
| P2-9 | 外链仅 2 条，其中 1 条未标 sponsored | 全站（300 页）仅 `/methodology/`→Wikipedia、`/reviews/kasamba/`→`kasamba.com/psychic-reading/cheap`。后者是**指向变现平台的 dofollow 商业链接，无 `rel="sponsored"`**，且是唯一绕过 `/go/` 门禁的出口 —— 触碰 Google 链接政策（`src/content/reviews/kasamba.md:104`） |
| P2-10 | 安全响应头缺失 | 线上仅 `X-Content-Type-Options: nosniff` + `Referrer-Policy`。**无 HSTS、无 CSP、无 Permissions-Policy、无 X-Frame-Options**（首访存在 SSL-strip 窗口，对含变现跳转的站点值得开 HSTS） |
| P2-11 | 无 Cookie / 同意层 | 站点运行 PostHog（默认 `localStorage+cookie` 持久化、`autocapture: true`）与联盟点击追踪，但**没有任何同意提示组件** → EU/UK 访客存在 GDPR / ePrivacy 暴露（站点确有 GB 真实流量） |

### 附：`public/_headers` 的两处认知偏差

1. 注释声称「只有 `Link` 头会被重复」——**实测首页四个响应头全部双份**：`Cache-Control: public, max-age=0, must-revalidate, public, max-age=0, must-revalidate`、`Referrer-Policy` ×2、`X-Content-Type-Options` ×2、`Link` ×2。原因是 `/` 与 `/*` 两条规则都匹配首页。当前值等价、无害，但注释与实际不符。
2. `/_astro/*` 的规则写的是 `public, max-age=31536000, immutable`，线上实际是 `public, max-age=31536000, must-revalidate, immutable` —— Cloudflare **合并**而非**覆盖**默认值。因 `max-age` 为一年，`immutable` 在新鲜期内仍生效，功能无碍，但"精确控制"的假设不成立。

---

## 4. P3 — 观察项

1. **内容协商覆盖率**：`/coupons/`(539KB)、`/reviews/kasamba|keen|purple-garden/`(~250–300KB) 超过 150KB 阈值，AI 智能体拿到的是 HTML 而非 markdown
2. **Schema 噪音**：1,183 个 `@type: "Thing"`（弱语义，主要来自 ES 页的 `about` 数组）；920 个无 `@type` 的嵌套节点（多数为合法嵌套，无需处理）
3. **抓取预算**：79 个 `/astrology/*` + 177 个 `/go/*` 共 256 个 noindex 页面仍可被抓取（noindex 需可抓取才有效，属正确设计，但可评估是否需收敛）
4. **首页与页脚指向 noindex 段落**：首页「Zodiac Compatibility Hub / Explore the Zodiac →」卡片与页脚 `/astrology/` 链接均指向 **`noindex, nofollow`** 的 `/astrology/` 全段（该段又被 sitemap filter 排除）—— 三处对待方式不一致
5. **仓库脚本卫生**：`scripts/` 留有 22 个一次性脚本（`final-fix.mjs`、`repair-v2/v3.mjs`、`fix-schema-corruption.mjs` 等），建议归档

---

## 5. 已验证正常项（无需改动）

务必保留这些结论，避免后续"顺手优化"时破坏：

- ✅ **0 组重复 title、0 组重复 description**（线上出现的 3 组重复是 301 跳转页被跟随抓取产生的假象）
- ✅ 0 页缺 title / 缺 description（可索引页）
- ✅ **0 个孤儿页、0 条内部死链**（历史遗留的首页 `/tools/` 死链已在源码修复，新构建与线上均已确认指向 `/astrology/`）
- ✅ 0 个 `<img>` 缺 `alt`
- ✅ 0 页缺 `<h1>`，0 页多个 `<h1>`
- ✅ **JSON-LD 全部可解析，0 个格式错误**
- ✅ 重定向链路全部正确：`www` → 301；`http` → 301；`/reviews/kasamba-psychics/` → 301；`/reviews/keen-psychics/` → 301；AI 幻觉 URL `/comparisons/keen-vs-purple-garden-accuracy-2026/` → 301
- ✅ `og-default.jpg` 实测 **1200×630**，与声明的 `og:image:width/height` 完全一致；`logo.jpg` 实测 1024×817，与 Organization schema 一致
- ✅ `/go/` 177 页全部 `noindex, nofollow`，且**无 canonical**（正确：不该有）
- ✅ 11 个 ES 页 `lang="es"` 正确；EN 550 页 `lang="en"` 正确
- ✅ sitemap 299 条与构建产物**零偏差**（无指向未构建页面的条目）
- ✅ `llms.txt` 41 条链接**全部可解析**
- ✅ `/go/*` 门禁设计完整（referrer / sessionStorage / `ea_sub` 三重判定 + 幂等 `$insert_id` + sendBeacon 双通道）
- ✅ AI 爬虫 UA 实测未被 WAF 硬拦（`GPTBot`/`ClaudeBot`/`Google-Extended`/`CCBot` 均返回 200 —— 限制来自 robots，非 WAF）

---

## 6. 修复清单（按执行顺序）

### 第一批 — 今天（改动极小、收益确定）

| # | 动作 | 文件 | 改动量 |
|---|---|---|---|
| 1 | 删除 4 个 Keen 页的 `canonicalUrl` 行并修正 `hreflangEn` | `src/content/readers/keen/{flora-knows-all,love-psychic-victoria-sands,psychic-suzen-on,psychicreader19622-raymond}-*-2026.md` | 删除 4 行 |
| 2 | 确认 Cloudflare AI 爬虫策略是否符合预期 | Cloudflare 控制台（无需改代码） | — |
| 3 | `hreflangEn` 置空 | `src/content/es-readers/purple-garden-es/{luna-aestethic,luz-tarot}.md` | 2 行 |
| 4 | PG-ES 索引页补 `hreflangEn` | `src/pages/es/resenas/purple-garden-es/index.astro` | +1 行 |
| 5 | `/red-flags/` 改 noindex（或下线）并移除 About 页链接 | `src/pages/red-flags/index.astro`、`src/pages/about.astro` | 2 处 |
| 6 | 修正 158→162 口径 | `most-accurate-love-psychics.md`、`before-you-pay-for-a-psychic-reading.md`、`top-love-psychics-online.md`、`public/llms.txt` | 4 文件 |
| 7 | 清理 `llms.txt` 的「实测」口径 | `public/llms.txt` | 3 处措辞 |

### 第二批 — 本周

| # | 动作 |
|---|---|
| 8 | Product schema 内层 `@type` 改 `Thing`（或补 `aggregateRating`） |
| 9 | `/terms/` 与 `/es/terminos/` 索引信号统一（三选一，全站一致） |
| 10 | 头像批量转 WebP + 限制尺寸 + 补 `width`/`height` |
| 11 | comparison 页与 3 个 guide 补 Article `image` / `datePublished` |
| 12 | 4 个超长标题（>100 字符）与 21 个过短描述（<70 字符）修正 |
| 13 | `astro.config.mjs` 的 sitemap `serialize` 增加 `lastmod`；排除 `/es/404/` |
| 14 | 6 个双 `<main>` 页面修正；全站补 skip link |
| 15 | `/reviews/kasamba/` 的 `kasamba.com` 直链加 `rel="noopener sponsored"` |

### 第三批 — 结构性

| # | 动作 |
|---|---|
| 16 | 把 Markdown 正文与 `llms.txt` 中的人数口径改为构建期注入（根治漂移） |
| 17 | 评估把每页 3–7 个样式表合并为 1 个（Astro 构建配置） |
| 18 | 云层开启 HSTS；评估最小化 CSP |
| 19 | 为 EU/UK 访客补同意层（或在 PostHog 关闭 cookie 持久化 + 关 autocapture 以降低暴露） |
| 20 | `/go/*` 挑战策略复核（先读 PostHog 的 `aff_go_hit` / `aff_go_blocked` / `click_to_go_ms`） |

---

## 7. 审计过程中一并处理的本地问题

**审计开始时**：`dist/` 处于损坏状态 —— 406 html / 550 文件，**缺 `index.html`、`sitemap-index.xml`、`sitemap-0.xml`、`robots.txt`、`_headers`，以及 `about/`、`es/`、`comparisons/` 等整段目录**（基线为 563 html / 765 文件）。这是典型的「构建被中断后只重建了一部分」事故形态。

**处理**：
1. 用正确入口 `node ./node_modules/astro/bin/astro.mjs build` 重建 → `✓ Completed in 19.15s`，563 页
   - ⚠️ 前两次尝试挂死（进程存活但 CPU 归零、日志停在 `Collecting build info`）。**根因是我自己并发启动了构建**；清理 `.astro` 缓存并单独重跑即成功。astro 进程渲染完成后仍不退出（`timeout 300` 被杀），这是已知行为，与记忆中的记录一致。
2. `rm -rf dist/.prerender`（清掉 32 个残留文件）
3. sitemap 缺失 → **先 diff 新 dist 与备份的页面集合，结果完全一致** → 直接复用备份的 `sitemap-0.xml`(118,220 B) 与 `sitemap-index.xml`(191 B)

**最终状态**：563 html / 767 文件 / `go/` 177 页 / sitemap 299 条 / sitemap-index 191 B。
（767 = 765 + 2，多出的 2 个是 9/8 之后新增的 `_headers` 与 `_routes.json`，属预期差异。）

---

*本报告的每一条结论均基于线上实测、源码检查或完整重建产物之一；已明确区分「线上确认」「源码确认」「仅产物体现」。*

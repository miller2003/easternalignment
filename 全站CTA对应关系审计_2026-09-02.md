# 全站 CTA ↔ 页面 ↔ /go/ 落地对应关系审计报告

**日期**：2026-09-02
**范围**：dist 全部 360 个内容页（/go/ 的 169 个 gated 页单独核对）· 2,000+ 条 /go/ 链接 · 169 个 slug 的最终落地 URL
**方法**：脚本全量解析 dist 渲染后 HTML（按 `data-cta-source` 分类 CTA 表面）→ 每条链接五层校验：①slug 是否存在于 affiliateLinks.ts ②落地域名是否匹配平台 ③落地 profile 是否匹配本页顾问 ④页面身份（URL + frontmatter）vs CTA href ⑤存活实测（直连平台页面 + TUNE 重定向 + 搜索引擎交叉验证）
**原始数据**：`scratch/audit20260902_cta/findings.json`（脚本：同目录 `audit_cta_aff_mapping.py`）

---

## 一、对之前 AI 诊断的复核结论：两条都不成立

### ❶ 「master-sher 页的 CTA slug 指向其他顾问」→ **误报**

master-sher 页（/reviews/keen/master-sher/）全部 /go/ 链接逐一核实：

| 链接 | 上下文 | 判定 |
|---|---|---|
| `/go/keen-master-sher/` ×6 | hero CTA、deal strip、**end CTA**、inline CTA、右栏 deal、side tab | ✅ 六大 CTA 表面全部指向本页顾问 |
| `/go/keen/` ×2 | topbar、左栏 Keen deal 卡 | ✅ 平台级，页面平台就是 keen |
| `/go/kasamba/` `/go/purplegarden/` | 左栏三平台 deal 卡（设计如此，按优先级全列） | ✅ |
| `/go/keen-david7` 等 8 条 | 「Other Keen Readers」卡片 + 正文 reader-summary 块 | ✅ 每张卡本来就是那个顾问自己的 CTA，设计内 |

之前的 AI 大概率是抓到 HTML 里存在 `keen-david7` 等 slug、没看上下文就下了结论。

### ❷ 「Kasamba 文案的 slug 是 keen」→ **误报**

- 158 个 reader 页 frontmatter `affiliateUrl` 平台前缀 vs 页面平台段：**0 例跨平台**。
- 全部 reader 页 end CTA（431 处）、side tab（347 处）、hero（412 处）、inline CTA、topbar（255 处）：**0 例错配、0 例跨平台**。
- 渲染 href vs frontmatter：**100% 一致**（构建注入无误）。

---

## 二、真正查出的问题

### 🔴 P0-1｜tarot-by-elena 页的 CTA 落到已下线的顾问页（英文站，收入级）

`/reviews/purple-garden/tarot-by-elena/` → end CTA `/go/purple-garden-elena/` → `purplegarden.co/psychics/11714-psychic-elena`

实测：该 profile 页只剩 58KB 空壳（正常顾问页 570KB+，内嵌顾问 JSON），**无任何顾问数据**；搜索引擎也已无此 profile 索引。判定与 keen-raj 当时的情况同类：**顾问已离开 Purple Garden**。买家从评测页点「Book」会落到 PG 的空/错误态页面。

**修复建议**（沿用 keen-raj 的既有处理模式）：`tarot-by-elena.md` 加 `unavailable: true`（end CTA 自动回落 `/go/purplegarden/` 通用优惠页），或直接把 `affiliateUrl` 改为 `/go/purplegarden/`；同时评审正文加一句现状说明。

### 🔴 P0-2｜西语站 2 个页面的侧栏 CTA 按钮是 404

`/es/resenas/purple-garden-es/luna-aestethic/` 与 `/luz-tarot/` 的左栏两张 deal 卡按钮指向 `/go/psiquicos/` 和 `/go/purple-garden-es/`——**这两个 slug 从未在 affiliateLinks.ts 注册**，点击即 404。

根源：`src/components/es/EsLeftSidebar.astro` 第 25、48 行硬编码了这两个 slug，而西语主 CTA（EsSpanishCTA）用的是直连 TUNE，slug 体系两套脱节。

**修复建议**：二选一——①在 affiliateLinks.ts 补这两个 slug（psiquicos → offer_id=42，purple-garden-es → offer_id=34，与 EsSpanishCTA 的 OFFERS 保持同参）；②把 EsLeftSidebar 的 href 改成与 EsSpanishCTA 相同的 TUNE 直链（但见 P1-1 的追踪代价）。

### 🟠 P1-1｜西语站主 CTA 绕过 /go/ 门控 → PostHog 完全无埋点（对账盲区）

`EsSpanishCTA.astro` 直链 `bargestech.go2cloud.org/aff_c?...`，而 PostHog 点击拦截只匹配 `/go/` 链接。后果：

1. 西语站所有主 CTA 点击**不产生 `affiliate_link_click` 事件**——如果按 PostHog 对账 TUNE 后台，西语站的量会全部「消失」，这正是「收入在对账时无声流失」的真实形态（只是发生在西语站，不是英文站）。
2. 直链暴露在 HTML 里，bot 可直接打 TUNE 链接，门控失效。

**修复建议**：给 `psiquicos` / `purple-garden-es` 建 /go/ slug（即 P0-2 的方案①），EsSpanishCTA 改为输出 `/go/<slug>/`，与英文站机制对齐。

### 🟡 P2｜两个观察项（不需急办）

1. **mystic-raj** end CTA 落 Keen 通用页——有意为之（profile 已死，affiliateLinks.ts 有注释），处理方式正确；建议给 elena 做同样处理。
2. **offer_id 口径**：`keen-intuitive-jade` 和 `keen-suzen` 用 **offer_id=209**（实测有效，落 Keen `lp/psychic-advice-v2` 落地页），其余 keen 链接用 221。TUNE 后台是按 offer 分报表的——**对账时这两页的转化在 209 的报表里**，只看 221 会以为丢了量。如介意口径分裂，可让联盟把两页迁到 221。
3. `/go/keen-love-lake/` 孤儿页：slug 存在但全站无入链（无对应 review），无害，留档即可。

---

## 三、验证通过的部分（大盘健康）

- **169 个 /go/ slug**：167 个有入链且落地平台与 slug 前缀 100% 一致（keen.com / kasamba.com / purplegarden.co）。
- **4 个「疑似顾问错配」全部实测为同一人**（平台 URL slug ≠ 显示名，属正常差异）：
  - `annah` → `308-annaleota`：实测 nickname **"Annah"**、37,343 readings、2015 年加入、5.0 星，与评审数据吻合 ✅
  - `empathic-intuitive-marcus` → `2146-marcus-andy`：实测 about_me「读能量/意图/行为模式」与评审定位一致 ✅
  - `twin-flame-specialist-aria` → `2270-readings-by-aria`：实测 love/tarot 定位一致 ✅
  - `intuitive-jade` → `isis-jade/6292704`：实测 display name **"Intuitive Jade"**、4.97 星、8,261 ratings、28,107 readings since 2006、name-vibration 方法，与评审完全吻合（另有同 handle 小号 6650895，我们链的是数据吻合的大号）✅
- **705 个 reader-summary 块**（guide 页内嵌推荐卡）：aff 链接与卡片顾问头像/名称 **0 例错配**。
- 158 个 reader 页全部有构建产物，无缺失；158 个 frontmatter affiliateUrl 全部存在且无跨平台。
- 正文相关文章里的 12 条跨平台链接均为平台级通用链接（/go/keen 等），非顾问错配。

---

## 四、建议动作清单

| 优先级 | 动作 | 涉及文件 |
|---|---|---|
| P0 | elena 页标记 unavailable / 回落通用 PG 链 | `src/content/readers/purple-garden/tarot-by-elena.md` |
| P0 | 补注册 `psiquicos`、`purple-garden-es` 两个 slug | `src/data/affiliateLinks.ts` |
| P1 | EsSpanishCTA 改走 /go/（恢复埋点） | `src/components/es/EsSpanishCTA.astro` |
| P2 | 对账时分 offer 209 / 221 两个报表看 | TUNE 后台操作，无代码 |

---

## 五、处置记录（2026-09-02 第二轮，用户 5 点指示后）

### ✅ P0-1 elena → 已按「标记不删页」处置

1. `affiliateLinks.ts`：`purple-garden-elena` 重指向通用 PG 优惠（offer_id=30），点击全部落活页。
2. `purple-garden/[reader].astro`：从 keen 模板移植 `unavailable` 机制（CTA 文案/按钮/URL + FAQ 三问切换；用 `readerDisplayName` 变量而非硬编码人名）。按钮变 **"See Live Purple Garden Advisors →"** → `/go/purple-garden/`。
3. `tarot-by-elena.md`：frontmatter 加 `unavailable: true` + `updatedDate: 2026-09-02`；正文顶部加英文状态说明（可能已离开 PG，评审作为档案保留，$30 credit 对任何顾问有效）。标题/H1/正文未动（GEO 锁定规则）。

### ✅ P0-2 + P1-1 西语站 → 统一进 /go/ 管线（不需要 /go2/）

4 个新 slug 注册进 affiliateLinks.ts：`psiquicos`（offer 42）、`purple-garden-es`（offer 34）、`purple-garden-es-luz-tarot`、`purple-garden-es-luna-aestethic`（均 offer 34 + url= 深层落地）。EsSpanishCTA 的 OFFERS/READER_URLS 全部改为 `/go/<slug>/`。**结论：不建 /go2/——同一 /go/ 管线统一复用，门控与 PostHog 埋点自动恢复**（PostHog 拦截正则匹配任意 `/go/<slug>/`）。

### ✅ mystic-raj 下线查证（用户要求查清楚）

- 实测 `keen.com/psychic-readings/mystic-raj/12475371`：现返回 **"Love And Relationships" 通用分类页**（列出 siobhandarroch、michael kelley、david7 等），无 Mystic Raj —— Keen 已下架该 profile。
- WebSearch：Mystic Raj 仅出现在其他平台（emystics.co.uk、76psychics.com、seekraj.me），Keen 无踪。
- 结论：**确实已离开 Keen，原重指向处理正确**。

### 📋 给用户的官方链接（供 TUNE 后台做深层链接）

| Offer | 官网 | 现有 slug |
|---|---|---|
| 42 | `https://www.psiquicos.net/` | `psiquicos` |
| 34 | `https://www.purplegarden.co/es` | `purple-garden-es`（+ 两个顾问深层 slug） |

用户从 TUNE 后台拿到深层链接后，替换 affiliateLinks.ts 中对应 `url=` 参数即可（offer_id/aff_id 保持不变）。

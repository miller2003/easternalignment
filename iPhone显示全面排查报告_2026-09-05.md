# iPhone 显示全面排查报告（2026-09-05）

**测试方法**：Chromium + iPhone 仿真（390×844、DPR 3、触屏、iOS Safari UA，触发全站 iOS 专用代码分支），实测线上 5 类代表页面（首页 / reader 评测页 / guides 页 / noCta 页 / Kasamba 聚合页），截取首屏、中滚、底部、全页及加载中逐帧画面，并采集 DOM 指标（溢出、定位、叠放、触控目标、JS 报错）。iOS 26 Safari 特有行为另以公开资料交叉验证。
**原始数据**：`scratch/iphone-audit-20260905/results.json` + `shots/`（22 张截图）。

---

## 一、直接回答两个问题

### 1. "顶部 CTA 栏会被 iPhone Safari 地址栏挡住"——代码没错，是 iOS 26 的坑

**仿真实测：顶部三层叠放完全正确。** 滚动后 offer bar 钉在 top:0（36px），header 钉在其下（36–96px），z-index 110 > 100，逻辑无误。在经典 iOS（地址栏在底部）下，sticky 顶栏**物理上不可能**被底部地址栏遮挡——它在网页视口内部。

你观察到的问题，最可能的解释是 **iOS 26（Safari 26，Liquid Glass 浮动工具栏）的已知 bug 家族**：

- iOS 26 的地址栏/搜索栏是**浮动在网页内容之上**的玻璃层，滚动时展开/收起会覆盖页面边缘的 fixed/sticky 元素。这是被大量报道的 iOS 26 回归，"使用 fixed/sticky 边缘元素的站点受影响最大"（Apple 尚未彻底修复）。
- **触发条件最关键的一条：你的 Safari 地址栏设在顶部**（设置 → App → Safari → 标签页页栏 → 顶部）。地址栏在顶部时，浮动玻璃条展开就会正好压住你的 offer bar。请先确认这个设置——若地址栏在底部而顶栏仍被遮，那就是 iOS 26 的合成 bug，用户侧可尝试重启/清缓存，但根因在系统。
- **本站有一个加重因素**：Header 把 `background: rgba(...)` + `backdrop-filter: blur(12px)` 直接写在 sticky 元素**本身**。WebKit 社区针对 Safari 26 的明确修法是：fixed/sticky 元素本身必须透明，视觉背景/毛玻璃移到 absolute 子元素——否则会触发 Safari chrome 的着色/合成异常。这是全站唯一一处与 iOS 26 官方建议相冲突的写法。

### 2. "侧边按钮不太顺"——无跳变，但有三处"总在动"的源头

慢速 3G 下逐帧观察（每 450ms 一帧）：侧边 tab 一出现就在 iOS 正确位置（top 354px = 42vh），**JS 切换无位置跳变**，这个嫌疑排除。真正的不适感来自：

| # | 现象 | 机制 |
|---|---|---|
| ① | 每次翻页它都从屏幕外滑入一次（0.55s） | 入场动画 `sd-in` 每页必播，与组件注释"renders immediately on first paint"的设计意图自相矛盾 |
| ② | 光扫动画无限循环（每 5.2s 一次） | fixed 元素上的永久动画 = 持续 GPU 合成，iOS 滚动中易掉帧、耗电 |
| ③ | 点过之后按钮保持"按下态"（左移+变亮不消失） | `:hover` 在触屏上粘滞，应包进 `@media (hover: hover)` |

---

## 二、确认的真问题（按优先级）

### P0 — 首屏 chrome 过高，转化钩子被压到半屏以下
第一屏：disclosure 80px（灰条在 390px 下换行成 **4 行**）+ offer bar 36px + header 60px = **176px**。iPhone Safari 地址栏展开时可用视口约 664px → **26.5% 的首屏不是内容**。reader 页首屏只能露出 H1 的前两行，H1（全文钩子）被推到视觉中线以下。
**建议**：移动端 disclosure 压成一行短句（如 "I test these platforms myself. Links fund my research — how it works."，约 32px），全文保留在 /disclosure/ 页。首屏立刻多出 ~48px 内容。这是收益最大、风险最小的一处。

### P1 — TopOfferBar 移动端文案截断
实测显示 "Kasamba Special Offer — 3 FREE minutes + 50%…"，**"50% off"这个最有转化力的信息被省略号吃掉**。移动端已按设计去掉 Claim 胶囊，但文案仍超宽。
**建议**：`offers.ts` 增加 `topBarLineShort`（如 "Kasamba: 3 FREE minutes + 50% off →"），CSS 在 ≤640px 切换显示，保证移动端完整露出价格锚点。

### P1 — Header 毛玻璃写法与 Safari 26 冲突（见第一节）
**建议**：把 `.site-header` 的 background/backdrop-filter 移到一个 `position: absolute; inset: 0` 的子元素上，sticky 父元素本身保持透明。这是 WebKit 社区针对 iOS 26 浮动工具栏着色/遮挡异常的标准修法。

### P1 — 侧边 tab 三处动效收敛（见第一节）
移动端：`animation: none` 去掉入场滑入；光扫动画停掉（或 `animation-iteration-count: 2`）；`:hover` 包 `@media (hover: hover)`。桌面端保持不变。

### P2 — 触控目标偏小
- 汉堡按钮 32×32px、顶栏关闭钮 28×28px，均低于 Apple HIG 44pt 建议。
- 建议：视觉尺寸不变，用透明 padding 把热区撑到 ≥40px。顶栏关闭钮尤为重要——现在按偏一点就会直接点到背后的联盟链接（误触跳出）。

### P2 — viewport 缺 `viewport-fit=cover`（可选加固，需真机回归）
当前 meta 是 `width=device-width, initial-scale=1.0`，导致全站 `env(safe-area-inset-*)` 全部是死代码（SideOfferTab 底部偏移里的 safe-area 实际恒为 0）。加 cover 后需同步给顶部 chrome 补 `env(safe-area-inset-top)` 内边距。**会改变刘海屏布局，必须真机验证后再上**，列为 P2。

### P3 — 杂项
- noCta 页面包屑 "Home / Guides / More Guides /" 结尾多一个悬空的 "/"，当前页 crumb 缺失。
- reader 页 H1 第一行 "Review" 的尾字母 w 被侧边 tab 遮住；正文每行末约 1 个字符在 tab 覆盖区——属 2026-08-30 已确立的"接受轻微重叠"原则范围，仅记录，不重议。

---

## 三、实测确认没有问题的部分（不必动）

- **零横向溢出**：5 个页面 scrollWidth 均 = 390，无左右滑动手感。
- **sticky 三层叠放**（disclosure → offer bar → header）滚动中位置全部正确，无 JS 报错。
- **移动菜单**：开合正常，493px 高不溢出视口，子导航（Reviews 下拉）清晰可达。
- **转化组件渲染优秀**：Reader 卡片（头像/星级/价格胶囊/双按钮）、InlineCta 框、FAQ 手风琴（整行大触控区）、Footer 深色区在 390px 下均干净整齐。
- **主 CTA 按钮** 308×48px 触控达标；侧边 tab 在 reader 页正确深链 `/go/kasamba-master-enigma/`。
- **noCta 页**正确关闭了全部 CTA 部件（只剩 header sticky）。
- 侧边 tab 位置在 iOS 模式下三个滚动位次恒定（top 恒 354px），无抖动。

---

## 四、建议的落地顺序

1. **P0** disclosure 移动端单行化（TopDisclosureBar.astro 一处 CSS/文案）
2. **P1** 侧边 tab 动效三件套（SideOfferTab.astro，仅移动端媒体查询内改动）
3. **P1** TopOfferBar 移动端短文案（offers.ts + TopOfferBar.astro）
4. **P1** Header 毛玻璃下沉到子元素（Header.astro）
5. **P2** 触控热区扩大（Header.astro / TopOfferBar.astro）
6. **P2** viewport-fit=cover + safe-area 适配（真机回归后决定）

1–5 均为低风险改动，不涉及正文布局、不改变 tab 常驻形态，与既有原则（正文不动、浮层不收起）无冲突；构建后建议用你的 iPhone 实测一遍再定 P2。
